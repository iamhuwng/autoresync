# PRD0062 Ticket 25 / #40 blocker

## Owner

Canonical PRD0062 acceptance/coverage ownership decision. The published #37
matrix contract and #40/#73 runtime contracts currently disagree.

## Required action

Choose and publish one destination-first canonical contract:

1. Keep #40 as a dormant long-response renderer/codec producer, remove its
   registration/deployment requirement, and revise #73 so integrated
   long-response proof runs only after a separately owned supported coverage
   classification exists; or
2. Name an authoritative researched long-response Task Profile, add it through
   the coverage owner with schema fixture/evidence, and retain #40/#73
   registration proof.

No generic registration or fabricated matrix row may be added merely to close
#40.

## Evidence

- Live #37 requires the matrix to retain the exact researched IELTS
  Reading/Listening row set and requires registration cross-check failure when
  tickets #38-#40 add a renderer without one supported row.
- `documentation/architecture/data/prd0062-activity-coverage.matrix.json`
  contains 32 rows and zero `long-response` rows.
- `scripts/lib/prd0062-activity-coverage/validator.mjs` emits
  `registration-without-supported-row` for every manifest registration without
  a supported row.
- Live #40 requires production long-response registration loading and a
  validator proof that the registration maps to supported coverage.
- Live #73 currently owns assembled proof for the #40 long-response
  registration.

## Impact

#40 is acceptance-ineligible. Its registration and deployment gates cannot be
executed without violating #37. #73's long-response assembled proof is also
non-executable until the ownership decision is published. No #40 product code,
matrix row, registry entry, or test work may begin meanwhile.
