# Task List: Mobile IELTS Reading Test-Taking Interface

> **Source PRD:** `0043-prd-mobile-ielts-reading-test-taking-interface.md` (v2 gap-closed)  
> **Generated:** 2026-04-07

## Relevant Files

### Files To Create

- `src/core/platform/hooks/useMobileExamMode.ts` - Single source of truth for phone Reading activation (device classification + QA override).
- `src/core/platform/hooks/useMobileExamMode.test.ts` - Unit tests for device classifier, QA override states, fail-safe behavior.
- `src/components/test/mobile/MobileReadingExamScaffold.tsx` - Shared phone Reading presentation scaffold consumed by both live and solo/homework hosts; receives state and callbacks from the hosts.
- `src/components/test/mobile/MobileReadingExamScaffold.test.tsx` - Tests for scaffold rendering, prop passthrough, callback wiring, and overlay rendering from host-provided props.
- `src/components/test/mobile/MobileReadingHeader.tsx` - Compact phone header: timer, passage label, overflow menu trigger.
- `src/components/test/mobile/MobileReadingHeader.test.tsx` - Tests for header content, timer display, overflow menu.
- `src/components/test/mobile/MobilePassageTabs.tsx` - Horizontally scrollable sticky passage tab strip, reusable in page and sheet contexts.
- `src/components/test/mobile/MobilePassageTabs.test.tsx` - Tests for tab rendering, active state sync, scroll-to-active behavior.
- `src/components/test/mobile/MobileQuestionsFab.tsx` - Dynamic floating `Questions X/Y` button with unanswered/flagged badges.
- `src/components/test/mobile/MobileQuestionsFab.test.tsx` - Tests for dynamic label, badge counts, safe-area positioning.
- `src/components/test/mobile/MobileQuestionSheet.tsx` - Near-fullscreen bottom sheet wrapper with slide-up/down animation, backdrop, close gestures.
- `src/components/test/mobile/MobileQuestionSheet.test.tsx` - Tests for open/close, animation, close triggers (X, swipe, backdrop, back), state preservation.
- `src/components/test/mobile/MobileQuestionSheet.css` - Sheet animation keyframes and z-index styles.
- `src/components/test/mobile/MobileReviewSummary.tsx` - Passage-grouped pre-submit review screen with question chips.
- `src/components/test/mobile/MobileReviewSummary.test.tsx` - Tests for passage grouping, counts, chip navigation, severity states.
- `src/components/test/mobile/MobileTextSizeControl.tsx` - Text-size slider (14px–22px, 1px steps) used from overflow menu.
- `src/components/test/mobile/MobileTextSizeControl.test.tsx` - Tests for range, step, default, persistence trigger.
- `src/components/test/mobile/MobileInstructionsModal.tsx` - Read-only modal: mode-specific rules recap + controls help.
- `src/components/test/mobile/MobileInstructionsModal.test.tsx` - Tests for mode-specific content rendering.
- `src/components/test/mobile/mobileInstructionsContent.ts` - Shared source of truth for mode-specific rules recap and controls-help copy, used by both the mobile start screen (Task 2A) and the in-test instructions modal (Task 8.3). Created in Phase 2A.
- `src/components/test/mobile/MobileStartScreen.tsx` - Mobile-optimized start/instructions screen with mode-specific rules and dominant Start button (FR-24–29).
- `src/components/test/mobile/MobileStartScreen.test.tsx` - Tests for mode-specific content, Start button, compact layout.
- `src/components/test/mobile/MobileOverflowMenu.tsx` - Overflow dropdown: Text size, Review answers, Submit test, Leave test, Instructions/Help.
- `src/components/test/mobile/MobileOverflowMenu.test.tsx` - Tests for menu items, z-index, open/close behavior.

### Files To Modify

- `src/components/practice/IELTSPracticeView.tsx` - Phase 0: migrate `useNavigate` → `useNavigation`, `localStorage` → platform storage. Then: add `useMobileExamMode()` branch, own the solo/homework mobile UI state contract, and pass state/callbacks into the phone scaffold.
- `src/skills/reading/components/ReadingTestPage.tsx` - Add `useMobileExamMode()` branch, own the live mobile UI state contract, hydrate from session data, and preserve desktop/tablet rendering.
- `src/constants/routes.ts` - Add the missing `STUDENT_ACADEMIC_RECORD` route key so the Phase 0 navigation migration can stay on the route registry.
- `src/types/navigation.types.ts` - Extend `NavigationOptions` to support explicit router `state` payloads during `navigateTo()` migrations.
- `src/services/navigation.service.ts` - Forward `NavigationOptions.state` through to React Router so migrated flows preserve existing location-state behavior.
- `src/components/test/IELTSQuestionsPanel.tsx` - Add `embedded` layout prop to suppress internal scrolling when rendered inside the mobile sheet.
- `src/components/test/QuestionNavigator.tsx` - Add `collapsible` variant prop and horizontal-scroll single-row mode for mobile.
- `src/components/test/QuestionNavigator.test.tsx` - Tests for collapsible mode, horizontal scroll, show-all toggle.
- `src/types/practice.types.ts` - Add the shared `SavedMobileState` type and extend solo progress typing to carry mobile Reading resume state.
- `src/hooks/useTestAutoSave.ts` - Extend payload with a typed `mobileState` key containing flagged questions, scroll positions, overlay state, and text size. Add debounced scroll persistence.
- `src/hooks/test/useTestSession.ts` - Surface the current player's saved `mobileState` from RTDB so the live host can hydrate exact phone state on re-entry.
- `src/hooks/solo/useSoloAutoSave.ts` - Same typed `mobileState` extension. Also migrate from direct `localStorage` to platform storage and return autosave status to the host.
- `src/hooks/solo/useSoloResume.ts` - Restore `mobileState` fields on resume (scroll positions, flags, sheet state, text size).
- `src/hooks/test/useTestSubmission.ts` - Add `skipConfirm` parameter to bypass `window.confirm` when mobile scaffold handles review-summary + confirm flow.
- `src/hooks/solo/useSoloSubmission.ts` - Migrate to `useNavigation`, preserve route-state redirects through the navigation abstraction, and add `skipConfirm` for the mobile review flow.
- `src/hooks/solo/useSoloSubmission.test.ts` - Update navigation mocks for the Phase 0 abstraction migration and add `skipConfirm` coverage.
- `src/hooks/test/useFullscreenMode.ts` - Ensure `enabled: false` is respected when mobile exam mode is active for homework.
- `src/core/platform/hooks/useScreenSize.ts` - No changes needed; `useMobileExamMode` composes on top of this hook.
- `src/skills/reading/components/PassageRenderer.tsx` - Guard the internal `localStorage` font-size persistence so it only fires when no external `fontSize` prop is provided, preventing a shadow persistence source on mobile.
- `src/core/platform/storage.ts` - Add the key-enumeration capability needed by `cleanupExpiredProgress()` so the solo autosave migration does not fall back to raw `localStorage`.

### Notes

- Unit tests should be placed alongside the code files they test (e.g., `MobileQuestionSheet.tsx` and `MobileQuestionSheet.test.tsx` in the same directory).
- Use `npx vitest run [optional/path/to/test/file]` to run tests. Running without a path executes all tests found by the Vitest configuration.
- No `@mantine/*` imports are allowed in any new or modified file (FR-104).
- All new files must use platform abstractions — no direct `window.innerWidth`, `matchMedia`, or `localStorage` (FR-19, FR-90).
- `MobileReadingExamScaffold` is a pure presentation component. It must not import submission hooks, storage helpers, or services, and it must not own the PRD Section 7.3 host-state contract via internal `useState`.

## Tasks

- [x] 1.0 Prerequisite Cleanup: Migrate IELTSPracticeView to Platform Abstractions
  - [x] 1.1 Replace `import { useNavigate } from 'react-router-dom'` (line 14) with `import { useNavigation } from '@/hooks/useNavigation'` in `src/components/practice/IELTSPracticeView.tsx`. Remove the `const navigate = useNavigate()` call (line 72). In the same phase, add `STUDENT_ACADEMIC_RECORD: '/student/academic-record'` to `src/constants/routes.ts` because the current route registry does not yet expose that existing page.
  - [x] 1.2 Convert the three path-based destinations in `IELTSPracticeView.tsx` to `navigateTo()` from the navigation hook: `navigate('/student/homework')` → `navigateTo('STUDENT_HOMEWORK')`, `navigate(`/student/courses/${practiceContext.courseId}`)` → `navigateTo('STUDENT_COURSE_DETAIL', { courseId: practiceContext.courseId })`, and `navigate('/student/library')` → `navigateTo('STUDENT_LIBRARY')`. Replace the error-state `navigate(-1)` button with the existing context-aware `handleBack` callback. Do **not** invent a `BACK` route key; no such key exists in `src/constants/routes.ts`.
  - [x] 1.3 Replace the `localStorage.getItem(`solo_student_prefs_${user?.uid}`)` call (line 78) with `await storage.get(`solo_student_prefs_${user?.uid}`)` from `@/core/platform/storage`. Because platform storage is async, the initial state must be loaded via `useEffect` instead of the synchronous `useState` initializer. Default to `DEFAULT_STUDENT_PREFS` until the async load completes.
  - [x] 1.4 Replace the `localStorage.setItem(...)` call (line 91) with `await storage.set(...)` from platform storage. Wrap in try/catch as the existing code does.
  - [x] 1.5 Before migrating `useSoloSubmission.ts`, extend the navigation abstraction to support explicit router state: add `state?: unknown` to `NavigationOptions` in `src/types/navigation.types.ts`, then update `src/services/navigation.service.ts` so `navigateFunction(to, { replace: options?.replace, state: options?.state ?? params })` preserves existing `location.state` flows.
  - [x] 1.6 In `src/hooks/solo/useSoloSubmission.ts`: replace `import { useNavigate } from 'react-router-dom'` with `import { useNavigation } from '@/hooks/useNavigation'`; convert homework completion to `navigateTo('STUDENT_HOMEWORK', undefined, { replace: true, state: { justSubmitted: true } })`; convert solo/course completion to `navigateTo('STUDENT_ACADEMIC_RECORD', undefined, { replace: true, state: { resultId, showResult: true } })`. Add `skipConfirm?: boolean` because the hook currently **does** call `window.confirm(...)` for unanswered manual submissions. When `skipConfirm === true`, bypass that confirm and proceed directly to submission. Default `skipConfirm` to `false` so desktop behavior is unchanged.
  - [x] 1.7 Update `src/hooks/solo/useSoloSubmission.test.ts` to mock `useNavigation` / navigation options instead of `useNavigate`, and add coverage proving `skipConfirm: true` bypasses the unanswered-questions confirm.
  - [x] 1.8 Run `npx vitest run src/hooks/solo/useSoloSubmission.test.ts` and `npx vitest run src/components/practice` to verify no regressions. Fix any failures caused by the migration.
  - [x] 1.9 Commit these changes as a standalone commit titled `fix: migrate IELTSPracticeView to platform abstractions (PRD-0043 Phase 0)`.

- [x] 2.0 Device Classification Foundation and Scaffold Entry Points
  - [x] 2.1 Create `src/core/platform/hooks/useMobileExamMode.ts`. The hook must: (a) import `useScreenSize` from the same platform directory, (b) check `navigator.userAgent` for mobile/handheld signals as the primary heuristic, (c) fall back to `isMobile` from `useScreenSize` plus touch capability (`pointer: coarse`) as secondary, (d) if neither signal is conclusive, return `false` (fail-safe to desktop/tablet). Export `{ isMobileExamMode: boolean }`.
  - [x] 2.2 Add a session-scoped QA override to `useMobileExamMode`. Read the `__qa_mobile_exam_override__` key through `sessionStore` from `@/core/platform/storage`, not direct `sessionStorage`. Supported values: `'auto'` | `'force-mobile'` | `'force-standard'`. Because `sessionStore` is async, load the override in a `useEffect` into hook state that defaults to `'auto'` until the read completes. `force-mobile` returns `true`, `force-standard` returns `false`, and `auto` / missing falls back to the device heuristic.
  - [x] 2.3 Write `src/core/platform/hooks/useMobileExamMode.test.ts`: test cases for (a) returns `false` on desktop viewport with no touch, (b) returns `true` when UA contains `'Mobile'` and pointer is coarse, (c) returns `false` when uncertain (fail-safe), (d) `force-mobile` override returns `true` on desktop, (e) `force-standard` override returns `false` on mobile, (f) the override is read through the session-scoped storage abstraction rather than persistent storage.
  - [x] 2.4 In `src/skills/reading/components/ReadingTestPage.tsx`: import `useMobileExamMode`. Call it at the top of `ReadingTestPageContent`. Add a conditional branch: when `isMobileExamMode === true`, render `<MobileReadingExamScaffold ... />` (stub/placeholder for now) instead of the existing `<TwoColumnLayout>` + `<InspiraFooterNav>` + floating arrows block. The `else` branch keeps the entire existing desktop render path unchanged. The mobile branch must source all state from `ReadingTestPage.tsx`, even while the scaffold is still a placeholder.
  - [x] 2.5 In `src/components/practice/IELTSPracticeView.tsx`: same pattern. Import `useMobileExamMode`. Add conditional branch: when `isMobileExamMode === true`, render `<MobileReadingExamScaffold ... />` (stub) instead of `<TwoColumnLayout>` + `<InspiraFooterNav>`. The `else` branch keeps the entire existing desktop render path unchanged. The mobile branch must source all state from `IELTSPracticeView.tsx`, not internal scaffold state. For solo/homework-specific scaffold props: pass `isPaused={false}` (no teacher-driven pause), `isConnected={true}` (no live session), `sessionStatus={'in-progress'}`, and `antiSelectClass={isHomework && antiCheatConfig?.detectCopyPaste ? 'anti-select' : undefined}`.
  - [x] 2.6 Create a minimal stub `src/components/test/mobile/MobileReadingExamScaffold.tsx` that renders a placeholder `<div>Mobile Reading Exam Mode — scaffold not yet implemented</div>`. This unblocks the entry-point wiring and lets you verify the conditional branch works. Define the full `MobileReadingExamScaffoldProps` interface from PRD Sections 7.2a and 7.3 in this file so it already expresses host-owned state values plus callbacks; do not add internal `useState` for those contract fields.
  - [x] 2.7 Manually verify on desktop: load a live Reading test and a solo Reading test. Confirm both still render the existing `TwoColumnLayout` + footer. Then set the browser session-storage key `__qa_mobile_exam_override__` to `force-mobile`, reload, and confirm the placeholder scaffold renders instead.
  - [x] 2.8 Run all existing Reading-related tests to confirm no regressions: `npx vitest run src/skills/reading` and `npx vitest run src/components/practice`.

- [x] 2A.0 Mobile Start/Instructions Screen (FR-24–29)
  - [x] 2A.1 Create `src/components/test/mobile/mobileInstructionsContent.ts` (pulled forward from Task 8.3 because the start screen needs it). This file is the single source of truth for mode-specific rules and controls-help copy. Export `getMobileInstructionsContent(mode: 'live' | 'solo' | 'homework', practiceContext?, resolvedSettings?): { rules: string[], controlsHelp: string[] }`. Build rules from existing product behavior/data only: live rules from timed-session behavior (timer, teacher control, no pause for student), homework rules from existing homework/resume/attempt constraints already exposed by the host, solo rules from the unsupervised/resume flow. Do not hardcode new claims unsupported by existing code.
  - [x] 2A.2 Create `src/components/test/mobile/MobileStartScreen.tsx`. Props: `mode`, `testTitle`, `testSkill`, `passageCount`, `questionCount`, `timeLimit` (number | null), `onStart`, `showStartButton` (default `true`; set to `false` for live mode where the teacher controls start), `practiceContext?`, `resolvedSettings?`. Render: compact single-column layout filling the viewport. Top: test title and skill badge. Middle: mode-specific rules list from `getMobileInstructionsContent()`. Bottom (when `showStartButton`): dominant Start button, full-width, min 48px touch target. Do not render `TestHeader`, `ReadingHeader`, `SoloSettingsModal`, or any desktop header/body chrome. No `@mantine` imports.
  - [x] 2A.3 In `IELTSPracticeView.tsx`: when `isMobileExamMode === true` and `resumeDecision !== 'pending'`, add a `mobileTestStarted` boolean state (default `false`). When `mobileTestStarted === false`, render `<MobileStartScreen mode={isHomework ? 'homework' : 'solo'} ... onStart={() => setMobileTestStarted(true)} />` instead of the scaffold. If `resumeDecision === 'resume'` (student chose to continue from `SoloResumeModal`), skip the start screen and set `mobileTestStarted = true` immediately via a `useEffect`. `SoloResumeModal` is already responsive (`width: min(420px, calc(100vw - 32px))`) and needs no changes. Do **not** render `SoloSettingsModal` on mobile — it uses `@mantine`; mobile text-size/line-spacing/highlighter are handled by the overflow menu (Task 8.0) or are fixed mobile defaults.
  - [x] 2A.4 In `ReadingTestPage.tsx`: when `isMobileExamMode === true` and `sessionStatus === 'waiting'`, render `<MobileStartScreen mode="live" showStartButton={false} ... />` as the base content behind `TestWaitingOverlay`. `TestWaitingOverlay` is already responsive (`maxWidth: 500px`, centered) and renders correctly on mobile — no changes needed. Once `sessionStatus` transitions to `'in-progress'`, the existing conditional branch (Task 2.4) renders the scaffold instead.
  - [x] 2A.5 Write tests for `MobileStartScreen`: (a) solo mode shows solo-specific rules from `getMobileInstructionsContent`, (b) homework mode shows homework rules, (c) live mode shows live rules and hides Start button, (d) Start button fires `onStart`, (e) no desktop header chrome renders, (f) compact single-column layout.

- [x] 3.0 Mobile UI Shell: Header, Passage Tabs, Floating Button, and Question Sheet
  - [x] 3.1 Create `src/components/test/mobile/MobileReadingHeader.tsx`. Props: `mode` (`'live'` | `'solo'` | `'homework'`), `timeRemaining`, `formatTime`, `activePassageLabel` (string like `"Passage 1"`), `onOverflowMenuToggle`, `isPaused`, `testSubmitted`. Render: compact 48px-height bar with timer (left), passage label (center), overflow menu trigger icon (right). Apply safe-area padding on top via `env(safe-area-inset-top)`. No student name. No `@mantine` imports.
  - [x] 3.2 Create `src/components/test/mobile/MobilePassageTabs.tsx`. Props: `passages` (array of `{ id, label? }`), `activePassageId`, `onPassageChange`. Render: horizontally scrollable tab strip with labels `Passage 1`, `Passage 2`, etc. Height target 44px. Active tab has a distinct bottom border. Tab labels must use `text-overflow: ellipsis` and constrained `max-width` so they truncate gracefully at large text sizes (PRD Section 10 edge case). On `activePassageId` change, auto-scroll the active tab into view. The component must be reusable — it renders identically in both the page and the sheet.
  - [x] 3.3 Create `src/components/test/mobile/MobileQuestionsFab.tsx`. Props: `answeredCount`, `totalCount`, `unansweredCount`, `flaggedCount`, `onPress`. Render: floating button positioned bottom-right with safe-area bottom padding. Label: `Questions {answeredCount}/{totalCount}`. Small badge indicators for unanswered (warning color) and flagged (informational color) counts when > 0. Minimum touch target 48px. z-index: 1000.
  - [x] 3.4 Create `src/components/test/mobile/MobileQuestionSheet.tsx` and `MobileQuestionSheet.css`. Props: `isOpen`, `onClose`, `children` (the sheet body content). Behavior: when `isOpen` transitions from `false` → `true`, slide the sheet up from the bottom edge over 250ms with `ease-out`. When closing, slide down with the same timing. A semi-transparent backdrop (z-index 2000) fades in behind the sheet (z-index 2001). Close triggers: (a) close button in top-right corner, (b) swipe-down gesture on the sheet header area, (c) backdrop tap. The sheet must occupy near-full viewport height (leaving ~48px for the mobile header above). The sheet must not redirect or navigate.
  - [x] 3.5 Write tests for `MobileReadingHeader`, `MobilePassageTabs`, `MobileQuestionsFab`, and `MobileQuestionSheet` in their respective `.test.tsx` files. Verify: header renders timer and passage label, tabs render correct count and highlight active, FAB shows correct counts and badges, sheet responds to open/close props and fires `onClose` on close triggers.
  - [x] 3.6 Wire the shell components into `MobileReadingExamScaffold.tsx`. Replace the placeholder stub with real rendering: `MobileReadingHeader` at top → `MobilePassageTabs` (page-level) → passage content area (render `PassageRendererComponent` prop with `currentPassage`) → `MobileQuestionsFab` (floating). The scaffold must **consume** host-provided props such as `questionSheetOpen`, `onOpenQuestionSheet`, `onCloseQuestionSheet`, `activePassageId`, `onPassageChange`, and `currentPassage`; it must not create those states internally. The scaffold must also receive and apply: `isConnected` (for connection-loss indicator rendering), `sessionStatus` (for waiting/paused state display), `isPaused` (for pause overlay passthrough), and `antiSelectClass` (applied as `className` on the scaffold root element for anti-copy-paste CSS). When `questionSheetOpen` is `true`, render `MobileQuestionSheet` containing: sheet header (passage label + question range + answered count + close) → `MobilePassageTabs` (synced instance) → body (placeholder for question content, wired in Task 4.0).
  - [x] 3.7 Ensure the following desktop elements are **not rendered** when `isMobileExamMode === true` in both hosts: `TwoColumnLayout`, `InspiraFooterNav`, `PassageControls`, floating ←/→ arrows (in `ReadingTestPage`), `ReadingHeader` (live), `TestHeader` (solo/homework). Additionally, pass `highlighterActive={false}` to `PassageRendererComponent` on mobile so any inline highlight selection UI is suppressed per FR-99/FR-100. Verify by toggling QA override and checking rendered output.
  - [x] 3.8 In both host components (`ReadingTestPage.tsx` and `IELTSPracticeView.tsx`), add the host-owned mobile shell state required by PRD Section 7.3: `questionSheetOpen`, `passageScrollByPassage`, and any refs needed to read/write the passage scroller. On passage switch, save the current scroll position of the active passage content area, then restore the new passage's last known position. Pass the current values and callbacks into `MobileReadingExamScaffold`.

 - [ ] 4.0 Question Sheet Content: Navigator, Question Blocks, and Flagging
  - [ ] 4.1 Add a `collapsible` prop to `src/components/test/QuestionNavigator.tsx`. When `collapsible === true`: (a) default render is a single horizontally scrollable row of question-number chips (~40px height) with `overflow-x: auto`, `white-space: nowrap`, (b) a `Show all` toggle button expands the navigator into the existing wrapping multi-row grid, (c) a `Collapse` toggle returns to the single row. Each chip shows answered (filled), unanswered (neutral), current (highlighted border), and flagged (small dot indicator) states.
  - [ ] 4.2 Write tests for the `collapsible` variant in `QuestionNavigator.test.tsx`: verify default single-row render, toggle to grid and back, correct state colors for answered/unanswered/current/flagged chips.
  - [ ] 4.3 Add an `embedded` prop to `src/components/test/IELTSQuestionsPanel.tsx`. When `embedded === true`: suppress the component's own outer `overflow-y: auto` scrolling (the mobile sheet handles scroll), and suppress the sticky internal header (the sheet header replaces it). The component still renders grouped question blocks with instructions and answer controls identically.
  - [ ] 4.4 In both host components, add host-owned `flaggedQuestions: Set<number>` plus `toggleFlag(questionNumber)` handlers. Pass `flaggedQuestions` and `onToggleFlag` down to `QuestionNavigator` and `IELTSQuestionsPanel`. Add a small `Flag for review` toggle button on each question block inside `IELTSQuestionsPanel` (only when `embedded === true`, mobile context). Flagging toggles the question number in/out of the set and must not modify answers or score.
  - [ ] 4.5 Wire the question sheet body in `MobileReadingExamScaffold`: inside `MobileQuestionSheet`, below the synced tab strip, render `QuestionNavigator` (collapsible mode, current-passage questions only, with `flaggedQuestions`) → `IELTSQuestionsPanel` (embedded mode, current-passage questions, with `flaggedQuestions` and `onToggleFlag`). The scaffold renders these from props only; students answer inside the sheet using the same controls as desktop.
  - [ ] 4.6 In both host components, implement per-passage question-group memory with host-owned `activeQuestionGroupByPassage: Record<string, number>` and `questionSheetScrollByPassage: Record<string, number>`. On passage switch while the sheet is open: save the current passage's sheet scroll position and active question group, then load the new passage's saved values (or default to the first unanswered group on first visit per FR-52). On sheet close and reopen, restore the exact scroll position and question group via scaffold props and callbacks.
  - [ ] 4.7 Write tests for `MobileReadingExamScaffold` covering callback wiring and prop-driven rendering: flagged questions show in navigator/question block, passage switch respects prop updates, and sheet open/close rendering follows host state. Write host-level tests in `ReadingTestPage` / `IELTSPracticeView` (or their hook-level seams) for persistence of flags, sheet scroll, and active question group across close/reopen.

 - [ ] 5.0 Review Summary Screen and Submit-Flow Refactor
  - [ ] 5.1 Create `src/components/test/mobile/MobileReviewSummary.tsx`. Props: `passages`, `questions`, `answers`, `flaggedQuestions`, `onQuestionChipTap(passageId, questionNumber)`, `onConfirmSubmit`, `onClose`, `isSubmitting`. Render: full-screen overlay (z-index 2002) grouped by passage. Each passage section shows: passage label, answered/total count, unanswered count (warning style), flagged count (informational style), and a grid of tappable question chips. Chips are color-coded: answered (green), unanswered (amber), flagged (blue dot). Tapping a chip calls `onQuestionChipTap`. Bottom: `Submit Test` button (calls `onConfirmSubmit` after a final confirmation step) and `Back to Test` button (calls `onClose`).
  - [ ] 5.2 Implement the final confirmation step inside `MobileReviewSummary`: when student taps `Submit Test`, show a confirmation modal (z-index 9500) with the unanswered count as a warning message (e.g., "You have X unanswered questions. Are you sure you want to submit?"). `Confirm` calls `onConfirmSubmit`. `Cancel` closes the modal and returns to the review summary.
  - [ ] 5.3 In `src/hooks/test/useTestSubmission.ts`: add a `skipConfirm?: boolean` parameter to the hook's options. When `skipConfirm === true`, the `handleSubmit(false)` path skips the `window.confirm(...)` call (lines 564-568) and proceeds directly to submission. Default `skipConfirm` to `false` for backward compatibility so desktop behavior is unchanged.
  - [ ] 5.4 In both host components, add host-owned `reviewSummaryOpen` state plus callbacks to open/close it. "Submit test" actions (from overflow menu or any manual submit trigger) set `reviewSummaryOpen = true` and **also set `questionSheetOpen = false`**. `MobileReadingExamScaffold` only renders `MobileReviewSummary` when the host passes `reviewSummaryOpen = true`. `MobileReviewSummary.onConfirmSubmit` calls `props.onManualSubmit()`. `onQuestionChipTap` closes the review summary, switches to the correct passage, opens the question sheet, and scrolls to the tapped question/group via host callbacks.
  - [ ] 5.5 In `ReadingTestPage.tsx`: when wiring the scaffold, pass `onManualSubmit` as a wrapper around `submitTestRef.current?.(false)` that first calls `flushEvents('manual_submit')` then `submitTest(false)`. Pass `skipConfirm: true` to the `useTestSubmission` hook options when `isMobileExamMode` is true.
  - [ ] 5.6 In `IELTSPracticeView.tsx`: pass `onManualSubmit` as a wrapper around the existing `submitTestRef.current?.(false)` call, and configure `useSoloSubmission` with `skipConfirm: isMobileExamMode`. This is required because `useSoloSubmission` currently shows `window.confirm(...)` for unanswered manual submissions.
  - [ ] 5.7 Write tests for `MobileReviewSummary`: verify passage grouping, counts are correct, chip colors match states, tapping a chip fires `onQuestionChipTap` with correct args, confirmation modal shows unanswered warning, confirm calls `onConfirmSubmit`, cancel returns to summary.

 - [ ] 6.0 Autosave Extension, Resume Restoration, and State Persistence
  - [ ] 6.1 In `src/types/practice.types.ts`, add the shared `SavedMobileState` interface and reuse it everywhere mobile Reading state is persisted. Extend `SoloSessionProgress` with `mobileState?: SavedMobileState`. Keep the persisted shape JSON-safe: `flaggedQuestions` must be a `number[]`, not a `Set`.
  - [ ] 6.2 In `src/hooks/useTestAutoSave.ts` (live mode): extend the hook's options interface to accept `mobileState?: SavedMobileState`. When provided, write it to `game_sessions/${sessionCode}/players/${studentId}/mobileState` alongside `answers`. Keep answer saving on the existing cadence, but debounce `passageScrollByPassage` and `questionSheetScrollByPassage` writes separately so they persist only after ≥500ms without scroll changes.
  - [ ] 6.3 In `src/hooks/solo/useSoloAutoSave.ts` (solo/homework): extend with the same `mobileState?: SavedMobileState` field. Write it alongside existing `answers`, `currentQuestion`, and `timeElapsed` under the saved progress payload. Migrate the entire hook from direct `localStorage` to `import { storage } from '@/core/platform/storage'` (async API), add the key-enumeration support needed by `cleanupExpiredProgress()` in `src/core/platform/storage.ts`, and make `clearSoloProgress` / `cleanupExpiredProgress` use the storage abstraction instead of raw browser APIs. Return an autosave status object from `useSoloAutoSave` so the host can react to save failures.
  - [ ] 6.4 In `src/hooks/solo/useSoloResume.ts`: extend the return type to include `savedProgress.mobileState` if present. Migrate from direct `localStorage.getItem` / `removeItem` to `await storage.get(...)` / `await storage.remove(...)`. Keep the existing `checking` / `savedProgress` state pattern by loading in a `useEffect` async IIFE.
  - [ ] 6.5 In `src/hooks/test/useTestSession.ts`, surface the current player's saved `mobileState` from `data.players?.[currentPlayerId]?.mobileState` and return it to `ReadingTestPage.tsx`. Do not make the host infer this from unrelated player fields.
  - [ ] 6.6 In both host components (`ReadingTestPage.tsx` and `IELTSPracticeView.tsx`), add explicit hydrate/serialize helpers for mobile Reading state. Hydration must: merge defaults, restore `SavedMobileState`, convert `flaggedQuestions: number[]` back to `Set<number>`, and fall back to the separately persisted text size when no saved mobile state exists. Serialization must convert the host-owned runtime state back into `SavedMobileState` before passing it into the autosave hooks. `MobileReadingExamScaffold` receives already-hydrated runtime props only; it does not accept or parse `initialMobileState`.
  - [ ] 6.7 Implement text-size persistence in the hosts: include `textSize` in `mobileState` autosave, and also persist the cross-test fallback with `storage.set(`reading_text_size_${studentId}`, textSize)`. Load that fallback with `storage.get(...)` before initializing host mobile state when no saved session progress exists.
  - [ ] 6.8 In both host components, watch the autosave status returned by `useTestAutoSave` / `useSoloAutoSave` and surface failures with `toast.error(...)` from `src/components/modern/ToastNotification`. Deduplicate by storing the last shown autosave error in a ref and only re-showing when the message changes or a later successful save resets the guard. Healthy autosave remains silent; do not show success toasts.
  - [ ] 6.9 Write tests: (a) `useTestAutoSave` writes `mobileState` to the player path when provided, (b) `useSoloAutoSave` stores `mobileState` through the storage abstraction and returns error status, (c) `useSoloResume` returns `mobileState` from stored progress, (d) `useTestSession` exposes the player's `mobileState`, (e) host hydration restores `flaggedQuestions`, scroll maps, and text size correctly, and (f) autosave error toasts are deduped until a successful save resets the guard.

 - [ ] 7.0 Interruption Handling, Back-Button Integration, and Post-Submit Cleanup
  - [ ] 7.1 Implement the z-index hierarchy from PRD Section 7.7 in `MobileReadingExamScaffold`. Ensure: page content = auto, FAB = 1000, sheet backdrop = 2000, sheet = 2001, review summary = 2002, overflow menu = 3000, system overlays (TimeUp, Waiting, Connection) = 9000+, final confirm modal = 9500. Add these as CSS custom properties or constants in a shared file for the mobile directory.
  - [ ] 7.2 Implement force-close on system overlays in the **host components**, not inside the scaffold. Watch `testSubmitted`, `showTimeUpOverlay`, and session interruption signals there; when any of them trigger, immediately set the host-owned `questionSheetOpen` and `reviewSummaryOpen` states to `false` so `TimeUpOverlay`, `TestWaitingOverlay`, and force-submit flows get full viewport control. The scaffold simply reflects the new props.
  - [ ] 7.2a Wire `onAutoSubmit` in both hosts per PRD Section 7.4. In `ReadingTestPage.tsx`: pass `onAutoSubmit={() => { submitTestRef.current?.(true); }}` to the scaffold. In `IELTSPracticeView.tsx`: pass `onAutoSubmit={() => { submitTestRef.current?.(true); }}`. Inside `MobileReadingExamScaffold`, add a `useEffect` that watches `timeRemaining` and `testSubmitted`: when `timeRemaining <= 0 && !testSubmitted`, first set `questionSheetOpen = false` and `reviewSummaryOpen = false` (via host callbacks), then call `onAutoSubmit()`. This provides the scaffold-driven auto-submit path specified by the PRD. Both the scaffold and the host's existing timer hook may trigger auto-submit — the submission hooks are idempotent and guard against double submission.
  - [ ] 7.3 Implement browser back-button integration per PRD Section 7.8 in the **host components** because overlay state is host-owned: (a) when `questionSheetOpen` becomes `true`, push a history entry via `window.history.pushState(null, '', window.location.href)`, (b) when `reviewSummaryOpen` becomes `true`, push another history entry, (c) add a `popstate` listener that closes the topmost overlay by updating host state, and (d) clean up that listener on unmount. Each `popstate` must close exactly one layer — review first, then sheet, then fall through to existing leave protection.
  - [ ] 7.4 In `IELTSPracticeView.tsx`: when `isMobileExamMode === true` and `practiceContext.type === 'homework'`, pass `enabled: false` to `useFullscreenMode`. Verify the hook already respects `enabled: false` by checking its `useEffect` guard (line 73-74 of `useFullscreenMode.ts` — confirmed: `if (!enabled) return;`). No code change needed in the hook itself, only in the host's call site.
  - [ ] 7.5 Implement post-submit cleanup in the hosts: when `testSubmitted` transitions to `true`, close sheet/review state immediately, suppress the FAB via props, and then allow the existing host navigation flow to continue. The scaffold must not navigate or mutate hidden state after submit.
  - [ ] 7.6 Write tests: (a) back-button closes sheet when open, (b) back-button closes review before sheet, (c) back-button does nothing when neither overlay is open, (d) host force-close triggers on `testSubmitted` / interruption changes, and (e) z-index values are correct for each rendered layer.

 - [ ] 8.0 Overflow Menu, Text Size, Instructions Modal, and Line Spacing
  - [ ] 8.1 Create `src/components/test/mobile/MobileOverflowMenu.tsx`. Props: `isOpen`, `onClose`, `menuItems`. Five items: Text size (opens text-size slider inline), Review answers (opens review summary), Submit test (opens review-summary → confirm), Instructions/Help (opens instructions modal), Leave test (triggers leave-protection flow). z-index: 4000. The component is purely presentational — it calls host-provided callbacks, no internal logic. Menu item labels must use `text-overflow: ellipsis` and flexible widths so they render cleanly at all text sizes.
  - [ ] 8.2 Create `src/components/test/mobile/MobileTextSizeControl.tsx`. Props: `currentSize`, `onSizeChange`, `onClose`. Render: a panel/modal with a slider from 14px to 22px in 1px steps, default 16px. Display the current value. Changes call `onSizeChange` immediately (live preview). The `fontSize` state is managed by the host components and passed through the scaffold to both `PassageRendererComponent` and `IELTSQuestionsPanel` via their existing `fontSize` props. Text-size must **not** resize tabs, navigator chips, or action buttons — only passage body text and question body text.
  - [ ] 8.3 Create `src/components/test/mobile/MobileInstructionsModal.tsx`. It imports `getMobileInstructionsContent()` from `mobileInstructionsContent.ts` (already created in Phase 2A). Props: `isOpen`, `onClose`, `mode`, `practiceContext?`, `resolvedSettings?`. Render the content as a read-only modal with `rules` and `controlsHelp` sections plus a close button.
  - [ ] 8.4 In both host components, own the overlay state for `overflowMenuOpen`, `textSizeControlOpen`, and `instructionsOpen`, and pass those values/callbacks into `MobileReadingExamScaffold`. Wire callbacks so `onTextSize` opens the text-size control, `onReviewAnswers` and `onSubmitTest` both open the host-owned review summary, `onLeaveTest` triggers the existing host leave/back handler, and `onInstructionsHelp` opens the instructions modal.
  - [ ] 8.5 Set line spacing to a fixed `1.6` for mobile. In both hosts, when `isMobileExamMode` is true, pass `lineSpacing={1.6}` to the `PassageRendererComponent` inside the scaffold and ignore the existing adjustable-lineSpacing controls. This replaces `PassageControls` (already hidden in Task 3.7) and matches the PRD requirement for a consistent mobile reading line height.
  - [ ] 8.6 In `src/skills/reading/components/PassageRenderer.tsx`, guard the internal `localStorage` font-size write. Currently `useEffect(() => { localStorage.setItem('passage_font_size', fontSize.toString()); }, [fontSize]);` fires unconditionally, even when the host provides an external `fontSize` prop. Change the write to only fire when `externalFontSize === undefined`, i.e., when no external prop is provided. This prevents a shadow persistence source on mobile while preserving desktop behavior where `PassageRenderer` self-manages font size. The guard: `useEffect(() => { if (externalFontSize === undefined) localStorage.setItem('passage_font_size', internalFontSize.toString()); }, [externalFontSize, internalFontSize]);`.
  - [ ] 8.7 Write tests for `MobileOverflowMenu`, `MobileTextSizeControl`, `mobileInstructionsContent.ts`, and `MobileInstructionsModal`: verify menu items render correctly, text-size slider range/step/default, and each mode receives the correct shared rules/help content.

 - [ ] 9.0 Regression Tests, Mobile QA, and Documentation Crosscheck
  - [ ] 9.1 Run the full test suite: `npx vitest run`. Fix any failures introduced by PRD-0043 changes. Pay special attention to: `src/skills/reading/`, `src/components/practice/`, `src/hooks/solo/`, `src/hooks/test/`, `src/components/test/`. **Likely regression point:** Task 3.7 conditionally hides desktop elements (`ReadingHeader`, `TestHeader`, `InspiraFooterNav`, `PassageControls`, floating arrows) when `isMobileExamMode === true`. Existing tests that assert these components always render will fail and need conditional assertions or mock overrides for `useMobileExamMode`.
  - [ ] 9.2 Desktop safety verification: load a live Reading test on desktop (wide viewport). Confirm: `TwoColumnLayout` renders, `InspiraFooterNav` renders, `ReadingHeader` renders, floating arrows render, `PassageControls` renders. No mobile components visible. Repeat for solo/homework Reading on desktop: `TestHeader` renders, `TwoColumnLayout` renders, `InspiraFooterNav` renders.
  - [ ] 9.3 Mobile QA — device classification: on a real phone browser (or Chrome DevTools mobile emulation), load a Reading test. Confirm: `MobileReadingHeader` renders (no `ReadingHeader` or `TestHeader`), `MobilePassageTabs` renders, `MobileQuestionsFab` renders, no `TwoColumnLayout`, no `InspiraFooterNav`, no `PassageControls`, no floating arrows.
  - [ ] 9.4 Mobile QA — passage switching: switch between passages via tabs. Confirm: passage scroll position is preserved per passage, header passage label updates, if sheet is open the sheet content swaps to new passage's questions without closing.
  - [ ] 9.5 Mobile QA — question sheet: tap FAB to open sheet. Confirm: sheet slides up, shows collapsible navigator + question groups + answer controls. Answer a question — confirm answer is captured. Flag a question — confirm flag dot appears in navigator and question block. Close sheet (X, swipe-down, backdrop, back button) — confirm passage scroll position is preserved. Reopen — confirm same question group and sheet scroll position restored.
  - [ ] 9.6 Mobile QA — review and submit: tap Submit (overflow menu). Confirm: review summary opens with passage-grouped counts. Tap a question chip from a different passage — confirm passage switches, sheet opens at that question. Return to review. Tap Submit Test — confirm final confirmation modal with unanswered warning. Confirm submit — verify submission completes and host post-submit navigation runs.
  - [ ] 9.7 Mobile QA — interruptions: (a) simulate time expiry while sheet is open — confirm sheet closes, TimeUpOverlay appears, auto-submit runs. (b) Back button with sheet open — closes sheet only. (c) Back button with review open — closes review only. (d) Back button with nothing open — triggers leave protection. (e) Rotate device while sheet is open — confirm all state preserved. (f) **Verify existing overlays render correctly in mobile scaffold DOM:** confirm `TestWaitingOverlay` (position: absolute, z-index 1000) covers the mobile scaffold, the connection-loss indicator (position: fixed, top: 80px in `ReadingTestPage:518`) is visible above the mobile header, and `TimeUpOverlay` appears above all mobile layers.
  - [ ] 9.8 Mobile QA — resume: (a) Solo mode: answer some questions, close the browser, reopen — confirm Resume modal offers to restore exact mobile state (passage, scroll, flags, sheet state, text size). (b) Live mode: refresh the page mid-test — confirm re-entry restores mobile state. (c) Homework mode: verify auto-resume without `Start New` when homework rules disallow restart.
  - [ ] 9.9 Mobile QA — autosave and toast: (a) Verify healthy autosave is silent (no badge, no toast). (b) Simulate autosave failure (e.g., disconnect network) — verify a `toast.error(...)` notification appears and auto-dismisses. (c) Verify repeated failures during the same error burst do **not** stack duplicate toasts. (d) Restore connectivity, allow one successful autosave, fail again, and verify the dedupe guard resets so a new failure toast can appear.
  - [ ] 9.10 Mobile QA — overflow menu: open overflow menu. Verify all 5 items present. Test: text-size slider changes passage and question text but not tabs/chips/buttons, instructions modal shows the shared mode-specific rules/help content, and leave test triggers leave protection.
  - [ ] 9.11 Documentation crosscheck: verify the PRD's edge case table (Section 10) against the implementation. Confirm each of the 21 edge cases is handled. Update `documentation/tasks/tasks-0043-prd-mobile-ielts-reading-test-taking-interface.md` to mark all tasks as complete.

---

## Reassessment Pass 2 — Resolution Log (2026-04-08)

All 10 items from the second reassessment have been integrated into the task list above.

| ID | Issue | Resolution |
|----|-------|------------|
| B1 | Start/Instructions Screen (FR-24–29) | **Phase 2A added** — `MobileStartScreen`, `mobileInstructionsContent.ts` pulled forward, integration in both hosts |
| B2 | PassageRenderer Font-Size Shadow Write | **Task 8.6 added** — guard `localStorage` write with `if (externalFontSize === undefined)` |
| B3 | `onAutoSubmit` Scaffold Prop Wiring | **Task 7.2a added** — both hosts wire prop, scaffold calls it on `timeRemaining <= 0` |
| M1 | Unwired Scaffold Props | **Tasks 2.5 and 3.6 updated** — explicit `isConnected`, `sessionStatus`, `isPaused`, `antiSelectClass` wiring |
| M2 | Sheet-Close on Review-Open | **Task 5.4 updated** — "also set `questionSheetOpen = false`" |
| M3 | Mobile Interruption State Verification | **Task 9.7(f) added** — verify overlay rendering in mobile DOM |
| M4 | Large Text Overflow Handling | **Tasks 3.2, 8.1 updated** — `text-overflow: ellipsis` / flexible widths |
| L1 | Highlighter Suppression | **Task 3.7 updated** — `highlighterActive={false}` on mobile |
| L2 | `isPaused` for Solo/Homework | **Task 2.5 updated** — explicit `isPaused={false}`, `isConnected={true}` |
| L3 | Conditional Rendering Test Breakage | **Task 9.1 updated** — flagged as likely regression point |
