# G-P2B0-001 — Private source R2 boundary proof

Date: 2026-07-13

Status: `VERIFIED_REMOTE_FAITHFUL` for `T-P2B0-001` only. Packet 2B source metadata/upload, PDF-engine, RTDB, browser, and broader production-composition gates remain open.

## Scope and non-actions

Proved one distinct private source bucket/binding and one exact disposable-object direct-access denial. Used no `kahoot-media`, source PDF, production object, native presigned URL, or R2 public URL. Created one disposable text object only, then deleted it. No Worker, route, domain, preview, public-access, or unrelated resource was changed.

## Local source/config proof

- `cloudflare/wrangler.book-source.jsonc:9-10` sets `workers_dev: false` and `preview_urls: false`.
- `cloudflare/wrangler.book-source.jsonc:34-38` declares sole source binding `BOOK_SOURCE_R2 -> luyentap-book-source-private`; bucket differs from generic `kahoot-media`.
- `cloudflare/test/book-source-private-boundary.test.js` asserts unknown methods/paths return `404` and every R2 operation remains uncalled. JSONC parsing/config assertions were repaired during this proof.
- `rtk npx vitest run --config vitest.book-source.config.mjs test/book-source-private-boundary.test.js` from `cloudflare` exited `0`: 1 file, 31 tests passed.
- WSL `npx --yes wrangler@4.103.0 deploy --config cloudflare/wrangler.book-source.jsonc --env-file .env --dry-run` from repo root exited `0`; output exposed `env.BOOK_SOURCE_R2 (luyentap-book-source-private)`.

Local proof establishes intended configuration only. Remote reads below establish current deployed/bucket state.

## Current remote readback

Runtime: WSL Ubuntu ARM64 `/usr/bin/node`, isolated temporary Wrangler package `wrangler@4.103.0`; repo root `/mnt/c/Users/The Lord/Desktop/luyentap-writing-import-rebased`. `.env` was passed with `--env-file`; its values were not printed.

- `whoami` exited `0`; authenticated target account read succeeded.
- R2 bucket list showed both `kahoot-media` and distinct `luyentap-book-source-private`.
- `r2 bucket info luyentap-book-source-private` exited `0` before proof: `object_count: 0`, `bucket_size: 0 B`.
- `r2 bucket dev-url get luyentap-book-source-private` exited `0`: `r2.dev` disabled.
- `r2 bucket domain list luyentap-book-source-private` exited `0`: no custom domains.
- Deployment status exited `0`: `book-source-private-gateway` active version `7676c260-e300-4ff5-8e89-537617802ecb` at `100%`.
- Worker settings API returned only `BOOK_SOURCE_R2 -> luyentap-book-source-private` as R2 binding.
- Worker subdomain API returned `enabled: false`, `previews_enabled: false`; service-filtered Worker custom-domain API returned zero domains.

Zone route API read for `GET /zones/{zone_id}/workers/routes` returned HTTP `403` / Cloudflare `10000`. Therefore this proof does not claim a fresh zero zone-route result. This is a residual read-authority limitation, not a positive route finding.

## Exact disposable-object denial

Object key: `_proof/t-p2b0-001-20260713-direct-denial.txt`.

1. Authenticated remote put exited `0` with payload `t-p2b0-001 disposable proof`.
2. Authenticated exact-key get exited `0` and returned the expected payload.
3. Unsigned canonical R2 endpoint request used the exact bucket/key, no auth header, no presigned query, and `redirect: manual`:
   - `HEAD` returned HTTP `400`, no redirect, no payload.
   - `GET` returned HTTP `400`, no redirect, `InvalidArgument` / `Authorization`, and no payload match.
4. Authenticated exact-key delete exited `0`.
5. Authenticated post-delete exact-key get exited `1` with `The specified key does not exist.`
6. Final bucket info exited `0`: `object_count: 0`, `bucket_size: 0 B`. This is supporting interval metric readback; exact-key post-delete failure is cleanup authority.

The direct probe used a known existing object, so denial evidence is not an ambiguous guessed-key `404`.

## Boundary and residual risk

This closes only `T-P2B0-001` at its stated boundary. It does not prove source metadata/upload behavior, source immutability, PDF processing, RTDB rules, grants, rendition safety, browser delivery, or full Packet 2 closure. Active deployed Worker settings report compatibility date `2026-01-20`, while local dry-run config uses `2026-07-13`; only the exact private binding and public-surface facts above are claimed across those readbacks. Zone-route absence remains unreadable with current token authority.
