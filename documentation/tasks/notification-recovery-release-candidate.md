# Notification recovery combined release candidate

**Prepared:** 2026-09-24. **Current status (2026-09-26):** first-batch Firebase rules, notification Worker, and selective Hosting are deployed to `temp-a1437` / `kahut1`. The first-batch retry gate is now deployed too. Later notification families and historical backfill remain staged separately. The dated preparation and readback sections below record earlier states.

**Rotation QA, 05:25 UTC:** exact-source `bee5a7d3` served locally at `localhost:5174` (student) and `localhost:5173` (teacher) with the existing Student Test and Teacher Test accounts. A student class notice Open reduced the unread count from 12 to 11; a homework reset feed item opened `/student/homework/VFDPyjnvbhe8TBNWpq3l` on repeated clicks. A teacher Homework Submitted bell item opened `/teacher/homework/VFDPyjnvbhe8TBNWpq3l` and reduced its unread count from 7 to 6. A fresh Reading V2 submission appeared as submitted in both student and teacher pages and produced a new teacher bell item, which also marked read on Open.

The dedicated QA assignment was reset through the teacher UI. Before that reset, result `reading-v2-result-fdc5c8c2-66af-4911-9b26-366a370b10a3` and its `test_results_by_session/reading-v2`, `test_results_by_student/x3hDfjYVN7cJtSbwq0ChIjl1Bk62`, and `test_results_by_teacher/glMHCrzMnyS6AqFcb9I0nlOqQ6X2` rows all existed. After reset, authenticated Firebase REST readback found all four absent, and a Firestore query found zero matching `homework_submissions` documents. The teacher page showed zero submitted and a new student Homework Reset feed item appeared. This is live test-data and local exact-source browser proof, not deployed Hosting proof; older orphaned QA indexes remain separate.

Source `bee5a7d3` annotates an existing failure issue once with verified success and conditionally clears only its matching pointer; later healthy successes do not rewrite that issue. [CI 36219800815](https://github.com/iamhuwng/autoresync/actions/runs/36219800815) passed. Wrangler deployed that Worker source at 05:28:32 UTC as `d4da93f2-37a2-4a23-bda2-faeada990cf7`; deployment status readback showed 100% active. Version readback showed `fetch` and `scheduled`, both secret binding names, and `NOTIFICATION_RETRY_BATCH=class-homework`. Deploy output retained exactly one `*/2 * * * *` schedule. The isolated checkout used a temporary build-only alias to the canonical locked `jose` dependency, and the dry-run config matched the checked-in config after removing that alias. Cold populated CPU and realistic backlog capacity remain open.

**2026-09-27 recovery ordering correction (18:09 UTC on September 26):** source `7fdfb636` saves `lastFailureAt` during terminal failure bookkeeping. A delayed success observed at or before that failure leaves its streak and issue pointer intact; class and homework retry callbacks observe success time after confirmed delivery instead of using the pass start. One verified success after a failure is sufficient evidence, without promising the earliest concurrent success. The focused gate suite passes seven checks, including stale success followed by valid recovery and a concurrently newer failure pointer. [Exact-source CI 36261259648](https://github.com/iamhuwng/autoresync/actions/runs/36261259648) passed the app, rules, Workerd and bundle gates. Wrangler deployed Worker `be0c3e4e-338e-4771-b06a-368b5306b4f6` at 18:09:22 UTC; Cloudflare deployment readback at 18:17 UTC confirms 100% active. Version readback retains both secret names, fetch/scheduled handlers, the `class-homework` batch and one `*/2 * * * *` Cron. The build-only canonical `jose` alias was removed after deployment. If an issue was purged, clearing its matching pointer does not establish visible admin recovery evidence. A failing state without a known failure timestamp is retained for operator inspection rather than labelled recovered. Fresh visible admin recovery, populated cold CPU and backlog remain open.

## September 27 follow-up measurements

**September 27 follow-up (September 26 UTC):** current Worker `be0c3e4e` completed two synthetic two-recipient class retries at 18:16/18:24, with 10.646/7.319 ms CPU and nine subrequests each. Both retained intents are done/attempts 2 and all four expected inbox rows were read back. A fresh real Reading V2 QA submission deliberately blocked only its immediate notification request. After the tab closed and its exact record's due time was conditionally advanced, the 18:26 retry delivered to Teacher Test at 8.573 ms CPU/seven subrequests. The teacher bell Open persisted read=true. Exact IDs, provider state and measurement limits are in the [CPU record](notification-recovery-cpu-escalation.md). Cold populated CPU and realistic backlog remain open; the [internal DO option](notification-recovery-executor-proposal.md) is **UNDER REVIEW**, not deployed. Fresh admin visibility remains blocked by a blank hocthem.net page in the currently available browser; no backend recovery failure is inferred.

## Implementer rotation checkpoint (2026-09-26 04:49 UTC)

Read the [current handoff](notification-recovery-rotation-handoff-2026-09-26.md) before continuing. Implementation source is `4b3997ca`; recovery docs were committed through `70d9519b` on `codex/notification-recovery-rotation`. The old `6dbb` checkout disappeared after retirement; its uncommitted student dashboard navigation draft and untracked `output/notification-recovery/` evidence did not transfer to the new worktree. Recreate the needed draft from source review and repeat dependent QA instead of treating those missing files as proof. The previous implementor stopped at a Codex usage limit, so no post-stop verification is implied.

The last recorded active notification Worker is `2a1df6dc-0772-49ed-9edc-36b7e0572ef6` with first-batch gating and one `*/2 * * * *` Cron. The last recorded Hosting release is `sites/kahut1/releases/1790391739218000`, version `8b3746631a8336c0` (03:02:19 UTC). Re-read these remote versions and served assets before any new deploy claim. Native inbox create on prior Worker `5daa8079` measured class retries at **14.735 ms CPU / 10 subrequests cold**, **8.110 ms / 9 warm**, and **9.550 ms / 12 warm with a prior issue**; cold CPU and queue capacity remain open. The cached homework token change was deployed separately; its populated retry CPU was not recorded. An earlier real closed-app homework retry delivered to the teacher at 14.212 ms CPU. Three repeated teacher notification Opens navigated correctly after the app fix. Atomic result/index deletion passed Linux owner and unauthorized rule tests; fresh reset browser/readback proof remains open. Two older QA results have orphaned indexes from the former sequential deletion path; their repair is not inferred from the new code. Later families, second Cron, Book flow, and backfill remain held or unverified as stated in the handoff.

## Independent homework retry proof (2026-09-26 02:33 UTC)

Student Test's actual Reading V2 submission `VFDPyjnvbhe8TBNWpq3l_x3hDfjYVN7cJtSbwq0ChIjl1Bk62_1790388311658` committed at 02:07:48 UTC, including result `reading-v2-result-70531762-6242-41a0-9142-787987043f75` and durable delivery evidence. Immediate notification failed against the incompatible silent-write version while the submission stayed saved. Only this QA record's due time was advanced using its exact Firestore updateTime; event identity, submitted state, and attempts were preserved. With the student signed out, corrective version `741a9945` completed attempt 2 at 02:26:14 UTC. Readback shows submitted/done, the original occurrence time, and teacher inbox `38e40fef-9621-5be1-8d02-2cbf780414fd` with the correct homework link. Teacher Test received it in the real bell and opening it persisted `read=true`. Opening it from another homework detail did not change the live route; that routing discrepancy remains under investigation. Screenshot evidence is saved under `output/notification-recovery/teacher-homework-retry-notice.png`.

GraphQL measures this homework retry at **14.212 ms CPU**, nine subrequests, one successful request and zero errors. This adds another populated CPU failure, not a capacity pass. A new manual reminder attempt was correctly skipped by the existing cooldown; it is not fresh reminder-delivery proof. The browser also logged the existing app homework statistics write permission error; the submitted record remained committed.

## Restored delivery and CPU escalation (2026-09-26 02:17 UTC)

Source `7604a853` deployed as `741a9945-cf7b-4718-a8f6-228db440c034` at 02:08:42 UTC. At 02:16:11 UTC the retained class canary completed attempt 2 exactly once; both expected inbox rows were read back with correct original time, links, and unread state. Tail confirms this version, no exception, and successful Cron outcome. GraphQL records one request, zero errors, 11 subrequests, and **11.627 ms CPU**. Supported transport and delivery are restored; the normal retry still exceeds the 10 ms target. Two materially different focused attempts are exhausted. See the [CPU escalation report](notification-recovery-cpu-escalation.md) for evidence, the failed silent-write attempt, the 15-request cold recovery branch, and calculated four-hour class backlog concern. Further CPU changes await planner decision; later families and the second Cron remain held.

## Focused CPU corrections (2026-09-26 02:09 UTC)

- CORS source `e32c9ba1` deployed as `333a16e9-9b9f-4df3-a41c-01e6427604f6`. Live preflights from the existing `https://hocthem.net` domain succeed for Book, class, deadline, and homework-reset routes; an unrelated origin remains denied and later feedback actions remain inactive.
- Source `eab74994` deployed as `6f98156e-3911-47b0-844c-2ae6c8205f43` to try Firebase `print=silent` for unused conditional-write response bodies. The 02:00:39 UTC class Cron threw before claiming QA intent `cf0c5e13-e7fc-4f4a-b102-21ea0b6f7ccc` (4 subrequests, 6.306 ms CPU). A direct REST read followed by a conditional same-value write proves Firebase returns HTTP 400: mixing `print=silent` with `if-match` is unsupported. The canary remains `retry_due`, attempt 1. Mocked 204 responses did not prove this provider behavior.
- The corrective source removes that incompatible option and skips healthy-success gate writes only when there is no failure streak, suppression, or issue to update. It preserves the latest gate read, conditional writes, failure reset, and required recovery evidence. The focused x64 Workerd set passes 21 tests across four files; its normal class retry budget is 12 external subrequests (one fewer write). Deployment and populated CPU remeasurement remain pending at this entry.
- Edge control was recovered through a fresh tab using the existing Super Admin session. Student quick-login, the class notice Open/read action, and an existing homework-reset notice link were exercised in Edge. A fresh Reading V2 homework submission is in progress; these observations alone do not prove a newly delivered homework notice.

## Corrective deployment and normal retry CPU (2026-09-26 01:50 UTC)

- Source `a8c9e75f` includes the two reporting corrections and passed 7 Admin Reports tests plus 15 focused Workerd tests. Wrangler dry-run passed at 391.78 KiB. The same first-batch config deployed Worker version `f5f1448a-04e7-440a-99ef-5a1096271fb4` at 01:39:02 UTC; `versions view` confirms fetch/scheduled handlers, both secret names, and `NOTIFICATION_RETRY_BATCH=class-homework`. No additional families or trigger were activated.
- Selective Hosting source `af25f127` passed its production build and 247 KB bundle budget. Hosting release `sites/kahut1/releases/1790386820636000` / version `c9881a60391daa5a` is live at 01:40:20 UTC. Served `AdminReportsPage-BZVqAcGJ.js` matches the build (SHA-256 `d791bb603e212278470855ac60f7be2dd6c5c205a8bf85a4ac7a8e6585a2e3eb`). A fresh Edge tab reused the existing Super Admin session and rendered the Error Log with all 739 records. Live purge was not run against existing reports; the focused test proves recent timestamps survive old-bucket retention.
- A synthetic QA `retry_due` pending-join intent `5d1a4bd7-06f6-444b-821d-4ffba765184c` ran at 01:44:16 UTC on that Worker version. It claimed attempt 2, created both expected student/teacher inbox rows with correct links and `read=false`, and reached `done`. GraphQL records one successful invocation, zero errors, 13 subrequests, and **12.422 ms CPU**, above the 10 ms Workers Free limit. This is normal retry delivery proof, not a successful free-tier capacity gate or real browser product-action proof. Further populated queue-drain tests require a CPU correction first.
- Live preflight accepted `https://kahut1.web.app` but rejected the signed-in custom domain `https://hocthem.net` with `403 cors_origin_denied`. Source now adds only that existing authorized domain to the first-batch allowlists; 9 focused Worker checks pass, including rejection of unrelated origins and continued later-family gating. Its deployment/readback is pending at this checkpoint.

## Latest Worker, Hosting, and admin-browser proof (2026-09-26 01:35 UTC)

- [Exact-source Linux CI run 36178061741](https://github.com/iamhuwng/autoresync/actions/runs/36178061741) passed the Worker correction and rules gates. Wrangler deployed Worker version `06ec2c39-4d3d-4a2a-b799-18ad277b4279` at 19:15:45 UTC. A fresh `wrangler deployments list` at this checkpoint still shows that version as the newest 100% deployment. Earlier `versions view` readback showed `fetch` and `scheduled` handlers, both secret binding names, `NOTIFICATION_RETRY_BATCH=class-homework`, and one `*/2 * * * *` trigger. The last live CPU figures are the four **unpopulated** slots on the preceding version; populated Cron CPU after this correction is unmeasured.
- The selective admin-log Hosting source was committed and pushed on `codex/notification-admin-log-hosting` at `53cf3084`. It corrects stale three-day copy and uses the full error list for an older-bucket diagnostic detail. Focused Admin Reports tests passed 6/6; the ARM64 Vite production build and bundle-budget check passed (247 KB root entry). The repository x64 harness stopped before Vite because this worktree already had an ARM64 dependency tree, so the build used the installed Vite directly.
- Firebase Hosting deployed `sites/kahut1/releases/1790366907447000`, version `sites/kahut1/versions/bf548da46f525368`, at 20:08:27 UTC. The served `AdminReportsPage-Bc6v7CPL.js` bytes match the built file (SHA-256 `1b9f879ca24879cac76dca86c2b99d4de4c73fa247d73518cf439ad5b24764d0`). The signed-in Super Admin Edge browser at `https://hocthem.net/admin/reports` loaded that same bundle, displayed the terminal class-notification issue and its context for class `YB3LP7`, and showed the corrected all-dates copy. A read-only March 22–31 filter displayed 60 older error records out of 739 loaded; filters were reset and the notification issue left expanded.
- The 20:10 checkpoint proved the deployed admin read path and historical-date filtering. The later canary below adds newly emitted reason-code proof. Normal retry delivery CPU, later-family activation, the Book flow, and the 35-row historical backfill remain open.

### Populated recovery-report canary and follow-up source corrections

- At 2026-09-25 20:24 UTC, version `06ec2c39-4d3d-4a2a-b799-18ad277b4279` processed one QA `retrying` class intent whose inbox row was absent. Firebase readback showed `state=failed`, one terminal issue with `reasonCode=inbox_missing_after_claim`, and one distinct failure recorded in the class family gate. The signed-in Edge admin page received the issue live (5 of 740 loaded records) and displayed its reason, action ID, and recipient context.
- Cloudflare GraphQL recorded that invocation as successful with one request, zero errors, eight subrequests, and 7.424 ms CPU p50/p99. This measures the interrupted-retry readback/report branch only. A normal `retry_due` claim plus inbox delivery, sustained queue progress, other families, and the full-stage CPU budget remain unproven.
- The exact QA intent, issue, and family gate were removed after the gate was reread and contained only this canary's action ID. No historical backfill ran.
- Follow-up source preserves a new error's current timestamp when purging its old event-date bucket. Purge batches only expired child IDs, so it cannot delete an unseen new issue by removing the whole bucket. The class reporter omits `failedRecipientIds` for a one-recipient `join-pending` failure because a count alone does not identify the missing person. Focused checks passed: 7 Admin Reports tests and 15 Workerd tests. These corrections are not yet deployed at this checkpoint.
- Further families and product-action migration are paused pending the source-based [route boundary audit](notification-recovery-route-boundary.md). Existing rule denial does not establish that a product action must move to a Worker.

## Source continuation and retry-gate release (2026-09-26)

- Source commit `efaa63fd` routes assignment approval and course request/type decision notices through `dispatchCommittedNotification` and removes three duplicate browser clients. Focused app tests passed: 55 tests across the producer, inventory, assignment, course, and course-request suites (the course-request suite used dummy Firebase config).
- Follow-up source work adds a durable class-family retry suppression gate. It needs live CPU measurement and deployment. The Windows RTDB emulator exited during startup with Netty `Unable to establish loopback connection`; Linux CI subsequently passed its RTDB and Firestore emulator gates. Windows x64 Wrangler used its existing OAuth login for the fresh remote readback below.
- Homework submission now uses the same gate for scheduled retries, terminal reports, immediate success, and verified immediate failures while suppressed. A focused Workerd run passed 10 tests across the homework worker, suppression, and subrequest suites. The broad Workerd suite was stopped after unrelated Book source, route, and composition failures; its generated-rules comparison also differs at multiple pre-existing non-notification paths, so it is not release proof.

### Fresh remote readback (2026-09-25 18:23 UTC)

- Windows x64 Wrangler used its existing OAuth login for account `e41db829dabe9993f03674afdfd56510`. Active notification Worker version is `f81d8256-a0af-46c2-951a-95ea320d2478` at 100%, deployed 15:18:26 UTC. It has `fetch` and `scheduled` handlers, `NOTIFICATION_RETRY_BATCH=class-homework`, the `FIREBASE_WEB_API_KEY` and `NOTIFICATION_COMMAND_GOOGLE_SA_KEY` secret bindings, and the single `*/2 * * * *` Cron trigger. This is newer than the historical `e1346b89` version above. Its bundle was downloaded read-only (389,015 bytes; SHA-256 `53D7DDD8B467A7F38083FC62AAF3EB32B37EE9D8460103ED426883A9E522CD80`); source equivalence is not yet proven.
- Google Cloud CLI auth is `iamhuwng@gmail.com` on `temp-a1437`. Live RTDB rules do not contain the candidate `notification_retry_families` node; its rule and source are not deployed. Firebase Hosting still reports `sites/kahut1/releases/1790338683902000`, version `89433adc6a8024cd`, from 2026-09-25 12:18:03 UTC. The first Hosting query lacked the required quota-project header; rerunning with `x-goog-user-project: temp-a1437` succeeded.
- Source now applies the retry gate to all four first-batch families: class membership, homework submission, manual reminder, and homework reset. Focused x64 Workerd verification passed 25/25 across seven files. The notification Worker Wrangler dry-run passed at 388.39 KiB. No candidate deployment has occurred; the populated class CPU margin, rule emulator, admin log browser rendering, and broader family coverage remain open.
- [Linux notification CI run 36173640494](https://github.com/iamhuwng/autoresync/actions/runs/36173640494) passed at `4fa6d89f`, including app tests, RTDB and Firestore emulators, Workerd, and Wrangler bundling. Source correction `fbc906ae` makes successful fresh delivery visible as `lastSuccessAt` on the existing admin issue and excludes idempotent inbox replays from recovery evidence. Its seven-file focused Workerd run passed 27/27, and its Wrangler dry-run bundled at 389.83 KiB. [Exact-source Linux CI run 36174482028](https://github.com/iamhuwng/autoresync/actions/runs/36174482028) passed app, RTDB emulator, Firestore emulator, Workerd, and Wrangler gates.
- Before release, fresh live RTDB rules matched the candidate everywhere except the new `notification_retry_families` node, which denies ordinary browser reads and writes at the child boundary. Firebase deployed `database` to `temp-a1437`; a direct remote readback then matched the entire checked-in rules tree. Wrangler deployed the exact `fbc906ae` notification Worker source as version `28f857f1-66a9-4e99-82f9-34bcdb2bf9e8` at 18:39:35 UTC. Deployment and version readback show 100% active, both required secret binding names, `fetch` and `scheduled` handlers, `NOTIFICATION_RETRY_BATCH=class-homework`, and the single `*/2 * * * *` trigger. Unauthenticated live probes returned `401` for the class route and `503` for an inactive assignment route. The 18:44 UTC scheduled invocation reported `Ok` in the live Worker tail. Cloudflare GraphQL reported success with zero errors for the 18:40, 18:42, 18:44, and 18:46 UTC Cron slots on this exact version, at 8.170, 4.730, 2.481, and 4.392 ms CPU respectively. The gate node read back as empty before these slots; they prove unpopulated passes only. Populated CPU, admin issue display, and later-family cutover remain open.
- A follow-up review found that the RTDB root super-admin write rule could bypass the gate child's `.write: false`. Source `80e7cd18` adds `notification_retry_families` to the root equality guard. [Linux CI run 36176927226](https://github.com/iamhuwng/autoresync/actions/runs/36176927226) passed the emulator negatives for direct and ancestor-shaped super-admin writes, plus all other notification gates. Fresh live rules differed only by that guard; Firebase deployed it and remote readback matched the full checked-in rules tree. The root `.read` still intentionally allows super-admin reads of the gate; the child `.read: false` does not override that ancestor grant.
- The next Worker source correction gives terminal issues a bounded reason code and failed recipient context. Issue paths use the saved event date, so a crash followed by a report after midnight targets the same row. The retry gate keeps the three distinct failed action IDs in its current streak; A/B/A counts as two failures. The Admin Error Log now uses its existing full error-tree subscription for the filtered list, so a newly reported failure remains visible even when its stable event bucket is older than three days; the health summary remains a recent-window view. Focused Workerd checks passed 28/28, Admin Reports tests 6/6, and Wrangler dry-run bundled at 391.74 KiB. This correction is not yet deployed at this checkpoint.

## First-batch cutover and live correction (2026-09-25)

- The exact combined RTDB candidate still differed from the fresh live tree at the 44 reviewed notification paths. Firestore's live source matched the inspected `070d2d3c` source. Firebase deployed `database` and `firestore:rules`; readback matched both candidate sources, with Firestore ruleset `f2b8e6ad-a28e-495a-9198-56522d612075`.
- Cloudflare activated the staged `FIREBASE_WEB_API_KEY` secret, then deployed the combined Worker with `NOTIFICATION_RETRY_BATCH=class-homework`. Version `5f6cacda-24e4-4625-a584-17082d5ff4ee` had `fetch` and `scheduled` handlers, both required secret bindings, and the single `*/2 * * * *` trigger. Later action routes returned `503` while unauthenticated first-batch routes returned `401`.
- The selective Hosting branch `codex/notification-class-homework-hosting` at `c3e83309` was built with the actual Firebase and Worker origin values, then deployed to `https://kahut1.web.app`. Its release is `sites/kahut1/releases/1790338683902000`, version `sites/kahut1/versions/89433adc6a8024cd` (2026-09-25 12:18:03 UTC).
- In real teacher/student browsers at `http://localhost:5173` and `http://localhost:5174`, Teacher Test created QA class `YB3LP7`; Student Test requested to join. The teacher received a new request, its Open link reached the class, and opening it reduced the unread count. Approval generated the student's Approved notification. Both class intents were read back as `done` with one attempt.
- A controlled live Cron canary changed the already delivered approval intent to `retrying` and added a synthetic retrying approval intent with no inbox entry. At the 14:00 UTC class slot, the first recovered idempotently to `done`; the second ended `failed` and created `reports/errors/2026-09-25/9d010b50-b81f-44be-8203-f10768f30738` with one missing recipient. No canary intent remains pending.
- Live Cron exposed `Illegal invocation` in the Firestore retry stores, missed by injected fetch mocks. Commit `03e4f676` binds Worker `fetch` to `globalThis` in the first-batch stores and homework retry path and makes the populated Workerd test reject lost receivers. The focused local Workerd set passed **22/22 across 5 files**, Wrangler dry-run passed at **379.61 KiB**, and Worker version `e1346b89-fee5-4ee1-b301-19c2edf1ae7b` deployed at 14:04:11 UTC with the same one-trigger, first-batch bindings. [Exact-source Linux CI run 36145092617](https://github.com/iamhuwng/autoresync/actions/runs/36145092617) passed app, RTDB emulator, Firestore emulator, Workerd, and Wrangler gates. All four post-fix slots succeeded: class 14:06 (9.6 ms CPU), homework submission 14:08 (2.888 ms), manual reminder 14:10 (5.474 ms), and homework reset 14:12 (success in Worker logs). The latest ten Cron events through 14:56 UTC were also all successful, with CPU from 1.25 to 4.348 ms; those were unpopulated passes.
- The populated local Workerd budget test measures 18 external fetches for two successful class intents, 11 for two terminal homework-submission intents, and 10 each for two terminal reminder/reset intents. Cloudflare measured the populated two-intent class canary at **16.361 ms CPU** (successful), above the documented **10 ms Workers Free Cron limit**. The runtime can allow infrequent overage, but this is not a safe sustained budget. A smaller batch or other CPU correction needs live proof before declaring first-batch capacity complete. Redirect counts and remaining browser checks are still open at this entry.

## Source and proof

- Combined branch: `codex/notification-combined-release`, based on the latest inspected Hosting source commit `070d2d3c` and merged with notification branch `codex/notification-recovery` at `1ff86c63`, then documentation checkpoint `c1446e53` and live-rule preservation commits `e3eb4baf`, `f7f12347`.
- Before this cutover, Hosting site `kahut1` had release `sites/kahut1/releases/1790240765374000`, version `aea32fa07517146f`, deployed 2026-09-24 09:06:05Z. A fresh Firebase Hosting REST read before deployment still returned this as the newest release. This version was associated with the dedicated THCS Gemma source and was the source baseline reviewed for the selective deployment.
- [Combined Linux CI run 35987727981](https://github.com/iamhuwng/autoresync/actions/runs/35987727981) passed focused app, RTDB emulator, Firestore emulator, Workerd, and Wrangler dry-run gates at `05bcbffa`, including the already-live role validator added at `f7f12347`.
- After that run, the THCS store was changed to reuse the shared OAuth token. The focused local Workerd suite passed **62/62 across 18 files** and the notification Worker Wrangler dry-run passed at **385.11 KiB**. [Linux CI run 36018675377](https://github.com/iamhuwng/autoresync/actions/runs/36018675377) passed all app, RTDB, Firestore, Workerd, and Wrangler gates at source commit `a016917e`.
- The later common-contract and batch correction at source commit `9ddd7f70` changes homework submission dispatch to `{eventKind, recordId}`, verifies the committed Firestore intent before inbox creation, removes dormant generic producer calls, gates Cron to class/homework with one trigger, and files terminal issues by report date. [Linux CI run 36125439365](https://github.com/iamhuwng/autoresync/actions/runs/36125439365) passed the app, RTDB, Firestore, Workerd, and Wrangler gates at that exact commit. Focused local checks passed: app **64/64 across 5 files**, Workerd **66/66 across 19 files**, Vite build and bundle budget, and Wrangler dry-run at **379.04 KiB**. The repository-wide TypeScript check still has unrelated pre-existing diagnostics; none point to the changed files.
- Combined source commit `2e189baa` routes ordinary post-commit homework submission/reset, test completion, and writing events through `dispatchCommittedNotification`, which sends only saved event identity to trusted Worker resolvers. Worker-owned source transitions remain narrow action commands. With `NOTIFICATION_RETRY_BATCH=class-homework`, the Worker entry point blocks other action routes. Local checks passed: **67/67 Workerd tests across 19 files**, **250/250 app tests across 17 files** plus **12/12 across the three files needing test Firebase config**, Vite build and bundle budget, and a Wrangler dry run at **380.99 KiB**. The class-manager test harness now models the Worker-committed roster/projection transition. [Linux CI run 36129380105](https://github.com/iamhuwng/autoresync/actions/runs/36129380105) passed app, RTDB emulator, Firestore emulator, Workerd, and Wrangler gates at that exact source commit.
- Source commit `ff9d0442` closes the first-batch retry gap: the allowed manual homework reminder and homework reset action routes now have scheduled retry slots alongside class membership and homework submission. The same trigger runs one family every two minutes; each family is visited every eight minutes. The focused scheduler test checks all four slots, the 19-file Workerd suite passed **67/67**, and the Wrangler dry run passed at **381.12 KiB**. [Linux CI run 36130152945](https://github.com/iamhuwng/autoresync/actions/runs/36130152945) passed app, RTDB emulator, Firestore emulator, Workerd, and Wrangler gates at that exact source commit.
- Source commit `241ad39e` makes the manual reminder and homework reset Firestore stores reuse the RTDB client's OAuth token, which already includes Firestore scope. A focused Workerd check observes one OAuth exchange per store across an empty due scan and follow-up read. The full 19-file Workerd suite passed **68/68**, and the Wrangler dry run passed at **379.38 KiB**. [Linux CI run 36131652274](https://github.com/iamhuwng/autoresync/actions/runs/36131652274) passed app, RTDB emulator, Firestore emulator, Workerd, and Wrangler gates at that exact source commit. Populated first-batch budget proof was still open at that point.
- Selective first-batch Hosting source was committed and pushed on `codex/notification-class-homework-hosting` at `c3e83309`, directly based on inspected live-source commit `070d2d3c`. It includes only class/homework clients and UI, with **65/65 focused tests across 5 files**, a Vite build, and bundle-budget pass. Its TypeScript run had existing repository diagnostics, with none in the changed files. That source artifact was subsequently deployed as recorded above.
- A local production Vite build from the combined source passed with configured Firebase build values. Bundle budget passed (`247 KB` root entry). Generated `notificationCommandClient-DpLzxhIH.js` contains `https://luyentap-notification-command.iamhuwng.workers.dev` and has SHA-256 `141230A782F51FEA71EAA29DC640E6C2F802C2A99555DF44786F544B1BCFB636`. No Gemini key values were loaded into this build. It is a build proof, not a deployable approved artifact until every required installation value and browser flow is checked.

## Rules composition

- The refreshed live RTDB rules snapshot was read after the separate AI gateway's role restriction. Its SHA-256 is `79CE0AFA69FD892BF941BD4BD30B2A143A4D92926E87011BD09F145AD51C33A3`; the local read-only copy is in `output/notification-recovery/live-rtdb-rules-refresh-20260924.json` in the notification worktree.
- The candidate `database.rules.json` preserves all four previously divergent live Book rule paths and the newer `users/$uid/role/.validate` restriction. Comparing the JSON rule tree with the refreshed live snapshot leaves **44 changed leaf/subtree paths**, all in notification-related source, query index, or intent rules. Their paths are listed in [the semantic diff inventory](notification-recovery-remote-rule-paths.txt); exact candidate expressions are in `database.rules.json`.
- Before any rules deploy, fetch live rules again, verify the snapshot hash or regenerate and review the semantic diff, then run the emulator against that exact composed file. A direct whole-file deploy from the notification source branch would overwrite live Book and AI rules and is not the release candidate.

## Cloudflare readback

At the 2026-09-25 readback, Wrangler authentication worked for account `e41db829dabe9993f03674afdfd56510`. The active deployment was version `6df57eec-587b-44b5-9a59-6d65ba13a70a` at 100% (2026-09-23). `wrangler versions view` showed its script had only a `fetch` handler and its bindings lacked `FIREBASE_WEB_API_KEY`; the candidate's scheduled handler was **not live**. The dedicated Identity Toolkit key is restricted to `identitytoolkit.googleapis.com` and its value was verified via `gcloud` without printing it. It was staged with `wrangler versions secret put` as **undeployed** version `f4d45512-f2d0-4d51-b8ea-8de9012ddb27`; version readback showed the secret binding, while deployment readback confirmed version `6df57eec` stayed active. The runtime service account then had `roles/datastore.user` in addition to `roles/firebasedatabase.admin`. No Worker deployment occurred in this source-preparation task. Read back the active script, bindings, cron triggers, identity roles, and secret names immediately before release.

## Fresh remote readback (2026-09-25 11:45 UTC)

- `gcloud` used active account `iamhuwng@gmail.com` and project `temp-a1437`. IAM still grants the notification runtime service account `roles/datastore.user` and `roles/firebasedatabase.admin`. The dedicated API key still targets only `identitytoolkit.googleapis.com`; this readback did not print its value.
- Firebase Hosting REST still returned `sites/kahut1/releases/1790240765374000` with version `aea32fa07517146f` as the newest release (2026-09-24 09:06:05 UTC). The selective Hosting source remains a candidate only.
- Live RTDB rules fetched from `/.settings/rules.json` had raw SHA-256 `00880084C563D6D24445B390FFF4312A041536AC8523E3626136DE6723746B5D`. Their parsed rule tree has **zero** differences from the previous 2026-09-24 snapshot. The candidate `database.rules.json` still differs at the same **44** notification-related paths listed in [the semantic diff inventory](notification-recovery-remote-rule-paths.txt); the path set matches exactly. The fresh raw copy is untracked at `output/notification-recovery/live-rtdb-rules-20260925.json` in this worktree.
- Firestore's live `cloud.firestore` release still points to ruleset `c8061403-2130-4837-97f8-a5dce3852f3a` (updated 2026-08-23). Its source hash differs from the candidate `firestore.rules`, so the candidate rules are not live.
- `wrangler deployments list` still reported active version `6df57eec-587b-44b5-9a59-6d65ba13a70a` at 100%. Version readback showed only a `fetch` handler and no `FIREBASE_WEB_API_KEY`. The undeployed version `f4d45512-f2d0-4d51-b8ea-8de9012ddb27` still carries the secret name. Cloudflare Dashboard Settings directly showed **no Cron triggers configured** for the notification Worker and listed both encrypted secret names at service scope. This readback did not exercise delivery.
- The populated first-batch Workerd check executes the real retry handlers and Firebase stores against mocked Firebase HTTP. Two due intents per pass used **18** external requests for successful class membership delivery, **11** for terminal homework submission failure, and **10 each** for terminal manual-reminder and homework-reset failure. The check covers one meaningful path per family and does not measure Cloudflare CPU time or network redirects. Local elapsed time includes test/runtime overhead and is not Cloudflare CPU proof.

## Product and release gates

The following checklist was written before cutover. The current status and remaining proof are recorded above; completed deployment steps here are historical instructions.

Local browser read-path check on 2026-09-25: the combined source rendered the teacher Materials and Classes pages at `http://localhost:5173` and the authenticated student dashboard and existing notification feed at `http://localhost:5174`. The existing student class-join notice's Open action targets the dashboard, so remaining on that URL is expected. The browser checks did **not** exercise a newly committed action, Worker delivery, readback, retry, or admin issue; those remain release gates.

1. Refresh live rules and Hosting source, compose and emulator-check the rules, then deploy the combined Worker with a verified `FIREBASE_WEB_API_KEY` secret and Firestore-capable service account. The candidate Worker config enables one `*/2 * * * *` trigger and `NOTIFICATION_RETRY_BATCH=class-homework`. Its four-slot first-batch rotation covers class membership, homework submission, manual homework reminder, and homework reset retries, visiting each every eight minutes; its entry point rejects other notification action routes. Read back active version, bindings, and trigger. The previous active-version readback was fetch-only and lacked the key; recheck before release.
2. Keep Hosting on its current live release while validating the class/homework Worker batch. Review the selective `c3e83309` Hosting source artifact against the latest live release, preserving its Book and AI changes. Deploy that artifact only after the server/rules phase, then verify an actual committed action, Worker inbox delivery, read flag, link, one retry, and a terminal admin issue in real teacher/student browsers at `http://localhost:5173` and `http://localhost:5174` and in the authorized live target. Terminal issue writers now use the report date, which the Admin Error Log reads. Green CI alone does not satisfy this gate.
3. Measure the fully composed scheduled invocation, including queue reads, OAuth, recipient writes, redirects, failure issue, CPU, and overlapping triggers, against current Free plan limits. Then stage the remaining families by changing `NOTIFICATION_RETRY_BATCH` to `all` and adding the `* * * * *` bulk trigger. Read back both triggers and queue progress for each family before the remaining combined Hosting cutover. The [scheduler budget](notification-recovery-scheduler-budget.md) records the proposed full cadence and the two free slots on 2026-09-25; populated-queue CPU proof remains open.
4. Establish evidence-backed outage bounds and source records for the [35-row historical ledger](notification-recovery-historical-backfill.md), then run read-only previews, reviewed bounded writes, readback, and replay checks. No historical backfill has run. Row 32, THCS writing auto grade, has no committing source and remains outside delivered coverage.
5. Keep Book assignment/update notices on the same existing inbox and verify their committed-action behavior in the live release. Check current Hosting source and remote RTDB rules immediately before any cutover because both can change independently of this candidate.
