# Handoff

## Working Folder

- Active folder: `C:\Users\The Lord\Desktop\luyentap-writing-import-rebased`
- Repo root: `C:/Users/The Lord/Desktop/luyentap-writing-import-rebased`
- Branch: `codex/prd0052-material-tabs-inline`
- HEAD: `6f05e857`
- Worktree identity: `C:/Users/The Lord/Desktop/luyentap-writing-import-rebased`, branch `refs/heads/codex/prd0052-material-tabs-inline`
- Worktree state: dirty; no files staged or committed in this session.
- Other worktrees exist. Do not assume sibling worktrees are target unless user names them.

### Dirty / Untracked Summary

- Deleted tracked, pre-existing: `.claude/skills/kn-*` skill files. Do not restore/remove unless asked.
- Modified tracked, pre-existing/mixed PRD-0052 work: PRD docs, `r2-backup-worker/src/reading-v2/submit.ts`, Teacher Lobby, Teacher Test Results, Reading V2 services, homework card/modal/detail, student practice tests.
- Modified tracked current-session: `package.json`, PRD-0052 docs, `src/components/homework/HomeworkCard.tsx`, `src/components/homework/HomeworkCreateModal.*`, `src/pages/TeacherHomeworkDetailPage.*`, `src/services/reading-v2/readingV2PassageHomeworkLaunch.*`, `src/services/reading-v2/readingV2PassageLibrary.*`.
- New untracked current-session: `scripts/material-catalog-repair.ts`, `src/components/homework/HomeworkCard.test.tsx`, `src/services/materialCatalog/materialCatalogRepair.service.ts`, `src/services/materialCatalog/materialCatalogRepair.service.test.ts`, `src/services/materialCatalog/materialCatalogRepairCli.test.ts`.
- Untracked pre-existing: `.agents/skills/handoff-writer/`, `.claude/skills/google-cloud-cli-first/`, `.claude/skills/handoff-writer/`, `output/firebase-*`, `output/playwright/prd0052-submit-*.txt`, `r2-backup-worker/src/reading-v2/submit.test.ts`, `r2-backup-worker/vitest.config.ts`.
- Git warning persists: `json` LF will be replaced by CRLF if Git touches it. Do not touch `json` unless asked.

## Next Session Focus

Continue PRD-0052 gap closure from current dirty branch. Best next move: decide whether to run the Material Catalog repair runner in dry-run/report mode, then review the report before any live write. Live mutation needs explicit user approval and the reviewed dry-run report digest/count must match.

After repair-plan proof, close remaining P0/P1 proof gaps: Book atomic write-failure proof or owner-approved deferment, RTDB/Firestore emulator rules proof when Java is available, then final docs reconciliation.

## Current State

- Main tasklist: `documentation/tasks/PRD0052/tasks-0052-prd-teacher-materials-books-and-reading-passage-library-gap-closure.md`
- Evidence note: `documentation/tasks/PRD0052/prd0052-gap-closure-evidence-2026-06-02.md`
- Backfill plan: `documentation/tasks/PRD0052/prd0052-reading-v2-backfill-dry-run-plan.md`
- Worker deployed earlier with Reading Passage set submit support:
  - URL: `https://r2-backup-worker.iamhuwng.workers.dev`
  - Version ID: `0c28124d-88b7-403b-b0cb-bb7d1cd25a79`
  - Firebase project binding: `temp-a1437`

### Live Proof Captured

- Fresh browser proof on `http://localhost:5174` for Reading Passage homework history/detail passed.
- Homework set: `SiDFz9BPXOCSKhgoxTBi`
- Timeline card showed:
  - `Selected Reading Passages`
  - `Active`
  - `Reading Passage Set`
  - source `Source unknown - PRD0052 QA Reading V2 Full Test 2026-06-03`
  - Test Type `IELTS`
  - submissions `1 / 1`
- Detail route `/teacher/homework/SiDFz9BPXOCSKhgoxTBi` showed same metadata plus:
  - `QUESTIONS 26`
  - assigned students `1`
  - completion `100%`
  - average `42%`
  - student status `Submitted`
- Browser console: 0 errors. Only unrelated existing Mantine guard warnings for `ClassSelectionModal.jsx` and `UseAsIsModal.jsx`.
- Dev server was started earlier on `localhost:5174`, PID `17100`, with PRD-0052 flags enabled. Verify it is still running before reuse.

Earlier live proof already captured:
- Bulk Reading Passage homework set submit passed for homework set `SiDFz9BPXOCSKhgoxTBi`; result `reading-v2-result-6211194e-a441-4192-a2d0-5353a668bf07`.
- Existing full Reading V2 live-session regression passed for session `SMC10J`; result `reading-v2-result-1c4b4c6b-e32b-4b99-9bc8-b8354c6f24e9`.
- Fresh deployed-Worker full-test recheck passed for session `A91JFM`; result `reading-v2-result-01384d15-cbc3-4e55-beb4-7ccacf6bebd2`.
- `Create full test from selected` recovered and proved against `material_catalog/material_indexes` with no duplicate `reading_v2/reading_passage_materials/{materialId}`.

### Local Changes Added

- Material Catalog repair planner/runner:
  - `src/services/materialCatalog/materialCatalogRepair.service.ts`
  - `src/services/materialCatalog/materialCatalogRepair.service.test.ts`
  - `src/services/materialCatalog/materialCatalogRepairCli.test.ts`
  - `scripts/material-catalog-repair.ts`
  - `package.json` script: `repair:material-catalog`
- Runner default is dry-run.
- Write mode requires `--write --approved <id> --from-report <dry-run-report.json>`.
- Write aborts if any Firebase reads fail.
- Write verifies reviewed dry-run report project/count/SHA-256 digest before mutation.
- Firebase payload sanitizes nested `undefined`.
- Reading V2 `library-eligible` visibility is normalized to Material Catalog `public`.
- Planner covers stale indexes, orphan/cascaded Book nodes, and missing Reading V2 composition-version records.
- No live repair write was run.

Docs updated with live proof and caveats:
- `documentation/tasks/PRD0052/prd0052-gap-closure-evidence-2026-06-02.md`
- `documentation/tasks/PRD0052/tasks-0052-prd-teacher-materials-books-and-reading-passage-library-gap-closure.md`
- `documentation/tasks/PRD0052/prd0052-final-handoff-checklist.md`
- `documentation/tasks/PRD0052/prd0052-implementation-coverage-matrix.md`
- `documentation/tasks/PRD0052/prd0052-implementation-notes.md`
- `documentation/tasks/PRD0052/prd0052-security-rule-validation-cases.md`

## Decisions And Constraints

- Persistent style: caveman full by default.
- Active root is `C:\Users\The Lord\Desktop\luyentap-writing-import-rebased`; stay inside unless user explicitly names another absolute path.
- Future live deploys, pushes, live backfill writes, and live repair writes require explicit user approval.
- Do not mutate sibling worktrees such as `C:\Users\The Lord\Desktop\luyentap` unless user explicitly targets them.
- Use dev quick-login buttons for browser QA; do not ask for credentials first.
- For browser QA use `http://localhost:5174`; `127.0.0.1` can be blocked by Firebase API-key referrer restrictions.
- User said port split: `5173` admin, `5174` teacher/student.
- For Windows Vitest/Vite/esbuild commands use `cmd /c ... --reporter=basic`.
- Firebase direct validation: prefer Firebase CLI first.
- Firebase CLI stdin JSON can be unreliable on Windows; use temp JSON files, verify writes, then delete temp files.
- Wrangler deploy shape requires bundled Windows x64 Node. Deploy only after explicit user approval.
- Do not reopen raw public Book reads; preserve public projection/security behavior.
- Do not add Mantine. If touching teacher UI with Mantine imports, replace per repo rule.
- Keep `TeacherHeader` attached to top shell edge if editing teacher pages.
- Do not mutate unrelated dirty/untracked files.
- Existing `output/playwright/*` request/response files may contain payload details. Inspect only as needed and do not paste secrets into docs.
- Java missing blocks emulator-backed rules proof.
- Broad root `tsc` still exits 2 from existing repo-wide debt; touched-file filter was clean.

## Remaining Work

1. Inspect current diff before edits:
   - `git status --short --branch`
   - `git diff --stat`
   - Focus on current-session files plus pre-existing Worker/TeacherLobby/TeacherTestResults diffs.
2. Material Catalog repair:
   - Review `src/services/materialCatalog/materialCatalogRepair.service.ts`, `src/services/materialCatalog/materialCatalogRepair.service.test.ts`, `src/services/materialCatalog/materialCatalogRepairCli.test.ts`, and `scripts/material-catalog-repair.ts`.
   - Run read-only dry-run/report if needed.
   - Do not run `--write` without explicit user approval.
   - If approved, write only from the reviewed report: `--write --approved <id> --from-report <dry-run-report.json>`.
3. Backfill execution:
   - Dry-run default already exists via `npm run backfill:reading-v2-passages -- [options]`.
   - Live mutation requires explicit `--write --approved` and user approval.
   - Run live backfill only after user approval.
4. Rules proof:
   - Continue RTDB/Firestore emulator validation when Java is available.
   - Emulator-backed proof remains blocked if Java is not on PATH.
5. Remaining P0/P1 proof:
   - Book atomic write-failure proof or owner-approved deferment.
   - Final material repair/backfill proof or owner-approved deferment.
6. Reconcile final docs after any further proof:
   - `documentation/tasks/PRD0052/prd0052-implementation-coverage-matrix.md`
   - `documentation/tasks/PRD0052/prd0052-final-handoff-checklist.md`
   - `documentation/tasks/PRD0052/prd0052-implementation-notes.md`
   - `documentation/tasks/PRD0052/prd0052-security-rule-validation-cases.md`
   - `documentation/tasks/PRD0052/prd0052-reading-v2-backfill-dry-run-plan.md`
   - `documentation/tasks/PRD0052/prd0052-visual-difference-note.md`
7. Verification after any further edits:
   - `cmd /c npx vitest run <targeted files> --reporter=basic`
   - `cmd /c npx tsc --noEmit --pretty false`, then touched-file filter if broad root debt remains.
   - `cmd /c npm run check:utf8 -- <changed paths>`
   - `git diff --check`
8. Do not call PRD-0052 complete/faithful until P0/P1 items have live/emulator proof or owner-approved deferment.

## Latest Verification

- `cmd /c npx vitest run src/components/homework/HomeworkCard.test.tsx src/pages/TeacherHomeworkDetailPage.test.tsx --reporter=basic`
  - PASS, 2 files / 3 tests.
- `cmd /c npx vitest run src/services/materialCatalog/materialCatalogRepair.service.test.ts src/services/materialCatalog/materialCatalogRepairCli.test.ts --reporter=basic`
  - PASS, 2 files / 7 tests.
- `cmd /c npm run repair:material-catalog -- --help`
  - PASS; help shows `--from-report`.
- `cmd /c npm run repair:material-catalog -- --write --approved lead-1`
  - Expected failure before Firebase reads: `Material Catalog repair mutation requires --from-report <dry-run-report.json>.`
- `cmd /c npm run check:utf8 -- scripts/material-catalog-repair.ts package.json src/services/materialCatalog/materialCatalogRepair.service.ts src/services/materialCatalog/materialCatalogRepair.service.test.ts src/services/materialCatalog/materialCatalogRepairCli.test.ts documentation/tasks/PRD0052/prd0052-gap-closure-evidence-2026-06-02.md documentation/tasks/PRD0052/tasks-0052-prd-teacher-materials-books-and-reading-passage-library-gap-closure.md documentation/tasks/PRD0052/prd0052-final-handoff-checklist.md documentation/tasks/PRD0052/prd0052-implementation-coverage-matrix.md documentation/tasks/PRD0052/prd0052-implementation-notes.md`
  - PASS; UTF-8 check passed for 10 files.
- `git diff --check`
  - PASS, with existing `json` CRLF warning only.
- `cmd /c npx tsc --noEmit --pretty false`
  - Broad root exits 2 from existing repo-wide debt.
  - Final touched-file filter: `touched-file-tsc-matches=0`, `tsc_exit=2`.

Earlier verification also passed:
- Expanded targeted PRD-0052 suite: 18 files / 127 tests.
- Focused Material Catalog/TeacherHomeworkDetail tests after TypeScript fixes: 2 files / 5 tests.
- TeacherHomeworkDetail focused test: 1 file / 2 tests.

## Suggested Skills

- `caveman`: keep concise default response style.
- `ripgrep-first`: narrow repo/doc discovery before edits.
- `superpowers:test-driven-development`: before new bugfix/feature implementation.
- `superpowers:systematic-debugging`: for browser/runtime failures.
- `superpowers:verification-before-completion`: evidence before completion claims.
- `firebase-cli-first`: Firebase rules/live RTDB validation.
- `playwright`: live browser QA with dev quick-login.
- `react-async-state-patterns`: use if touching teacher/student async list state.
- `wrangler` and `workers-best-practices`: only for future Worker edits/deploys; deploy only after explicit approval.

## Sensitive Data Handling

- Secrets, cookies, auth headers, API keys, passwords, private keys, and session IDs are redacted or omitted.
- Personal emails/accounts are omitted; use dev quick-login labels instead.
- Non-sensitive technical identifiers are preserved: file paths, branch names, commit hashes, task IDs, route names, function names, result IDs, attempt IDs, Worker URL, and version ID.
