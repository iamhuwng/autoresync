# Notification recovery scheduler budget

**Exact-source verification:** [CI 36264700700](https://github.com/iamhuwng/autoresync/actions/runs/36264700700) passed on `4b94ccb7d58422ad2ac2a19ec222af4ec1c5775b`: app notification services, RTDB/Firestore authority, atomic result/index deletion, Workerd actions, candidate bundle and same-class recovery generator/bundle. The final local focused run passed 36/36. Source review found no concrete defects; the production-exported singleton native RPC check closes the construction-only proof gap. **DEPLOYMENT HELD** pending the planner's consolidated decision. No DO provisioning, remote pause/deploy/resume or backlog seed has run for this candidate.

**September 27 source candidate:** **SOURCE IMPLEMENTATION AUTHORIZED, DEPLOYMENT HELD**. One native SQLite DO executes class every two minutes and rotates homework/reminder/reset every six minutes using two serial RPCs. Both calls are attempted and collected failures surface as AggregateError. Each RPC has a finally-cleared 20-second abort deadline propagated to every provider request, rejects redirects and caps inbox contention at four CAS rounds. The composed 47-call check is a source release gate; live DO CPU/duration/backlog remain unmeasured. [Candidate](notification-recovery-executor-proposal.md), [same-class recovery](notification-recovery-executor-recovery.md).

**Prior live measurement checkpoint, September 27 (September 26 UTC):** Worker `be0c3e4e` from `7fdfb636` is read back 100% active with the same batch and single Cron. Two normal two-recipient class slots measured **10.646 ms / nine subrequests** at 18:16:29 and **7.319 ms / nine** at 18:24:29; both intents completed attempt 2. The second invocation's tail identifies `be0c3e4e` and outcome `ok`. A real QA homework saved while its tab's notification transport was deliberately blocked; after the tab closed and only that record's due time was conditionally advanced, the 18:26:29 retry measured **8.573 ms / seven subrequests**, reached done/attempt 2 and delivered the teacher notice. Tail independently identifies the exact version and success. These cached-token samples do not close cold CPU or sustained backlog. Repeated tuning/deployment is paused at the planner's direction. The [single internal DO proposal](notification-recovery-executor-proposal.md) is **UNDER REVIEW**, with two capped RPCs per existing Cron and source-derived ceilings; no runtime, binding or schedule change has occurred. Later families, the second Cron and historical writes remain held. The following dated sections are retained historical checkpoints.

**Current rotation status (2026-09-26 05:28 UTC):** Wrangler deployment status shows Worker `d4da93f2-37a2-4a23-bda2-faeada990cf7` at 100%, with `NOTIFICATION_RETRY_BATCH=class-homework` and the same single `*/2 * * * *` Cron. This release fixes repeated recovery-issue rewrites; it does not prove capacity. Native inbox creation previously measured 14.735 ms cold, 8.110 ms warm, and 9.550 ms warm with a prior issue for one class retry. Cold CPU fails the 10 ms target. The real pre-cache homework retry measured 14.212 ms; populated post-cache homework CPU is unrecorded. The one-intent/eight-minute class cadence still yields a calculated four-hour drain for thirty due class actions. Therefore the full-family trigger, later families, and any capacity claim remain held. See the [handoff](notification-recovery-rotation-handoff-2026-09-26.md), [release record](notification-recovery-release-candidate.md), and [CPU record](notification-recovery-cpu-escalation.md). Dated measurements below are historical checkpoints.

Checked against source on 2026-09-25. The release candidate config has one `*/2 * * * *` trigger and `NOTIFICATION_RETRY_BATCH=class-homework`. It rotates class membership, homework submission, manual homework reminder, and homework reset retries, one bounded family every two minutes. Each first-batch family is visited every eight minutes, including the two action routes exposed in the selective Hosting artifact. The full-family stage requires a separate config change to `NOTIFICATION_RETRY_BATCH=all` plus a `* * * * *` bulk trigger. The full schedule below has three bulk families and 14 small-family slots, including two each for class and homework submission; both triggers can fire on even minutes.

**Current populated CPU gate (2026-09-26): open.** Version `f5f1448a-04e7-440a-99ef-5a1096271fb4` ran one normal `retry_due` pending-join intent at 01:44:16 UTC, claimed attempt 2, delivered both recipients, and marked it done. Cloudflare measured one successful invocation, zero errors, 13 subrequests, and **12.422 ms CPU**. Reducing the class pass to one intent still does not fit the 10 ms Workers Free CPU limit on this branch. The 7.424 ms interrupted-retry report below does not close this gate. Further family activation and populated backlog proof remain on hold pending a measured correction.

**Focused correction checkpoint (02:12 UTC):** the `print=silent` experiment on version `6f98156e-3911-47b0-844c-2ae6c8205f43` failed with Firebase HTTP 400 before claiming the intent; Firebase does not support that parameter with `if-match`. Corrective source `7604a853` deployed as `741a9945-cf7b-4718-a8f6-228db440c034` at 02:08:42 UTC, removing that option and skipping only unnecessary healthy-success gate writes. Supported conditional same-value REST write/readback succeeds with HTTP 200; the retained QA intent remains attempt 1 until its next scheduled pass. The four-file focused Workerd set passes 21 tests and Wrangler dry-run passes at 391.95 KiB. Normal healthy class delivery now accounts for 12 subrequests locally. Delivery success with an existing failure issue still needs a gate write plus the issue read/write, making 15 subrequests on that branch (source accounting, not live CPU proof). Both live CPU and realistic queue progress remain open. At the current one-intent-per-eight-minute cap, 30 simultaneously due class actions would require 30 passes, roughly four hours; this is a calculated capacity concern, not a measured queue-drain result.

**Live second-correction result (02:17 UTC):** version `741a9945` delivered the retained normal class retry at 02:16:11 UTC, with one successful request, zero errors, 11 subrequests, and **11.627 ms CPU**. Delivery is restored; CPU remains over the 10 ms target. Both focused attempts are exhausted. See the [CPU escalation report](notification-recovery-cpu-escalation.md). Additional CPU changes require the planner's decision; capacity, prior-issue recovery cost, and realistic backlog proof remain open.

| Queue | Maximum interval between its passes | Maximum delay after a one-hour retry due time |
|---|---:|---:|
| Course announcement, THCS homework, session | 3 minutes | 3 minutes |
| Class and homework | 20 minutes | 20 minutes |
| Other small families | 28 minutes | 28 minutes |

For a 30-recipient bulk event at 10 recipients per pass, an immediate first pass leaves two Cron passes, so nominal completion is within 6 more minutes when the backend is healthy. A 500-recipient session takes up to 150 minutes if it waits for Cron from the start. The configured 1,000-recipient announcement ceiling takes up to 300 minutes on the same assumption. Busy queues add delay; there is no per-student polling loop. Failed recipient batches become due for one retry about one hour after the initial pass finishes.

The first stage invokes the Worker 720 times per UTC day and uses one Cron slot. The full stage invokes it 1,440 + 720 = 2,160 times per UTC day, 2.16% of the Workers Free 100,000-request daily limit, and uses two of the account's five Cron slots. The source caps a bulk invocation at 10 recipients and a small-family due scan at one to three intents. On even minutes both full-stage triggers fire as **separate invocations**, so the per-invocation subrequest and CPU limits apply independently while both count toward the daily request limit.

Cloudflare API readback before the first-batch deploy on 2026-09-25 listed all 11 account Workers and their schedules: `luyentap-book-source-reconciliation-preview` (`*/5 * * * *`), `r2-backup-worker` (`0 3 * * 1`), and `r2-upload-signer` (`0 * * * *`). The first-batch cutover then added the notification Worker's `*/2 * * * *` trigger, so **4 of 5** account slots are now in use. The proposed full-stage `* * * * *` trigger would fill the last slot. Recheck the account before that stage. [Cloudflare schedule API](https://developers.cloudflare.com/api/resources/workers/subresources/scripts/subresources/schedules/methods/get/).

The THCS Firestore store shares the RTDB client's OAuth token, whose scope includes Firestore, instead of signing and exchanging a token on every Firestore request. The manual reminder and homework reset stores now use that same cached token path; a focused Workerd check observes one OAuth exchange per store across an empty due scan and follow-up Firestore read. Another focused Workerd test measures **23 external fetches** for an empty THCS due scan plus ten inbox creates: one OAuth, one Firestore query, one RTDB queue query, ten idempotency reads, and ten inbox writes. Source-path accounting for a ten-recipient scheduled pass gives 29 requests for course announcements with a terminal admin issue, 30 for sessions with a terminal admin issue, and up to **41 for THCS homework** with a class roster, terminal issue, and all three conditional-write attempts on both Firestore intent updates. These are no-redirect counts. Cloudflare Free currently allows 50 external subrequests and 10 ms CPU per invocation. Redirects count as extra subrequests. [Cloudflare Workers limits](https://developers.cloudflare.com/workers/platform/limits/), [Cron Trigger handler](https://developers.cloudflare.com/workers/runtime-apis/handlers/scheduled/).

The populated **first-batch** Workerd check executes each real retry handler and store against mocked Firebase HTTP with two due intents. It measures 18 external requests for successful class membership delivery, 11 for terminal homework submission failure, and 10 each for terminal manual-reminder and homework-reset failure. The homework path uses two OAuth exchanges because its Firestore query and RTDB failure reporter own separate token clients. These checks cover one path per family; Cloudflare CPU, redirects, and other populated branches remain unmeasured. Local elapsed timing is not a substitute for Cloudflare invocation CPU readback.

Source checkpoint 2026-09-26: the class family now checks durable retry suppression and records success through the same cached RTDB client. Its one-intent populated Workerd case uses 13 external requests (one OAuth exchange, seven reads, five writes), up from 10 before the gate. The older two-intent measurements above remain historical. Live populated CPU for this source is unmeasured.

Live recovery-report checkpoint: version `06ec2c39-4d3d-4a2a-b799-18ad277b4279` processed a single interrupted `retrying` class intent at 2026-09-25 20:24 UTC. GraphQL measured one successful invocation, zero errors, eight subrequests, and 7.424 ms CPU p50/p99. Firebase and the signed-in admin browser confirmed its terminal reason-coded issue and one gate increment. This branch reads the inbox and reports failure; it does **not** claim a normal retry or create an inbox row. Normal `retry_due` delivery and sustained backlog CPU proof remain open. The QA records were removed after readback.
The homework submission retry now uses nine external requests in its terminal `retrying` case, up from seven, because it records the family failure after the admin issue. Its first retry reads the gate before claiming. Immediate delivery records success, and a new immediate failure while suppressed reports the verified intent and marks it failed without consuming a later attempt. These are source and Workerd checks, not Cloudflare CPU proof.
The manual-reminder and homework-reset terminal `retrying` cases now use eight external requests each, up from six, to persist the family failure. The scheduled claim paths read suppression before consuming a retry. Their immediate failure paths report once and mark the intent failed while suppressed. Fresh success also writes `lastSuccessAt` to the existing admin issue, adding one issue read and one conditional write when an issue path is saved. A seven-file focused Workerd run passed 27/27; the notification Worker Wrangler dry-run bundled at 389.83 KiB. [Exact-source CI run 36174482028](https://github.com/iamhuwng/autoresync/actions/runs/36174482028) passed. Version `28f857f1-66a9-4e99-82f9-34bcdb2bf9e8` then had four successful unpopulated Cron slots at 18:40–18:46 UTC, with 8.170, 4.730, 2.481, and 4.392 ms CPU. Populated live CPU remains the release gate.

Before the full-family stage, recheck the one free Cron slot, measure subrequests/CPU for a composed 30-recipient event and failed batch, and read back scheduled queue progress. [Linux CI run 36145092617](https://github.com/iamhuwng/autoresync/actions/runs/36145092617) passed the app, RTDB emulator, Firestore emulator, Workerd, and Wrangler gates with the `fetch` receiver correction. Live class delivery and terminal Cron canaries passed. All four post-fix Cron slots succeeded; empty class, homework submission, and manual reminder slots measured 9.6, 2.888, and 5.474 ms CPU, respectively. The populated two-intent class canary succeeded at **16.361 ms CPU**, above [Cloudflare's 10 ms Workers Free Cron limit](https://developers.cloudflare.com/workers/platform/limits/#cpu-time). Cloudflare documents occasional limit flexibility, which does not establish a sustainable capacity margin. Optimize or reduce populated scheduled work, then remeasure; the full-stage CPU and redirect budget remains open.
