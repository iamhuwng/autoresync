# Task List: PRD-0034 — Teacher Homework Management Overhaul

**Source PRD:** `documentation/tasks/0034-prd-teacher-homework-management-overhaul.md`
**Generated:** 2026-03-13

---

## Relevant Files

### Phase 1: Foundation

- `src/components/modern/VanillaTabs.tsx` — Generic vanilla CSS tabs component replacing Mantine `Tabs`
- `src/components/modern/VanillaTabs.css` — Styles for VanillaTabs
- `src/components/modern/VanillaLoader.tsx` — Vanilla CSS spinner replacing Mantine `Loader`
- `src/components/modern/VanillaLoader.css` — Styles for VanillaLoader
- `src/components/modern/ToastNotification.tsx` — Vanilla toast system replacing Mantine `notifications`
- `src/components/modern/ToastNotification.css` — Styles for ToastNotification
- `src/components/modern/index.js` — Barrel export (add new components)
- `src/hooks/useHomeworkDetail.ts` — NEW hook: real-time homework detail + submissions via onSnapshot
- `src/hooks/useHomeworkDetail.test.ts` — Tests for useHomeworkDetail
- `src/hooks/useHomeworkList.ts` — MODIFY: add pagination, sort, archive filter, loadMore
- `src/hooks/useHomeworkList.test.ts` — Tests for useHomeworkList (update existing)
- `src/hooks/useClassRoster.ts` — NEW or existing: load class members for "not started" computation
- `src/pages/TeacherHomeworkDetailPage.tsx` — NEW page: homework detail with submission table
- `src/pages/TeacherHomeworkDetailPage.css` — Styles for detail page
- `src/pages/TeacherHomeworkListPage.tsx` — REWRITE: vanilla layout, remove Mantine
- `src/pages/TeacherHomeworkListPage.css` — Styles for list page
- `src/components/homework/HomeworkSummaryStats.tsx` — NEW: stats bar component
- `src/components/homework/HomeworkSummaryStats.css` — Styles
- `src/components/homework/HomeworkSubmissionTable.tsx` — NEW: student submission table
- `src/components/homework/HomeworkSubmissionTable.css` — Styles
- `src/components/homework/HomeworkAlertBanner.tsx` — NEW: "needs attention" banner
- `src/components/homework/HomeworkAlertBanner.css` — Styles
- `src/components/homework/HomeworkScoreDistribution.tsx` — NEW: 5-bar pure CSS chart
- `src/components/homework/HomeworkScoreDistribution.css` — Styles
- `src/components/homework/HomeworkBreadcrumb.tsx` — NEW: breadcrumb nav
- `src/components/homework/HomeworkBreadcrumb.css` — Styles
- `src/components/homework/HomeworkResultsSummary.tsx` — DELETE (replaced by detail page)
- `src/components/homework/UpcomingHomeworkWidget.tsx` — MIGRATE: remove Mantine imports
- `src/components/homework/HomeworkStatusBadge.tsx` — VERIFY: already Mantine-free
- `src/components/homework/HomeworkCard.tsx` — VERIFY: already Mantine-free; ADD alert badges
- `src/components/homework/index.ts` — UPDATE barrel exports
- `src/App.jsx` — ADD new routes for detail page (Phase 1) and student profile (Phase 3)
- `src/constants/routes.ts` — ADD TEACHER_HOMEWORK_STUDENT route constant

### Phase 2: Power Tools

- `src/services/homeworkManager.ts` — MODIFY: add archive/restore/permanentDelete/updateStudentOverride, modify closeHomework
- `src/services/homeworkAutoTransitionService.ts` — MODIFY: add auto-archive, fix alert window 1h→24h
- `src/services/homeworkSubmissionService.ts` — MODIFY: isLateSubmission override check, isStudentExempted
- `src/services/homeworkBulkOperations.ts` — REUSE existing functions (DO NOT duplicate)
- `src/hooks/useBulkSelection.ts` — NEW: generic multi-select state
- `src/hooks/useHomeworkTags.ts` — NEW: load tags from app_config/homework_tags
- `src/components/homework/HomeworkBulkActionBar.tsx` — NEW: floating action bar
- `src/components/homework/HomeworkBulkActionBar.css` — Styles
- `src/components/homework/BulkExtendModal.tsx` — NEW: bulk extend deadline modal
- `src/components/homework/BulkDeleteConfirmModal.tsx` — NEW: type "DELETE" confirmation
- `src/components/homework/HomeworkTagChips.tsx` — NEW: tag display + filter
- `src/components/homework/HomeworkTagChips.css` — Styles
- `src/components/homework/StudentActionMenu.tsx` — NEW: per-student action dropdown
- `src/components/homework/ExtendStudentDeadlineModal.tsx` — NEW: per-student extend modal
- `src/components/homework/ExemptStudentModal.tsx` — NEW: exempt with reason
- `src/components/homework/StudentNoteEditor.tsx` — NEW: inline note editor
- `src/components/admin/AdminTagManager.tsx` — NEW: tag CRUD in admin sidebar
- `src/components/admin/AdminTagManager.css` — Styles
- `src/types/homework.types.ts` — MODIFY: add tags, archived, closedAt, studentOverrides fields

### Phase 3: Intelligence

- `src/components/homework/ClassAnalyticsHeader.tsx` — NEW: per-class stats
- `src/components/homework/ClassAnalyticsHeader.css` — Styles
- `src/components/homework/AtRiskStudentList.tsx` — NEW: collapsible at-risk warning
- `src/components/homework/AtRiskStudentList.css` — Styles
- `src/pages/StudentHomeworkProfile.tsx` — NEW: student homework history page
- `src/pages/StudentHomeworkProfile.css` — Styles
- `src/hooks/useStudentHomework.ts` — NEW: load student submissions, paginated

### Phase 4: Polish

- `src/components/homework/TemplateSaveModal.tsx` — NEW: replaces window.prompt()
- `src/components/homework/TemplateSaveModal.css` — Styles

### Notes

- Unit tests should be placed alongside the code files they test (e.g., `MyComponent.tsx` and `MyComponent.test.tsx` in the same directory).
- Use `npx vitest run [optional/path/to/test/file]` to run tests.
- **Rule 15:** Zero `@mantine/*` imports in any new or modified file. Use `Card`, `Button`, `Input` from `src/components/modern`.
- **Route ordering:** `/teacher/homework/student/:studentId` MUST be registered BEFORE `/teacher/homework/:homeworkId` in `App.jsx`.
- **Field name:** Teacher ownership field is `createdBy` (NOT `teacherId`). All Firestore queries must use `createdBy`.
- **Existing modern components:** `Card`, `CardBody`, `CardHeader`, `CardFooter`, `Button`, `ButtonGroup`, `Input`, `Textarea`, `NativeSelect` are in `src/components/modern/index.js`.
- **Existing `ContentTabs`:** Located at `src/components/modern/ContentTabs.jsx` — this is hardcoded for TeacherLobby. Do NOT reuse for homework. Create a generic `VanillaTabs` instead.
- **`HomeworkStatusBadge`:** Already Mantine-free (`src/components/homework/HomeworkStatusBadge.tsx`). Reuse in alert badges.
- **Layout pattern:** Follow `TeacherLobbyPage` vanilla layout — `TeacherHeader` + `div` with `maxWidth: 1400px`, `margin: 0 auto`, `padding: 2rem 1rem`, gradient background with `backgroundAttachment: fixed`.
- **Color palette:** purple `#8b5cf6`, green `#10b981`, red `#ef4444`, amber `#f59e0b`, slate `#64748b`.
- **Animation pattern:** `slideDown` for headers, `slideUp` for cards (staggered `0.05s`), `scaleIn` for empty/loading states.

---

## Tasks

### Phase 1: Foundation

- [x] 1.0 Create Shared Vanilla UI Components (replaces Mantine primitives)
  - [x] 1.1 Create `VanillaTabs` component at `src/components/modern/VanillaTabs.tsx` with CSS file. Props: `tabs: Array<{ key: string; label: string; icon?: string }>`, `activeTab: string`, `onTabChange: (key: string) => void`. Render as a row of `<button>` elements. Active tab gets `.active` class with bottom border in purple (`#8b5cf6`). Use `slideDown` animation on mount. Must NOT import anything from `@mantine/*`. Reference the existing `ContentTabs.jsx` for button styling patterns (use `Button` from modern components with `variant="glass"` for inactive, `variant="primary"` for active). Unlike `ContentTabs`, this component must accept dynamic tab definitions via props instead of hardcoding tab labels.
  - [x] 1.2 Create `VanillaLoader` component at `src/components/modern/VanillaLoader.tsx` with CSS file. Props: `size?: 'sm' | 'md' | 'lg' | 'xl'`, `color?: string` (default: `#8b5cf6`). Render a CSS-only spinning circle using `border` + `border-top-color` + `@keyframes spin`. Size map: `sm=16px`, `md=24px`, `lg=32px`, `xl=48px`. Must NOT import anything from `@mantine/*`.
  - [x] 1.3 Create `ToastNotification` system at `src/components/modern/ToastNotification.tsx` with CSS file. Export two things: (A) a `<ToastContainer />` component that renders a fixed-position container at `top: 1rem; right: 1rem; z-index: 9999` showing active toasts with `slideDown` animation on enter and `fadeOut` on exit, and (B) a `toast` singleton object with methods `toast.success(message)`, `toast.error(message)`, `toast.info(message)` that add toasts to a module-level array and trigger re-render via a `useSyncExternalStore` pattern. Each toast auto-dismisses after 4 seconds. Color map: success=green `#10b981`, error=red `#ef4444`, info=blue `#3b82f6`. Toast card style: `background: white`, `border-radius: 12px`, `box-shadow`, `padding: 0.75rem 1rem`, left color bar (4px). Must NOT import anything from `@mantine/*`.
  - [x] 1.4 Update `src/components/modern/index.js` barrel export to add: `export { VanillaTabs } from './VanillaTabs'`, `export { VanillaLoader } from './VanillaLoader'`, `export { ToastContainer, toast } from './ToastNotification'`.
  - [x] 1.5 Add `<ToastContainer />` to `src/App.jsx` — place it just before the closing `</BrowserRouter>` tag, outside `<Suspense>`, so toasts are always visible. Import from `../components/modern`.

- [x] 2.0 Build `useHomeworkDetail` Hook — Real-Time Homework Detail Data Layer
  - [x] 2.1 Create `src/hooks/useHomeworkDetail.ts`. Input: `homeworkId: string | undefined`. Return type: `{ homework: HomeworkAssignment | null, submissions: HomeworkSubmission[], loading: boolean, error: string | null, refetch: () => void }`. Implementation: (1) Use `getDoc` to load the homework document from `homework_assignments` collection by `homeworkId`. (2) Use `onSnapshot` on `homework_submissions` collection with `where('homeworkId', '==', homeworkId)` to get real-time submissions. (3) Debounce the onSnapshot callback at 500ms using `setTimeout`/`clearTimeout` pattern (see PRD §6.5 for exact code). (4) Clean up both listener and timeout in the `useEffect` return. (5) Set `loading=true` initially, `false` after first data arrives. (6) Catch errors and set `error` string. Import Firestore functions from `firebase/firestore` and db from `../../services/firebase`.
  - [x] 2.2 Add a `refetch` function that manually re-reads the homework document (for when teacher modifies it). This does NOT need to re-subscribe to submissions (onSnapshot handles that). Just call `getDoc` again on the homework document and update the `homework` state.
  - [x] 2.3 Handle edge case: if `homeworkId` is `undefined` or empty string, return early with `loading=false`, `homework=null`, `submissions=[]`. Do NOT create any Firestore listeners.
  - [x] 2.4 Create or verify `src/hooks/useClassRoster.ts`. This hook provides the list of students enrolled in a class — required by `HomeworkSubmissionTable` to compute "Not Started" students (AC-1.7). Input: `classId: string | undefined`. Return type: `{ students: Array<{ uid: string; name: string }>, loading: boolean, error: string | null }`. Implementation: (1) Query RTDB path `classes/{classId}/students` using `get(ref(database, 'classes/' + classId + '/students'))`. (2) Map the snapshot to `{ uid: key, name: entry.name || entry.displayName || 'Unknown' }`. (3) Handle missing class: if classId is undefined or snapshot doesn't exist, return `{ students: [], loading: false, error: null }`. (4) Handle deleted class (Edge Case E4): if the query fails or returns empty, return empty array — the `HomeworkSubmissionTable` will show a banner: "Could not load class roster — some students may not appear." (5) Import `database` from `../services/firebase`, `ref, get` from `firebase/database`. Wire this hook into `TeacherHomeworkDetailPage`: call `useClassRoster(homework?.target?.type === 'class' ? homework.target.classId : undefined)` and pass the `students` array as the `classRoster` prop to `HomeworkSubmissionTable`.

- [x] 3.0 Refactor `useHomeworkList` Hook — Pagination, Sort, Archive Filtering
  - [x] 3.1 Open `src/hooks/useHomeworkList.ts`. Add new fields to `UseHomeworkListOptions`: `excludeArchived?: boolean` (default `true`), `excludeClosed?: boolean` (default `true`), `sortBy?: 'dueDate_desc' | 'dueDate_asc' | 'createdAt_desc' | 'updatedAt_desc' | 'completionRate_desc' | 'completionRate_asc' | 'title_asc'` (default `'dueDate_desc'`), `tagFilter?: string`, `pageSize?: number` (default `25`).
  - [x] 3.2 Add new fields to `UseHomeworkListReturn`: `loadMore: () => Promise<void>`, `hasMore: boolean`, `sort: string`, `setSort: (sort: string) => void`, `tagFilter: string | null`, `setTagFilter: (tag: string | null) => void`, `totalLoaded: number`, `statusCounts: Record<string, number>`. Compute `statusCounts` from the FULL unfiltered homework array (before status/tag filtering): `const statusCounts = allHomework.reduce((acc, hw) => { acc[hw.status] = (acc[hw.status] || 0) + 1; return acc; }, {} as Record<string, number>);`. This ensures filter buttons always show total counts regardless of current filter.
  - [x] 3.3 Modify `fetchHomework` logic: after calling `getHomeworkByTeacher(teacherId)`, apply client-side filtering: (1) If `excludeArchived` is true (default), filter out items where `hw.archived === true`. (2) If `excludeClosed` is true (default), filter out items where `hw.status === 'closed'`. (3) If `tagFilter` is set, filter to items where `(hw.tags ?? []).includes(tagFilter)`. (4) Apply sort using `Array.sort()` based on `sortBy` value. For `dueDate_desc`: sort by `hw.scheduling.dueDate` descending. For `title_asc`: sort by `(hw.title || hw.materialTitle).localeCompare(...)`. For `completionRate_desc`: sort by `(hw.stats?.submitted ?? 0) / Math.max(hw.stats?.totalAssigned ?? 1, 1)` descending. For `updatedAt_desc`: sort by `hw.updatedAt` descending.
  - [x] 3.4 Implement client-side pagination: store a `displayCount` state starting at `pageSize` (default 25). The `filteredHomework` return value should be `sortedHomework.slice(0, displayCount)`. `hasMore` = `displayCount < sortedHomework.length`. `loadMore` increments `displayCount` by `pageSize`. Reset `displayCount` to `pageSize` when sort or filter changes. **Clarification (AC-2.9):** The PRD's "page controls at bottom" is implemented as a "Load More" button pattern (not traditional Previous/Next pagination), consistent with AC-2.3. Do NOT implement numbered page navigation.
  - [x] 3.6 **Firestore Index Note:** The following queries may require composite indexes in Firestore. If filtered queries fail at runtime with "The query requires an index" errors, click the link in the error message to auto-create the index in the Firebase console. Known index requirements: (1) `createdBy` + `archived` (for excluding archived by default), (2) `createdBy` + `tags` (for array-contains tag filtering). Alternative: if index creation is blocked, query WITHOUT the filter and apply it client-side (which is already the pattern used in task 3.3).
  - [x] 3.5 Remove the existing `Promise.all(data.map(hw => updateHomeworkStatus(hw.id)...))` call that fires on every fetch — this is N+1 writes and is slow at scale. Move status transitions to the `homeworkAutoTransitionService` (already runs on schedule). This is a **breaking change** — verify no other consumer depends on this side effect.

- [x] 4.0 Build Homework Detail Page (`TeacherHomeworkDetailPage`)
  - [x] 4.1 Create `src/pages/TeacherHomeworkDetailPage.tsx` and `.css`. Use vanilla layout pattern: import `TeacherHeader` from `../components/navigation`, wrap content in a `div` with `maxWidth: 1400px`, `margin: 0 auto`, `padding: 2rem 1rem`. Set page background to the gradient from PRD §7.1: `linear-gradient(135deg, #faf5ff 0%, #f0f9ff 25%, #f0fdfa 50%, #fff7ed 75%, #faf5ff 100%)` with `backgroundAttachment: fixed`. Apply `slideDown` animation on the header area.
  - [x] 4.2 Read `homeworkId` from URL params using `useParams()` from `react-router-dom`. Pass it to `useHomeworkDetail(homeworkId)`. Show `VanillaLoader` while loading. Show error modal (white card with error message + "Back to List" button using `navigateTo('TEACHER_HOMEWORK')`) if error. **Navigation setup:** Add `import { useNavigation } from '../hooks/useNavigation';` at the top of the file, then destructure inside the component: `const { navigateTo } = useNavigation('teacher');`. This is required for ALL `navigateTo()` calls on this page.
  - [x] 4.3 Create `HomeworkBreadcrumb` component at `src/components/homework/HomeworkBreadcrumb.tsx`. Props: `items: Array<{ label: string; onClick?: () => void }>`. Render as `span` elements separated by ` → `. Last item is plain text (current page), others are clickable links (blue, underline on hover). For the detail page, pass: `[{ label: 'Homework', onClick: () => navigateTo('TEACHER_HOMEWORK') }, { label: homework.title || homework.materialTitle }, { label: 'Details' }]`.
  - [x] 4.4 Create `HomeworkSummaryStats` component at `src/components/homework/HomeworkSummaryStats.tsx`. Props: `stats: { totalStudents: number, submittedCount: number, completionPercent: number, averageScore: number, onTimeCount: number, lateCount: number, needsAttentionCount: number }`. Render as a horizontal row of stat cards using `Card` from modern components with `variant="glass"`. Each card: icon (emoji) + label + value. Colors: completion uses green gradient if >75%, amber if 50-75%, red if <50%. Apply `slideUp` animation with staggered delay.
  - [x] 4.5 Create `HomeworkScoreDistribution` component at `src/components/homework/HomeworkScoreDistribution.tsx`. Props: `scores: number[]` (array of percentage scores). Compute 5 buckets: 0-20%, 20-40%, 40-60%, 60-80%, 80-100%. Render as 5 vertical bars using pure CSS — `div` with `height` proportional to count, `background: linear-gradient(to top, #8b5cf6, #a78bfa)`. Below each bar show the range label. Above each bar show the count. Max bar height: 80px. Empty buckets show a thin 2px line. No charting library.
  - [x] 4.6 Create `HomeworkSubmissionTable` component at `src/components/homework/HomeworkSubmissionTable.tsx`. Props: `submissions: HomeworkSubmission[]`, `classRoster: Array<{ uid: string; name: string }>`, `classRosterError: string | null`, `homework: HomeworkAssignment`, `onViewResult: (submission) => void`, `onResetHomework: (submission) => void`. Columns: Student Name, Status (badge using `HomeworkStatusBadge` pattern — "Not Started" grey / "In Progress" blue / "Submitted" green / "Graded" purple), Score (%), Attempt # (e.g., "2 of 3"), Time Spent (format: if `timeSpent` is undefined or 0, show "—"; if < 60: show `Xs`; if < 3600: show `Xm Ys`; if >= 3600: show `Xh Ym`), Submitted At (relative time e.g., "2h ago"), Late Flag (🟢 "On time" or 🔴 "Late"). Compute "not started" students by comparing `classRoster` UIDs against students who have submissions: `const submittedIds = new Set(submissions.map(s => s.studentId)); const notStarted = classRoster.filter(s => !submittedIds.has(s.uid));`. **Edge Case E4 (deleted class):** If `classRosterError` is not null OR `classRoster` is empty while `homework.target.type === 'class'`, show a warning banner above the table: "⚠️ Could not load class roster — 'Not Started' students may not appear. The class may have been deleted." In this case, only show students from the submissions array (no "Not Started" rows). **Edge Case E3 (submitted but left):** Students who have submissions but are NOT in classRoster should still appear in the table — submissions are independent entities. Each row has action buttons: "View Result" (calls `onViewResult`) and "Reset" (calls `onResetHomework`). Apply `slideUp` animation with staggered delay on rows.
  - [x] 4.7 Add an in-table search bar above the submission table. Input with magnifying glass icon. On typing, filter table rows by student name using accent-insensitive matching: `const normalize = (s: string) => s.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase()`. Debounce at 300ms using a `useRef` timer pattern. Show "No students match '[query]'" empty state if no results.
  - [x] 4.8 Wire "View Result" action to open `ResultDetailModal` from `src/components/results/ResultDetailModal.tsx`. Import it and manage open/close state with `useState`. Pass the selected submission's result data. This modal is confirmed Mantine-free — no migration needed.
  - [x] 4.9 Add THCS-specific config info section: if `homework.materialType === 'thcs-test'` AND `homework.thcsConfig` exists, render a collapsible info card at the top showing timer mode, penalty settings, max attempts, feedback timing. Use `Card` from modern components with a "⚙️ Test Configuration" header. Collapsed by default, expand on click.
  - [x] 4.10 Add route in `src/App.jsx`: Find the existing `<Route>` for `/teacher/homework` (the list page route — search for `TEACHER_HOMEWORK` or `TeacherHomeworkListPage`). Insert a new `<Route>` for `/teacher/homework/:homeworkId` AFTER the existing list page route. Wrap in `<PrivateRoute allowedRoles={['teacher']}>`. Lazy-load: add `const TeacherHomeworkDetailPage = lazyWithRetry(() => import('./pages/TeacherHomeworkDetailPage.tsx'));` to the lazy imports section at the top of the file (near the other `lazyWithRetry` calls). The route element is `<TeacherHomeworkDetailPage />`. Also add the `TEACHER_HOMEWORK_STUDENT` route constant to `src/constants/routes.ts` now (Phase 1): `TEACHER_HOMEWORK_STUDENT: '/teacher/homework/student/:studentId'` — add it after the existing `TEACHER_HOMEWORK_EDIT` entry. This is needed early because Phase 3 task 13.2 depends on it being registered BEFORE the `:homeworkId` route.

- [x] 5.0 Rewrite Homework List Page (`TeacherHomeworkListPage`) — Mantine Removal + Redesign
  - [x] 5.1 Back up the current `src/pages/TeacherHomeworkListPage.tsx` to `src/pages/TeacherHomeworkListPage.backup.tsx` (rename, do not delete) before rewriting. This preserves the original for reference during the rewrite.
  - [x] 5.2 Rewrite `TeacherHomeworkListPage.tsx` from scratch. Remove ALL `@mantine/core` imports (`AppShell`, `Tabs`, `Loader`, `Stack`, `Text`, `Center`). Remove `@mantine/notifications` import (`notifications`). Replace with: `Card, CardBody, Button, Input` from `../components/modern`, `VanillaTabs` from `../components/modern/VanillaTabs`, `VanillaLoader` from `../components/modern/VanillaLoader`, `toast` from `../components/modern/ToastNotification`. Use `TeacherHeader` from `../components/navigation`. Wrap in vanilla layout div (see 4.1 for exact styles).
  - [x] 5.3 Wire `useHomeworkList` hook with the new options: `{ teacherId: user?.uid, autoRefresh: true, excludeArchived: true, excludeClosed: true, pageSize: 25 }`. Destructure: `{ filteredHomework, loading, error, refetch, statusCounts, loadMore, hasMore, sort, setSort, tagFilter, setTagFilter }`.
  - [x] 5.4 Add `HomeworkSummaryStats` bar at the top of the page showing: total count, active/scheduled/past_due/closed breakdown (from `statusCounts`), quick action buttons: "➕ Create" (opens create modal) and "Close All Past Due" (calls `closeAllPastDueHomework(user.uid)` from `homeworkBulkOperations.ts` — import the function, NOT duplicate it). Show a confirmation dialog before "Close All Past Due" using `window.confirm()` for now.
  - [x] 5.5 Add search bar using `Input` from modern components. On typing, filter the loaded homework client-side by: `homework.title`, `homework.materialTitle`, and target display name. To get the target display name: if `homework.target.type === 'class'`, use `homework.target.className ?? 'Unknown Class'` (the `className` field is denormalized on the target object — see `HomeworkTargetClass` type in `homework.types.ts`); if `homework.target.type === 'course'`, use `homework.target.courseName`; if `homework.target.type === 'group'`, use `homework.target.groupName`; if `homework.target.type === 'students'`, use `homework.target.studentNames?.join(', ')`. **IMPORTANT:** The field is `homework.target.classId` (singular), NOT `classIds` (plural). The target is a union type — check `homework.target.type` before accessing type-specific fields. Use accent-insensitive matching (same `normalize` function as 4.7). Debounce at 300ms.
  - [x] 5.6 Add sort dropdown using `NativeSelect` from modern components. Options: `[{ value: 'dueDate_desc', label: 'Due date (newest)' }, { value: 'dueDate_asc', label: 'Due date (oldest)' }, { value: 'createdAt_desc', label: 'Created date' }, { value: 'updatedAt_desc', label: 'Last updated (newest first)' }, { value: 'completionRate_desc', label: 'Completion rate (highest)' }, { value: 'title_asc', label: 'Title A-Z' }]`. On change, call `setSort(value)`.
  - [x] 5.7 Add status filter buttons row: "All", "Active", "Scheduled", "Past Due", "Draft", "Closed". Each button shows count from `statusCounts`. Use `Button` from modern components with `variant="glass"` for inactive, `variant="primary"` for active filter. "Closed" filter toggles `excludeClosed` off and refetches.
  - [x] 5.8 Add view mode toggle: "Timeline" (chronological), "By Class", "By Status". Use `VanillaTabs` component. Default to "Timeline". "By Class" groups homework by target type: for `target.type === 'class'`, group by `target.classId` with `target.className` as the header; for `target.type === 'course'`, group by `target.courseId` with `target.courseName` as header; for `target.type === 'students'` or `target.type === 'group'`, group under header "Individual / Group Assignments". **IMPORTANT:** The field is `target.classId` (singular), NOT `classIds`. The target is a union type — always check `target.type` before accessing type-specific fields. "By Status" groups by `homework.status` with status headers.
  - [x] 5.9 Render homework cards using existing `HomeworkCard` component. Each card should be clickable — on click, navigate to `/teacher/homework/${hw.id}` using `navigateTo('TEACHER_HOMEWORK_DETAIL', { homeworkId: hw.id })`. Verify this route exists in `constants/routes.ts`. **Navigation setup:** Add `import { useNavigation } from '../hooks/useNavigation';` at the top of the file, then destructure inside the component: `const { navigateTo } = useNavigation('teacher');`. This is required for ALL `navigateTo()` calls on this page. Cards should have `slideUp` animation with staggered `0.05s * index` delay.
  - [x] 5.10 Add inline alert badges on homework cards (AC-2.10): compute badge text from homework timing. If `status === 'scheduled'` and becomes active within 24h: show "⚡ Goes live in [Xh]". If `status === 'active'` and deadline within 24h: show "🔥 Deadline in [Xh]". If `status === 'past_due'`: show "⚠️ Overdue [Xd]". Render as a small `<span>` badge on the top-right of the card. Use the existing `HomeworkStatusBadge` CSS pattern for styling.
  - [x] 5.11 Add `HomeworkAlertBanner` component at top of page (below stats bar). Show if there are homework going live within 24h OR past-due homework. Two columns: "Going live soon" (list) and "Past deadline" (list). Quick action buttons: "[View All]" scrolls to the relevant homework card, "[Close All Past Due]" triggers bulk close. Dismissible per session using `sessionStorage.setItem('homework_alert_dismissed', 'true')`.
  - [x] 5.12 Add "Load More" button at the bottom of the homework list if `hasMore` is true. Style: `Button` with `variant="glass"`, full width, text "Load more ({remaining} remaining)". On click, call `loadMore()`.
  - [x] 5.13 Add "Show Archived" toggle button in the filter area. When toggled on, set `excludeArchived: false` in the hook options and refetch. Archived homework renders with dimmed opacity (0.6) and an "Archived" badge.
  - [x] 5.14 Mobile responsive (AC-2.13): at `max-width: 768px`, stack cards vertically, hide score/attempt columns in submission tables. Homework cards show only title + status + due date. CSS media query in the page's CSS file.
  - [x] 5.15 Empty states: if no homework at all, show a centered card with neutral text: "No homework assigned yet. Create your first assignment!" with a "➕ Create Homework" button. If filter returns no results: "No homework matches your current filters." Use `scaleIn` animation.
  - [x] 5.16 Error handling: if `error` is set, show a modal overlay with "Failed to load homework" message and a "Retry" button that calls `refetch()`. For action errors (delete, duplicate, extend), use `toast.error(message)` instead of Mantine `notifications.show()`.
  - [x] 5.17 Replace all `notifications.show(...)` calls with `toast.success(message)` or `toast.error(message)`. There are approximately 8-10 notification calls in the current file — find them all by searching for `notifications.show` and replace each one.
  - [x] 5.18 Preserve existing functionality: create modal (`HomeworkCreateModal`), edit modal (`HomeworkEditModal`), THCS assign dialog (`THCSHomeworkAssignDialog`). These are already imported in the current file — keep them. They are confirmed Mantine-free.

- [x] 6.0 Mantine Audit & Cleanup Across All Homework Components
  - [x] 6.1 Verify `HomeworkCard.tsx` has zero `@mantine/*` imports. Open the file, search for `@mantine`. If found, list every Mantine component used and replace each with vanilla HTML/CSS or modern component equivalents. If clean, mark as verified.
  - [x] 6.2 Verify `HomeworkCreateModal.tsx` has zero `@mantine/*` imports. Search for `@mantine`. If clean, mark as verified. Do NOT modify the component.
  - [x] 6.3 Verify `HomeworkEditModal.tsx` has zero `@mantine/*` imports. Search for `@mantine`. If clean, mark as verified. Do NOT modify the component.
  - [x] 6.4 Verify `HomeworkConfigPanel.tsx` has zero `@mantine/*` imports. Search for `@mantine`. If clean, mark as verified. Do NOT modify the component.
  - [x] 6.5 Verify `HomeworkStatusBadge.tsx` has zero `@mantine/*` imports. Already confirmed Mantine-free (uses vanilla CSS + `./HomeworkStatusBadge.css`). Mark as verified.
  - [x] 6.6 Migrate `UpcomingHomeworkWidget.tsx` — currently imports `Paper, Text, Group, Stack, Badge, Button, Loader, ThemeIcon` from `@mantine/core`. Replace: `Paper` → `Card` (modern), `Text` → `<span>`/`<p>` with inline styles, `Group` → `<div style={{ display: 'flex', ... }}>`, `Stack` → `<div style={{ display: 'flex', flexDirection: 'column', ... }}>`, `Badge` → `<span className="badge">` with CSS, `Button` → `Button` (modern), `Loader` → `VanillaLoader` (modern), `ThemeIcon` → `<div>` with icon inside styled div. Remove all `@mantine/core` imports. Test that the widget still renders correctly on the student dashboard.
  - [x] 6.7 Delete `HomeworkResultsSummary.tsx` — this component is fully replaced by the new `HomeworkSubmissionTable` on the detail page. Before deleting: (1) Search entire codebase for imports of `HomeworkResultsSummary` using grep. (2) If any file imports it, remove that import and usage first. (3) Remove the export from `src/components/homework/index.ts`. (4) Delete the file.
  - [x] 6.8 Update `src/components/homework/index.ts` barrel export: remove `HomeworkResultsSummary` export, add exports for all new Phase 1 components (`HomeworkSummaryStats`, `HomeworkSubmissionTable`, `HomeworkAlertBanner`, `HomeworkScoreDistribution`, `HomeworkBreadcrumb`).
  - [x] 6.9 Final verification: run `grep -r "@mantine" src/components/homework/ src/pages/TeacherHomework*` and verify zero results. If any remain, fix them. This is the success criterion for AC-3.1 through AC-3.9.

### Phase 2: Power Tools

- [x] 7.0 Modify Core Services — Archive, Soft Delete, `closedAt`, Student Overrides
  - [x] 7.1 Open `src/types/homework.types.ts`. Add the following optional fields to the `HomeworkAssignment` interface: `tags?: string[]`, `archived?: boolean`, `archivedAt?: number`, `trashExpiresAt?: number`, `closedAt?: number`, `studentOverrides?: { [studentId: string]: { dueDate?: number; exempted?: boolean; exemptReason?: string; notes?: string; reminderCount?: number; lastRemindedAt?: number; } }`. Add them after the existing fields with clear comments: `// PRD-0034: Phase 2 additions`. Do NOT remove or modify any existing fields. Additionally, add the `HomeworkTagConfig` interface (used by `useHomeworkTags` in task 10.1) at the bottom of the file in a new section: `// PRD-0034: Tag Configuration` — `export interface HomeworkTagConfig { tags: Array<{ id: string; label: string; color?: string }>; updatedAt: number; updatedBy: string; }`. Also add a named type for student overrides for reuse: `export type StudentOverride = { dueDate?: number; exempted?: boolean; exemptReason?: string; notes?: string; reminderCount?: number; lastRemindedAt?: number; };` — then reference it in the `studentOverrides` field above: `studentOverrides?: { [studentId: string]: StudentOverride }`.
  - [x] 7.2 Open `src/services/homeworkManager.ts`. Add function `archiveHomework(homeworkId: string): Promise<void>` — calls `updateDoc(doc(db, HOMEWORK_COLLECTION, homeworkId), { archived: true, archivedAt: Date.now(), trashExpiresAt: Date.now() + 30 * 24 * 60 * 60 * 1000 })`. Add restore guard check at the top: `if (await isRestoreInProgress()) throw new Error('Restore in progress')`. **CRITICAL:** `isRestoreInProgress()` returns `Promise<boolean>` — you MUST use `await`. Without `await`, the condition always evaluates to `true` (a Promise object is truthy) and every call will throw. The function must be declared `async`. Apply this same `await` pattern to ALL new functions that call `isRestoreInProgress()` (tasks 7.3, 7.4).
  - [x] 7.3 Add function `restoreHomework(homeworkId: string): Promise<void>` — first read the document with `getDoc` to check `trashExpiresAt`. If `trashExpiresAt < Date.now()`, throw error "This homework has been permanently deleted." (edge case E12). Otherwise call `updateDoc` to set `{ archived: false, archivedAt: deleteField(), trashExpiresAt: deleteField(), status: 'draft' }`. Import `deleteField` from `firebase/firestore`. Status is set to `draft` so teacher must review before re-activating (deadline may have passed).
  - [x] 7.4 Add function `permanentlyDeleteHomework(homeworkId: string): Promise<void>` — calls `deleteDoc(doc(db, HOMEWORK_COLLECTION, homeworkId))`. Add restore guard check. NOTE: This does NOT delete submissions or test results (submission independence principle, AC-5.2, AC-5.5).
  - [x] 7.5 Modify existing `deleteHomework(homeworkId: string)` function — change it to call `archiveHomework(homeworkId)` instead of `deleteDoc(...)`. This is the soft-delete behavior (AC-5.1). Add a comment: `// PRD-0034: Soft delete — archives instead of permanent delete`.
  - [x] 7.6 Modify existing `closeHomework(homeworkId: string)` function — add `closedAt: Date.now()` to the `updateDoc` call alongside the existing `status: 'closed'` update. This timestamp is needed for the auto-archive 30-day check (AC-5.7).
  - [x] 7.7 Add function `updateStudentOverride(homeworkId: string, studentId: string, override: Partial<StudentOverride>): Promise<void>`. **CRITICAL: Do NOT pass `undefined` values to `updateDoc` — Firestore will throw "Unsupported field value: undefined".** Build the update object dynamically, only including defined fields: ```const updates: Record<string, any> = {}; if (override.dueDate !== undefined) updates[`studentOverrides.${studentId}.dueDate`] = override.dueDate; if (override.exempted !== undefined) updates[`studentOverrides.${studentId}.exempted`] = override.exempted; if (override.exemptReason !== undefined) updates[`studentOverrides.${studentId}.exemptReason`] = override.exemptReason; if (override.notes !== undefined) updates[`studentOverrides.${studentId}.notes`] = override.notes; if (override.reminderCount !== undefined) updates[`studentOverrides.${studentId}.reminderCount`] = override.reminderCount; if (override.lastRemindedAt !== undefined) updates[`studentOverrides.${studentId}.lastRemindedAt`] = override.lastRemindedAt; if (Object.keys(updates).length > 0) { const homeworkRef = doc(db, HOMEWORK_COLLECTION, homeworkId); await updateDoc(homeworkRef, updates); }``` This pattern prevents overwriting other fields in the override object (AC-7.6). To DELETE a field (e.g., clearing an override), pass `deleteField()` from `firebase/firestore` as the value — this is a valid Firestore sentinel value.
  - [x] 7.8 Open `src/services/homeworkAutoTransitionService.ts`. Find the `getHomeworkNeedingAttention()` function. Change the time window from 1 hour (`60 * 60 * 1000`) to 24 hours (`24 * 60 * 60 * 1000`) to match PRD AC-10.1. This makes the alert banner show homework that will go live within 24h and those past-due within 24h.
  - [x] 7.9 In the same file, add auto-archive logic to the `checkAndTransitionHomework()` (or equivalent transition function): after existing transition checks, add: for any homework with `status === 'closed'`, check if `(homework.closedAt ?? homework.updatedAt) + (30 * 24 * 60 * 60 * 1000) < Date.now()`. If true, call `archiveHomework(homework.id)`. The `closedAt` fallback to `updatedAt` handles legacy data that doesn't have `closedAt` yet.
  - [x] 7.10 Open `src/services/homeworkSubmissionService.ts`. Find `isLateSubmission()` function. Modify it to check per-student overrides first: `const studentDueDate = assignment.studentOverrides?.[studentId]?.dueDate ?? assignment.scheduling.dueDate`. Compare submission time against `studentDueDate` instead of `assignment.scheduling.dueDate`.
  - [x] 7.11 In the same file, add helper function `isStudentExempted(assignment: HomeworkAssignment, studentId: string): boolean`. Implementation: `return assignment.studentOverrides?.[studentId]?.exempted ?? false`.

- [x] 8.0 Build Bulk Operations UI (Checkboxes + Floating Action Bar + Modals)
  - [x] 8.1 Create `src/hooks/useBulkSelection.ts`. Generic hook with `<T extends string>` generic. State: `selected: Set<T>`. Return: `{ selected: Set<T>, selectedCount: number, toggle: (id: T) => void, selectAll: (ids: T[]) => void, deselectAll: () => void, isSelected: (id: T) => boolean }`. `toggle` adds if not present, removes if present. `selectAll` replaces the set. `deselectAll` clears the set.
  - [x] 8.2 Add a checkbox to each `HomeworkCard` on the list page. When bulk mode is active (at least 1 item selected), all cards show checkboxes. Render as `<input type="checkbox" checked={isSelected(hw.id)} onChange={() => toggle(hw.id)} />` positioned at the top-left of the card. Style: `position: absolute`, `top: 8px`, `left: 8px`, with a semi-transparent background circle.
  - [x] 8.3 Create `HomeworkBulkActionBar` component at `src/components/homework/HomeworkBulkActionBar.tsx`. Props: `selectedCount: number`, `onExtend: () => void`, `onClose: () => void`, `onDelete: () => void`, `onDuplicate: () => void`, `onDeselectAll: () => void`, `onCloseAllPastDue: () => void`. Render as a fixed-bottom bar (`position: fixed; bottom: 0; left: 0; right: 0; z-index: 100`) with glassmorphism background (`backdrop-filter: blur(12px); background: rgba(255,255,255,0.9)`). Show: "{N} selected" label, action buttons (Extend, Close, Delete, Duplicate), "Deselect All" button. Add `slideUp` animation on mount. On mobile (`max-width: 768px`), make full-width with stacked buttons.
  - [x] 8.4 Add "Select all matching filter" button: when status filter is active (e.g., "Past Due"), show a banner above the card list: "Select all {count} {status} homework?" with a button. On click, call `selectAll(matchingIds)` using the `selectHomeworkForBulkOperation()` function from `homeworkBulkOperations.ts`. DO NOT reimplement this function — import and call the existing one.
  - [x] 8.5 Create `BulkExtendModal` component at `src/components/homework/BulkExtendModal.tsx`. Props: `isOpen: boolean`, `selectedCount: number`, `onClose: () => void`, `onConfirm: (params: { mode: 'absolute' | 'relative', absoluteDate?: number, relativeHours?: number }) => void`. Two modes: (A) "Set new deadline" — date picker input (`<input type="datetime-local">`), (B) "Extend by" — preset buttons (24h, 3 days=72h, 1 week=168h) + custom input. The existing `bulkExtendDeadlines()` service uses `extendByHours`, so relative mode passes hours directly. For absolute mode, compute hours from `(absoluteDate - Date.now()) / (3600 * 1000)`. Use vanilla modal pattern (fixed overlay + centered card, see `AdminSettingsPage.tsx` `AddKeyModal` for reference).
  - [x] 8.6 Create `BulkDeleteConfirmModal` at `src/components/homework/BulkDeleteConfirmModal.tsx`. Props: `isOpen: boolean`, `selectedCount: number`, `onClose: () => void`, `onConfirm: () => void`. Show warning text: "This will archive {count} homework assignments. They can be restored within 30 days." Below, show a text input with label "Type DELETE to confirm". The confirm button is disabled until input value equals "DELETE" (case-sensitive). On confirm, call `onConfirm`. Red warning styling.
  - [x] 8.7 Wire bulk actions in `TeacherHomeworkListPage`: (1) Extend → open `BulkExtendModal` → on confirm call `bulkExtendDeadlines({ teacherId: user.uid, homeworkIds: [...selected], extendByHours })` from `homeworkBulkOperations.ts`. (2) Close → call `bulkCloseHomework({ teacherId: user.uid, homeworkIds: [...selected] })`. (3) Delete → open `BulkDeleteConfirmModal` → on confirm, loop selected IDs and call `archiveHomework(id)` for each. (4) After each bulk action, call `deselectAll()` and `refetch()`. Handle partial failures: wrap each operation in try/catch, count successes and failures, show toast: `toast.success("Extended {successCount} of {totalCount} deadlines")` or `toast.error("Extended {successCount} of {totalCount}. {failCount} failed.")`.
  - [x] 8.8 Add "Close All Past Due" quick action: wire the button in `HomeworkSummaryStats` and `HomeworkAlertBanner` to call `closeAllPastDueHomework(user.uid)` from `homeworkBulkOperations.ts`. Show confirmation first: `window.confirm('Close all past due homework?')`. On success, `toast.success('All past due homework closed')` and `refetch()`.

- [x] 9.0 Build Archive & Trash System (UI + Auto-Purge Logic)
  - [x] 9.1 Add "Show Archived" filter toggle to the list page (already scaffolded in 5.13). When toggled, archived homework loads and displays with visual indicators: dimmed opacity (0.6), strikethrough on title, and an "🗄️ Archived" badge.
  - [x] 9.2 Add actions on archived homework cards: "Restore" button (calls `restoreHomework(hw.id)`, then `refetch()`; handle the "already purged" error with `toast.error('This homework has been permanently deleted.')`) and "Permanently Delete" button (opens a confirmation modal with "DELETE" typing, then calls `permanentlyDeleteHomework(hw.id)`; show `toast.success` on success).
  - [x] 9.3 Implement client-side lazy auto-purge (AC-5.6): in the `useHomeworkList` hook, after loading data, check each homework: if `hw.archived === true && hw.trashExpiresAt && hw.trashExpiresAt < Date.now()`, call `permanentlyDeleteHomework(hw.id)` in background (fire-and-forget, log errors to console). Do NOT block the UI. Filter these auto-purged items out of the displayed list immediately. **Safety guards:** (1) Only purge if `hw.createdBy === userId` — the current teacher must own the homework (prevents race conditions if two teachers share a class). (2) Wrap each `permanentlyDeleteHomework` call in `try/catch` and log errors: `console.warn('[AutoPurge] Failed to purge', hw.id, err)` — do NOT re-throw. (3) Add a comment: `// PRD-0034: Auto-purge is intentionally a side-effect (AC-5.6). Only the creator triggers it.`

- [x] 10.0 Build Tags & Labels System (Data + Admin UI + Filter Chips)
  - [x] 10.1 Create `src/hooks/useHomeworkTags.ts`. Reads from Firestore document `app_config/homework_tags`. Returns `{ tags: Array<{ id: string, label: string, color?: string }>, loading: boolean }`. On first access, if document doesn't exist, create it with the 7 default tags from PRD AC-6.2: `practice/Luyện tập`, `midterm/Giữa kỳ`, `final/Cuối kỳ`, `revision/Ôn tập`, `extra/Bổ sung`, `homework/Bài tập về nhà`, `test-prep/Ôn thi`. Default colors: practice=blue `#3b82f6`, midterm=purple `#8b5cf6`, final=red `#ef4444`, revision=green `#10b981`, extra=amber `#f59e0b`, homework=slate `#64748b`, test-prep=indigo `#6366f1`. Use `setDoc` with `{ merge: true }` for creation.
  - [x] 10.2 Create `HomeworkTagChips` component at `src/components/homework/HomeworkTagChips.tsx`. Two modes: (A) **Display mode** — Props: `tags: string[]`, `allTags: TagConfig[]`. Renders colored chips for each tag on the homework card. (B) **Filter mode** — Props: `selectedTag: string | null`, `allTags: TagConfig[]`, `onTagSelect: (tag: string | null) => void`. Renders clickable filter chips on the list page. Active chip has filled background, inactive has outline. "All" chip to clear filter.
  - [x] 10.3 Add tag filter to the list page: render `HomeworkTagChips` in filter mode below the status filter buttons. On tag select, call `setTagFilter(tag)`.
  - [x] 10.4 Add tag display on homework cards: render `HomeworkTagChips` in display mode on each `HomeworkCard`, showing the homework's `tags` array.
  - [x] 10.5 Add tag selector to `HomeworkCreateModal` and `HomeworkEditModal`: add a multi-select section labeled "Tags (optional)" showing all available tags as checkable chips. Selected tags are stored in the form state as `string[]` and passed to the create/update service call. Use `useHomeworkTags()` to load available tags.
  - [x] 10.6 Create `AdminTagManager` component at `src/components/admin/AdminTagManager.tsx`. Renders a list of current tags with: each tag shown as a colored chip with a delete (✕) button, an "Add Tag" form (name input + color picker + label input), save/delete operations update the `app_config/homework_tags` Firestore document. Use `Card` from modern components. No `@mantine/*` imports.
  - [x] 10.7 Integrate `AdminTagManager` into the admin page: add a "Tags" tab/sidebar item to `AdminSettingsPage.tsx` (or a new admin page if admin sidebar supports it — check the `AdminLayout` component's `onNavigate` handler and `handleSidebarNavigate` for the pattern). Wire it so clicking "Tags" in the admin sidebar renders the `AdminTagManager`.

- [x] 11.0 Build Per-Student Actions on Detail Page (Extend, Exempt, Notes, Reminders)
  - [x] 11.1 Create `StudentActionMenu` component at `src/components/homework/StudentActionMenu.tsx`. Props: `student: Student`, `submission: HomeworkSubmission | null`, `homework: HomeworkAssignment`, `onExtendDeadline: () => void`, `onExempt: () => void`, `onAddNote: () => void`, `onSendReminder: () => void`. Render as a `<button>` with "⋮" icon that toggles a dropdown menu on click. Menu items: "📌 Extend Deadline", "🎓 Exempt Student", "📝 Add Note", "⚡ Send Reminder". Disable "Send Reminder" if: (A) student already submitted (tooltip: "Already submitted"), (B) `reminderCount >= 3` (tooltip: "Maximum reminders sent (3/3)"), (C) `Date.now() - lastRemindedAt < 24 * 60 * 60 * 1000` (tooltip: "Reminder sent recently. Try again in [Xh]"). Close menu on click outside or on action selection.
  - [x] 11.2 Create `ExtendStudentDeadlineModal` at `src/components/homework/ExtendStudentDeadlineModal.tsx`. Props: `isOpen`, `studentName`, `currentDeadline: number`, `onClose`, `onConfirm: (newDeadline: number) => void`. Show student name, current deadline formatted, date-time picker (`<input type="datetime-local">`), and confirm/cancel buttons. Validate: new deadline must be in the future. On confirm, parent calls `updateStudentOverride(homeworkId, studentId, { dueDate: newTimestamp })`.
  - [x] 11.3 Create `ExemptStudentModal` at `src/components/homework/ExemptStudentModal.tsx`. Props: `isOpen`, `studentName`, `onClose`, `onConfirm: (reason: string) => void`. Show student name, optional reason text input (`Textarea` from modern components), explanation text: "Exempted students are excluded from completion rate calculations." On confirm, parent calls `updateStudentOverride(homeworkId, studentId, { exempted: true, exemptReason: reason })`.
  - [x] 11.4 Create `StudentNoteEditor` at `src/components/homework/StudentNoteEditor.tsx`. Props: `studentId`, `homeworkId`, `currentNote: string`, `onSave: (note: string) => void`. Render as: if no note, show a small "📝" icon button. On click, expand to show a `Textarea` (from modern components) with the current note, plus Save and Cancel buttons. On save, parent calls `updateStudentOverride(homeworkId, studentId, { notes: noteText })`. Saved notes display as a small "📝" icon on the row that shows the note on hover (CSS tooltip: `::after` pseudo-element with `content: attr(data-note)`).
  - [x] 11.5 Wire reminder functionality: on "Send Reminder" click, (1) check `reminderCount < 3` and `Date.now() - lastRemindedAt >= 24h` (both checks already done by button disable in 11.1, but double-check in handler). (2) Call `updateStudentOverride(homeworkId, studentId, { reminderCount: (current + 1), lastRemindedAt: Date.now() })`. (3) Show `toast.success('Reminder sent to [studentName]')`. Note: the actual notification creation (`notificationService.create()`) is deferred to Phase 4 task 16.0. For now, just update the counter and show the toast.
  - [x] 11.6 Handle edge case E5 (AC-7.7): when teacher extends the GLOBAL deadline (via homework edit), check all `studentOverrides`. For each student where `override.dueDate` exists and `override.dueDate <= newGlobalDeadline`, clear the override. **Exact location:** Add a new exported function `clearSubsumedOverrides(homeworkId: string, newGlobalDeadline: number): Promise<void>` in `src/services/homeworkManager.ts`. Implementation: (1) Read the homework document with `getDoc`. (2) Iterate `Object.entries(homework.studentOverrides ?? {})`. (3) For each entry where `override.dueDate !== undefined && override.dueDate <= newGlobalDeadline`, build a batch update using Firestore dot-notation: `updates['studentOverrides.' + studentId + '.dueDate'] = deleteField()`. (4) If any updates exist, call `updateDoc(homeworkRef, updates)`. Then, in `HomeworkEditModal.tsx`, find the save/submit handler. AFTER the `updateHomework()` call succeeds, check if `scheduling.dueDate` was changed. If yes, call `clearSubsumedOverrides(homeworkId, newDueDate)`. Import `clearSubsumedOverrides` from `../services/homeworkManager` and `deleteField` from `firebase/firestore`.
  - [x] 11.7 Handle edge case E6 (AC-7.3): when exempting a student who already submitted, the exemption still applies. Status shows as "Submitted (Exempted)". In the submission table, check both `submission.status` and `isStudentExempted()`. If both are true, show combined status badge.
  - [x] 11.8 Show exempted students in a separate group at the bottom of the submission table with a grey background and "Exempted" header. Exclude them from the completion rate calculation: `completionRate = submitted / (totalStudents - exemptedCount) * 100`.
  - [x] 11.9 Add "📌 Extended to [date]" visual indicator on student rows that have `studentOverrides[studentId].dueDate` set. Show as a small purple badge next to the due date column.

### Phase 3: Intelligence

- [x] 12.0 Build Class Analytics in "By Class" View Mode
  - [x] 12.1 Create `ClassAnalyticsHeader` component at `src/components/homework/ClassAnalyticsHeader.tsx`. Props: `classId: string`, `className: string`, `homework: HomeworkAssignment[]`, `submissions: HomeworkSubmission[]`. Compute and display: total homework for this class, average completion rate, average score, overdue count, at-risk student count (<50% completion). Render as a horizontal stat row within the class group header in the "By Class" view. Use `Card` with `variant="glass"`. All computations are client-side from loaded data (AC-8.4).
  - [x] 12.2 Create `AtRiskStudentList` component at `src/components/homework/AtRiskStudentList.tsx`. Props: `students: Array<{ name: string, completionRate: number, avgScore: number }>`. Render as a collapsible section (collapsed by default). Header: "⚠️ At-Risk Students ({count})" with chevron toggle. Body: list of student cards showing name, completion rate (red if <50%), and average score. "At-risk" threshold: average submission completion rate < 50% across all homework for that class (AC-8.3).
  - [x] 12.3 Wire analytics into the "By Class" view mode on the list page. For each class group: (1) render `ClassAnalyticsHeader` above the homework cards, (2) compute at-risk students from the loaded submission data, (3) render `AtRiskStudentList` below the header if any at-risk students exist. For list page metrics, use stored `stats.submitted / stats.totalAssigned * 100` (approximate, fast). For detail page, use live roster count (accurate).

- [x] 13.0 Build Student Homework Profile Page
  - [x] 13.1 Verify route constant `TEACHER_HOMEWORK_STUDENT` exists in `src/constants/routes.ts` (should have been added in Phase 1 task 4.10). If missing, add it now: `TEACHER_HOMEWORK_STUDENT: '/teacher/homework/student/:studentId'` — place it after `TEACHER_HOMEWORK_EDIT`.
  - [x] 13.2 Add route to `src/App.jsx`. **CRITICAL: Route ordering matters.** The new route `/teacher/homework/student/:studentId` MUST be registered BEFORE `/teacher/homework/:homeworkId`. Otherwise `student` will be matched as a `:homeworkId` parameter. Insert the new `<Route>` above the detail page route. Add lazy import: `const StudentHomeworkProfile = lazyWithRetry(() => import('./pages/StudentHomeworkProfile.tsx'))`. Wrap in `<PrivateRoute allowedRoles={['teacher']}>`.
  - [x] 13.3 Create `src/hooks/useStudentHomework.ts`. Input: `studentId: string`. Query `homework_submissions` with `where('studentId', '==', studentId)`. Group results client-side by `homeworkId`. For each group, lazy-load the homework title from a local cache (avoid N+1 queries — batch-load homework docs for all unique `homeworkId` values using `Promise.all` with `getDoc`). Return: `{ groups: Array<{ homework: HomeworkAssignment, submissions: HomeworkSubmission[] }>, loading, error, loadMore, hasMore }`. Paginate at 25 submissions, show "Load More" if student has 50+ (AC-9.7).
  - [x] 13.4 Create `src/pages/StudentHomeworkProfile.tsx` and `.css`. Read `studentId` from `useParams()`. Use `useStudentHomework(studentId)`. Layout: vanilla layout pattern (same as detail page). Summary header: student name, overall completion rate, average score, late submission count, total homework assigned. Below: list of homework cards, each showing: homework title, status, best/latest score (%), attempt count, submitted date. Click a row to expand and show all attempts for that homework (lazy expand, AC-9.5). Navigation: accessible from homework detail page (clicking student name links here) and from class management page.
  - [x] 13.5 Wire student name clicks in `HomeworkSubmissionTable` (from task 4.6) to navigate to the student profile: `navigateTo('TEACHER_HOMEWORK_STUDENT', { studentId: student.uid })`. Use a `<span>` with click handler and blue text + underline cursor styling. Also verify the class management page has an entry point (AC-9.2) — if not, add a link in the students tab.

- [x] 14.0 Wire Alert System (Badges + Banner + Auto-Refresh) — implemented inline from loaded data (more efficient than separate service call)
  - [x] 14.1 Wire `getHomeworkNeedingAttention()` from `homeworkAutoTransitionService.ts` into the list page. Call it on page load with the teacher's UID. Use the returned data to populate: (1) `HomeworkAlertBanner` (already created in 5.11), (2) inline alert badges on cards (already created in 5.10). The function already exists — just import and call it. It returns `{ goingLiveSoon: HomeworkAssignment[], pastDue: HomeworkAssignment[] }`.
  - [x] 14.2 Auto-refresh: when `useHomeworkList` refetches data (via `autoRefresh`), also re-call `getHomeworkNeedingAttention()` to update badge and banner data. Store the alert data in component state and pass it down to the banner and card components.
  - [x] 14.3 Badge rendering on cards: for each homework card, check if the homework ID appears in the alert data and render the appropriate badge. The badge logic (which emoji/text to show) is already implemented in task 5.10 — just ensure the data flows correctly from the alert service to the card props.

### Phase 4: Polish

- [x] 15.0 Build Template Save Flow Improvement (TemplateSaveModal)
  - [x] 15.1 Create `TemplateSaveModal` at `src/components/homework/TemplateSaveModal.tsx`. Props: `isOpen`, `onClose`, `onSave: (name: string, description: string) => Promise<void>`, `existingTemplateNames: string[]`. Fields: template name (required, `Input` from modern), description (optional, `Textarea` from modern). Validation: name must not be empty, must be unique (check against `existingTemplateNames` — show inline error "A template with this name already exists"). On save: call `onSave`, show `toast.success("Template '[name]' saved successfully")`, close modal. Loading state on save button.
  - [x] 15.2 Find `window.prompt()` usage in `HomeworkCreateModal.tsx` (used for template name input). Replace: remove the `window.prompt()` call, instead open `TemplateSaveModal`. Pass existing template names from `getTemplatesByTeacher()` (from `homeworkTemplateService.ts`). Wire `onSave` to call the existing template save service.

- [x] 16.0 Build Reminder Notification Infrastructure
  - [x] 16.1 Wire the reminder "Send" action (from task 11.5) to also create an in-app notification via `notificationService`. The existing `createNotification` function is located in `src/services/notificationService.ts`. It accepts a `NotificationCreate` object (from `src/types/notification.types.ts`). **Exact code to add in the reminder handler** (in `TeacherHomeworkDetailPage` or wherever the remind action is handled): ```import { createNotification } from '../services/notificationService'; // Inside the remind handler, after updateStudentOverride succeeds: try { await createNotification({ userId: studentId, type: 'info', title: '📝 Homework Reminder', message: `Your teacher sent a reminder for "${homework.title || homework.materialTitle}". Don't forget to submit!`, link: `/student/homework/${homework.id}`, metadata: { homeworkId: homework.id, homeworkTitle: homework.title || homework.materialTitle, reminderType: 'teacher_reminder' } }); } catch (err) { console.warn('[Reminder] Notification failed (non-blocking):', err); }``` **Note:** The notification MUST be non-blocking — wrap in try/catch so a failed notification doesn't break the reminder flow. The `type` must be `'info'` (not `'homework_reminder'` — the existing notification type enum only supports: `'info'`, `'success'`, `'warning'`, `'feedback'`). Use `'info'` for reminders.
  - [x] 16.2 Track reminder data: `lastRemindedAt` is stored in `studentOverrides[studentId].lastRemindedAt` (already implemented in 11.5). The visual flag check: on the student's homework list, show "⚡ Teacher sent a reminder" badge on the homework card if `lastRemindedAt` is within the last 48 hours (`Date.now() - lastRemindedAt < 48 * 60 * 60 * 1000`). This requires adding a read of `studentOverrides` in the student-facing homework list (out of scope for this PRD — note as a follow-up).
  - [x] 16.3 Implement "Remind All" bulk action: on the homework detail page, add a "Remind All" button in the summary stats area. On click: find all students who (A) haven't submitted, (B) haven't reached 3-reminder limit, (C) aren't in 24h cooldown. For each qualifying student, send reminder (update override + create notification). Show toast: "Reminders sent to {count} students. {skippedCount} skipped (already submitted, limit reached, or cooldown)." Handle edge case E14: don't send to already-submitted students.
  - [x] 16.4 Edge case E15: once `reminderCount >= 3`, the "Send Reminder" button is permanently disabled for that student for that homework. Tooltip: "Maximum reminders sent (3/3)". This is already handled in the `StudentActionMenu` disable logic (task 11.1).

- [x] 17.0 Mobile Polish — Detail Page Card Layout, Full-Screen Modals, Three-Dot Menu
  - [x] 17.1 Detail page mobile: at `max-width: 768px`, the `HomeworkSubmissionTable` switches from a `<table>` layout to a card-based layout. Each student renders as a compact card showing: name, status badge, score, late flag. Use CSS media query to hide `<table>` and show `.mobile-cards` container. Cards should stack vertically with 0.5rem gap.
  - [x] 17.2 Mobile per-student actions: the `StudentActionMenu` three-dot menu (from 11.1) should be positioned at the top-right of each student card on mobile. The dropdown menu should open as a bottom sheet on mobile (fixed to bottom of screen, full-width, with backdrop). CSS: `@media (max-width: 768px) { .action-menu-dropdown { position: fixed; bottom: 0; left: 0; right: 0; } }`.
  - [x] 17.3 Full-screen modals on mobile: all modals (`ResultDetailModal`, `BulkExtendModal`, `BulkDeleteConfirmModal`, `ExtendStudentDeadlineModal`, `ExemptStudentModal`, `TemplateSaveModal`) should be full-screen on mobile. Add CSS: `@media (max-width: 768px) { .modal-content { width: 100vw; height: 100vh; max-width: none; border-radius: 0; } }`. This should be a shared CSS class (`.modal-fullscreen-mobile`) applied to all modal content divs.
  - [x] 17.4 Floating action bar mobile adaptation (AC-13.5): the `HomeworkBulkActionBar` (from 8.3) is already styled as `position: fixed; bottom: 0` which works on mobile. Add: on mobile, stack the action buttons vertically instead of horizontally. Add safe area padding for iOS: `padding-bottom: env(safe-area-inset-bottom, 0)`.
  - [x] 17.5 List page mobile cards (AC-13.2): on `max-width: 768px`, homework cards show only title + status + due date. Tap to expand full card (toggle a `.expanded` class that shows all fields). Use `max-height` transition for smooth expand/collapse animation.
