# Task List: PRD-0035 — Homework Target Grid Redesign

**PRD:** `documentation/tasks/0035-prd-homework-target-grid-redesign.md`  
**Generated:** 2026-03-13  
**Target Developer:** Junior Developer

---

## Relevant Files

- `src/components/homework/HomeworkIcons.tsx` - **NEW** — Central SVG icon registry (35 icons) for all homework redesign components.
- `src/hooks/useTargetGrid.ts` - **NEW** — Hook to group homework by target, compute per-target stats, sort by urgency.
- `src/hooks/useClassStudentStats.ts` - **NEW** — Hook to lazy-load per-student stats for a class drill-down.
- `src/hooks/useStudentHomeworkModal.ts` - **NEW** — Hook to filter/sort homework for a student modal with class priority.
- `src/components/homework/TargetGrid.tsx` - **NEW** — Target grid container component (responsive 3/2/1 column layout).
- `src/components/homework/TargetGrid.css` - **NEW** — Styles for target grid including responsive breakpoints.
- `src/components/homework/TargetCard.tsx` - **NEW** — Individual target card (class or student) with glowing overdue animation.
- `src/components/homework/TargetCard.css` - **NEW** — Styles including pulseGlow animation and status-colored accents.
- `src/components/homework/StudentGrid.tsx` - **NEW** — Student grid shown on class drill-down with breadcrumb + back button.
- `src/components/homework/StudentGrid.css` - **NEW** — Student grid styles.
- `src/components/homework/StudentCard.tsx` - **NEW** — Per-student card with avatar, progress ring, overdue indicators.
- `src/components/homework/StudentCard.css` - **NEW** — Student card styles including progress ring SVG animation.
- `src/components/homework/HomeworkListModal.tsx` - **NEW** — Large centered modal for student homework list with search + advanced filters.
- `src/components/homework/HomeworkListModal.css` - **NEW** — Modal styles (sticky header, expandable search panel).
- `src/components/homework/CompactHomeworkCard.tsx` - **NEW** — Redesigned compact homework card with SVG icons and kebab menu.
- `src/components/homework/CompactHomeworkCard.css` - **NEW** — Compact card styles (status-colored left border, ~80px height).
- `src/components/homework/KebabActionMenu.tsx` - **NEW** — Kebab (⋮) dropdown menu with all homework actions.
- `src/components/homework/KebabActionMenu.css` - **NEW** — Dropdown menu positioning and animation styles.
- `src/components/homework/CompactStatsBar.tsx` - **NEW** — Single-row stats bar replacing HomeworkSummaryStats.
- `src/components/homework/CompactStatsBar.css` - **NEW** — Compact stats bar styles (60px height, horizontal layout).
- `src/components/homework/AdvancedSearchPanel.tsx` - **NEW** — Expandable filter panel with all relocated filter controls.
- `src/components/homework/AdvancedSearchPanel.css` - **NEW** — Expandable panel slide-down animation styles.
- `src/components/homework/index.ts` - **MODIFY** — Add exports for all new components.
- `src/components/modern/VanillaTabs.tsx` - **MODIFY** — Update `icon` prop from `string` to `string | ReactNode` to support SVG icons.
- `src/pages/TeacherHomeworkListPage.tsx` - **MODIFY** — Major refactor: integrate new views, remove alert banner, add target grid as default, etc.
- `src/hooks/useHomeworkList.ts` - **EXISTING** — Referenced by hooks; not modified.
- `src/hooks/useClassRoster.ts` - **EXISTING** — Used by `useClassStudentStats` for fetching class student data.
- `src/services/homeworkSubmissionService.ts` - **EXISTING** — `getHomeworkSubmissions()` used by student stats hook.
- `src/components/homework/HomeworkCard.tsx` - **EXISTING** — Reference for CompactHomeworkCard redesign.
- `src/components/homework/HomeworkSummaryStats.tsx` - **EXISTING** — Being replaced by CompactStatsBar (NOT deleted).
- `src/components/homework/HomeworkAlertBanner.tsx` - **EXISTING** — Removed from list page rendering (NOT deleted from codebase).
- `src/components/homework/HomeworkBulkActionBar.tsx` - **EXISTING** — Relocated into AdvancedSearchPanel / modal context.
- `src/components/homework/HomeworkTagChips.tsx` - **EXISTING** — Relocated into AdvancedSearchPanel.
- `src/components/modern/icons.jsx` - **EXISTING** — Source for reusable icon re-exports (ClockIcon, EditIcon, etc.).
- `src/components/layout/StudentIcons.tsx` - **EXISTING** — Source for reusable icon re-exports (IconSearch, IconProfile).
- `src/types/homework.types.ts` - **EXISTING** — Type definitions for homework assignments and targets.

### Notes

- Unit tests should typically be placed alongside the code files they are testing.
- Use `npx jest [optional/path/to/test/file]` to run tests.
- The project currently uses `@tabler/icons-react` for some icons in `TeacherHomeworkListPage.tsx` (line 10). The new `HomeworkIcons.tsx` uses custom inline SVGs instead. Tabler imports are removed during the page refactor (Task 7).
- `VanillaTabs` currently only accepts `icon` as a `string`. Task 2.4 updates it to accept `ReactNode` so SVG icon components can be passed.
- The `HomeworkTarget` union type (in `homework.types.ts` lines 53-102) has 4 variants: `class` (with `classId`, `className`), `students` (with `studentIds`, `studentNames`), `course`, and `group`. Only `class` and `students` are used in the target grid (FR-4).
- The `useHomeworkList` hook already returns `homework: HomeworkAssignment[]` — the target grid derives its data from this existing array with zero extra queries.
- **CSS Imports (applies to ALL tasks):** For every new `.css` file created (e.g., `TargetCard.css`), the corresponding `.tsx` component file MUST import it at the top with `import './[filename].css';`. Without this import, none of the styles will load. Example: `TargetCard.tsx` must have `import './TargetCard.css';` at the top.

## Tasks

- [x] 1.0 Create SVG Icon Registry (`HomeworkIcons.tsx`)
  - [x] 1.1 Create file `src/components/homework/HomeworkIcons.tsx`. Define the shared `HomeworkIconProps` interface at the top of the file: `{ size?: number; color?: string; className?: string; style?: React.CSSProperties }`. All icons in this file MUST use this interface. Reference PRD Section 6.6.8 for the exact interface definition.
  - [x] 1.2 Import and re-export 6 existing icons from the codebase. Import `ClockIcon`, `EditIcon`, `DeleteIcon`, `CloneIcon` from `../modern/icons.jsx`. Import `IconSearch`, `IconProfile` from `../layout/StudentIcons.tsx`. Re-export `CloneIcon` as `DuplicateIcon` and `IconSearch` as `SearchIcon` and `IconProfile` as `StudentIcon`. **ALL 6 re-exports MUST be wrapped in adapter components because their signatures differ from `HomeworkIconProps`.** Specific adapter instructions:
    - **`IconProfile` and `IconSearch`** accept NO props at all — they hardcode `style={{ width: 24/20, height: 24/20 }}`. The adapter MUST accept `size` and pass it via `style={{ width: size, height: size }}` to override the hardcoded dimensions. Example adapter: `export const StudentIcon: React.FC<HomeworkIconProps> = ({ size = 20, className, style }) => <span style={{ display: 'inline-flex', ...style }} className={className}><IconProfile /></span>;` then override the inner SVG size with a wrapping `<span style={{ width: size, height: size, display: 'inline-flex' }}>` that clips/scales the SVG.
    - **`ClockIcon`, `EditIcon`, `DeleteIcon`, `CloneIcon`** accept `{ size, style, className }` but NOT `color`. The adapter MUST pass `size` directly (they already accept it) and apply `color` as a CSS `color` property on a wrapper `<span>` element so the icon's `currentColor` stroke/fill inherits the color. Example adapter: `export const DuplicateIcon: React.FC<HomeworkIconProps> = ({ size = 16, color, className, style }) => <span style={{ color, display: 'inline-flex', ...style }} className={className}><CloneIcon size={size} /></span>;`
    - Reference PRD Section 6.6.9 for the full reuse matrix.
  - [x] 1.3 Create **6** Target Card icons as inline SVG components: `ClassIcon` (open book), `OverdueFlashIcon` (lightning bolt, **filled** — this is the ONLY icon that uses `fill={color || 'currentColor'}` and `stroke="none"` instead of stroke-based), `CalendarIcon` (calendar with dot), `UsersIcon` (two overlapping person outlines), `CheckCircleIcon` (circle with checkmark), `TargetPinIcon` (map pin). Note: `StudentIcon` is already created in Task 1.2 as an adapter around `IconProfile` and is NOT re-created here. Each MUST use `viewBox="0 0 24 24"`, stroke-based (except `OverdueFlashIcon` which is filled), `strokeWidth="2"`, `strokeLinecap="round"`, `strokeLinejoin="round"`, and `fill="none"` (unless filled). Default `size` = 20. Reference PRD Section 6.6.1 for design descriptions.
  - [x] 1.4 Create **4** Compact Homework Card icons: `DocumentIcon` (page with folded corner + 3 lines), `RetryIcon` (270° circular arrow with arrowhead), `BarChartIcon` (3 vertical bars ascending), `ProgressIcon` (horizontal bar with partial fill — two `<rect>` elements: one full-width unfilled background rect and one partial-width filled rect using `currentColor`. The fill width defaults to 50% visually; the actual width is controlled by the consumer via a `progress` prop or CSS). Same SVG standards as 1.3. Reference PRD Section 6.6.2. Note: `ProgressIcon` may not be used directly if the `CompactHomeworkCard` (Task 5.4) uses an inline HTML progress bar instead — but it MUST still be created for completeness and future use.
  - [x] 1.5 Create 5 Kebab Menu Action icons: `KebabMenuIcon` (3 stacked circles, filled `currentColor`), `ExtendIcon` (clock with forward arrow), `ResetIcon` (counter-clockwise arrow with center x), `RestoreIcon` (upward arrow from box), `PermanentDeleteIcon` (trash can with X overlay). Reference PRD Section 6.6.3. Note: `CloseAllIcon` also appears in PRD Section 6.6.3 but is created in Task 1.8 under Stats Bar Icons since it is primarily used in the `CompactStatsBar`. Do NOT create it here.
  - [x] 1.6 Create 4 Search & Filter icons: `ClearIcon` (X cross), `FilterIcon` (funnel), `FilterActiveIcon` (funnel with dot), `SortIcon` (3 descending-width lines). Reference PRD Section 6.6.4.
  - [x] 1.7 Create 5 Navigation & UI icons: `BackArrowIcon` (left chevron/arrow), `ChevronRightIcon` (right chevron), `LoadMoreIcon` (downward chevron), `RefreshIcon` (full circular arrow), `ExternalLinkIcon` (square with top-right arrow, 1.5px stroke). Reference PRD Section 6.6.5.
  - [x] 1.8 Create 7 Stats Bar and Empty State icons: `TotalIcon` (stacked pages), `ActiveIcon` (circle with play triangle), `WarningIcon` (triangle with exclamation, filled), `PercentIcon` (donut chart arc), `AttentionIcon` (bell with dot), `CreatePlusIcon` (circle with +), `CloseAllIcon` (square with diagonal line). Reference PRD Section 6.6.6.
  - [x] 1.9 Create 3 Empty State icons: `EmptyHomeworkIcon` (large clipboard, `viewBox="0 0 64 64"`, default size 64), `EmptyStudentsIcon` (two people with question mark, `viewBox="0 0 64 64"`), `EmptyAssignmentsIcon` (document with empty checkboxes, `viewBox="0 0 64 64"`). These are larger icons used at 48-96px. Reference PRD Section 6.6.7.
  - [x] 1.10 Add all icon exports to `src/components/homework/index.ts`. Add a single line: `export * from './HomeworkIcons';`.

- [x] 2.0 Create Data Hooks (`useTargetGrid`, `useClassStudentStats`, `useStudentHomeworkModal`)
  - [x] 2.1 Create `src/hooks/useTargetGrid.ts`. The hook accepts `homework: HomeworkAssignment[]` and `searchQuery: string` as arguments. It MUST: (a) Group homework by `target.type === 'class'` → one card per unique `target.classId`, and `target.type === 'students'` → one card per unique `studentId` within `target.studentIds`. Exclude `course` and `group` types (FR-4). (b) For each target card, compute: `targetName`, `targetType` ('class' | 'student'), `targetId` (classId or studentId), `activeCount`, `overdueCount`, `totalCount`, `completionRate` (average of `stats.completionRate`), `latestHomeworkDate` (max `createdAt`), `studentCount` (from `stats.totalAssigned` of most recent hw, class only), and the list of `homework` items belonging to this target. (c) All grouping MUST use `useMemo` with `[homework, searchQuery]` as dependencies.
  - [x] 2.2 Implement the urgency-first sorting algorithm inside `useTargetGrid`. The sort MUST follow PRD FR-11 exactly: Tier 1 = targets with `past_due` homework (sorted by overdue count desc, then by longest-overdue `dueDate` asc). Tier 2 = targets with active homework `dueDate` within 48 hours (sorted by most imminent deadline asc). Tier 3 = targets whose most recent homework `createdAt` is within 48 hours (sorted by `createdAt` desc). Tier 4 = other active targets (sorted by nearest `dueDate` asc). Tier 5 = all-completed targets (sorted by most recent `closedAt` desc). Use `const DAY_IN_MS = 24 * 60 * 60 * 1000; const URGENCY_WINDOW = 2 * DAY_IN_MS;` for the 48h window.
  - [x] 2.3 Implement search filtering inside `useTargetGrid`. When `searchQuery` is non-empty, filter target cards to only include those whose `targetName` matches OR whose homework items have a matching `title` or `materialTitle`. **Copy the `normalizeSearchValue` function directly into `useTargetGrid.ts` as a local (non-exported) helper function** — it exists in `useHomeworkList.ts` line 53 but is NOT exported, so you cannot import it. The implementation is: `function normalizeSearchValue(value: string): string { return value.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase(); }`. This enables Vietnamese diacritic-insensitive search. Return the filtered + sorted array as `targetCards`.
  - [x] 2.4 Update `src/components/modern/VanillaTabs.tsx`. Change the `VanillaTabDefinition.icon` prop type from `icon?: string` to `icon?: string | React.ReactNode`. In the render, **replace the existing line 40** (`{tab.icon ? \`${tab.icon} \` : ''}`) with the following conditional rendering: `{typeof tab.icon === 'string' ? tab.icon + ' ' : tab.icon ? <span style={{ marginRight: '0.35rem', display: 'inline-flex', verticalAlign: 'middle' }}>{tab.icon}</span> : null}`. Add `import type { ReactNode } from 'react';` at the top of the file (after the existing `Button` import on line 1).
  - [x] 2.5 Create `src/hooks/useClassStudentStats.ts`. The hook accepts `classId: string | null`, `classHomework: HomeworkAssignment[]` (homework targeting this class, already filtered). It MUST: (a) Call `useClassRoster(classId)` to get the student list (1 RTDB read, may be cached). (b) When `classId` changes and is non-null, fetch submissions for each homework in `classHomework` using `getHomeworkSubmissions(homeworkId)` from `homeworkSubmissionService.ts` — this is N Firestore queries where N = `classHomework.length`. (c) Cross-reference roster students with submissions to compute per-student stats: `studentId`, `studentName`, `avatarUrl` (if available from roster, else null), `homeworkAssigned` (count), `completedCount`, `overdueCount`, `averageScore`, `completionRate` (percentage), `lastSubmissionDate`. (d) **Cache pattern:** Use `const cacheRef = useRef<Record<string, StudentStats[]>>({});` to store computed stats. Before fetching, check `if (cacheRef.current[classId]) { setStudentStats(cacheRef.current[classId]); setLoading(false); return; }`. After computing stats, store with `cacheRef.current[classId] = computedStats;`. This ensures re-opening the same class is instant without re-fetching. (e) Return `{ studentStats, loading, error, refetch }`. The `refetch` function should bypass the cache by doing `delete cacheRef.current[classId]` then re-fetching. (f) Show skeleton state via `loading: true` while fetching.
  - [x] 2.6 Create `src/hooks/useStudentHomeworkModal.ts`. The hook accepts `studentId: string | null`, `classId: string | null`, `allHomework: HomeworkAssignment[]`. It MUST: (a) Filter `allHomework` using the following explicit rules:
    - **Include** homework where `target.type === 'students'` AND `target.studentIds.includes(studentId)` (individual assignments).
    - **If `classId` is provided** (opened from class drill-down): ALSO include homework where `target.type === 'class'` AND `target.classId === classId` (class assignments).
    - **If `classId` is NOT provided** (opened from individual student card on target grid): do NOT attempt to find other classes the student may belong to — only include `students`-type homework that explicitly names this student.
    - Exclude `course` and `group` type homework.
  (b) When `classId` is provided, sort homework from that class FIRST (priority grouping per FR-33): partition into two groups — `classHomework` (where `target.classId === classId`) and `otherHomework` (the rest). Sort each group by `createdAt` desc. Concatenate: `[...classHomework, ...otherHomework]`. When `classId` is NOT provided, sort all by `createdAt` desc. (c) Return `{ studentHomework, displayCount, loadMore, hasMore }` where `displayCount` starts at 20 and `loadMore` increments by 20 (FR-36).
  - [x] 2.7 **FR-12 Sort Dropdown Decision (IMPORTANT):** The PRD FR-12 says users must be able to change the target grid sort order via a sort dropdown. The task list removes the sort dropdown from the main page (Task 7.8) and relocates it to the modal. This is intentional: the target grid ALWAYS uses urgency-first sorting (Task 2.2) and does NOT expose a user-changeable sort dropdown. FR-12 is satisfied by the default urgency sorting algorithm. The sort dropdown in the AdvancedSearchPanel (Task 5.6) controls homework sorting WITHIN the modal only. No additional sort dropdown is needed on the main page for the Targets view. If this design decision changes later, add a `SortIcon` dropdown to the main page filter area that calls a `setTargetSort` function on `useTargetGrid`.

- [x] 3.0 Build Target Grid Components (`TargetGrid`, `TargetCard`)
  - [x] 3.1 Create `src/components/homework/TargetCard.css`. Define styles: `.target-card` base with glass-morphism (matching existing `Card variant="glass"`), `cursor: pointer`, hover elevation. `.target-card--overdue` with `border: 1.5px solid rgba(239, 68, 68, 0.4)` and `animation: pulseGlow 2s ease-in-out infinite`. `.target-card--completed` with `opacity: 0.7`. Define `@keyframes pulseGlow` per PRD Section 6.3: 0%/100% `box-shadow: 0 0 8px rgba(239,68,68,0.3)`, 50% `box-shadow: 0 0 20px rgba(239,68,68,0.5)`. Completion bar styles: `.target-card__progress-bar` (height 6px, border-radius 3px, background `rgba(148,163,184,0.2)`) and `.target-card__progress-fill` (green gradient).
  - [x] 3.2 Create `src/components/homework/TargetCard.tsx`. Props: `target` (from `useTargetGrid` output), `onClick: (target) => void`. Render: top row with `ClassIcon` or `StudentIcon` + target name (bold) + overdue badge using `OverdueFlashIcon` with red count. Meta line: "5 active · 2 overdue · 32 students" using inline SVG icons. Progress bar with percentage. Footer: total count + "Latest: Mar 13" using `CalendarIcon`. Apply CSS class `.target-card--overdue` when `target.overdueCount > 0`. Apply `.target-card--completed` when `target.activeCount === 0 && target.overdueCount === 0`. Card uses `Card variant="glass"` from modern components.
  - [x] 3.3 Create `src/components/homework/TargetGrid.css`. Define responsive grid: `.target-grid` with `display: grid`, `gap: 1rem`. Media queries: `≥1024px` → `grid-template-columns: repeat(3, 1fr)`, `≥768px` → `repeat(2, 1fr)`, `<768px` → `1fr` (FR-7). Add staggered animation: `.target-grid > *:nth-child(n) { animation: slideUp 0.45s ease-out calc(var(--index) * 0.04s) backwards; }`.
  - [x] 3.4 Create `src/components/homework/TargetGrid.tsx`. Props: `targetCards` (from `useTargetGrid`), `onTargetClick: (target) => void`, `onCreateHomework?: () => void`. Render a `<div className="target-grid">` mapping each target card to `<TargetCard>`. Pass `--index` CSS variable via `style={{ '--index': index } as React.CSSProperties}` for staggered animation. If `targetCards.length === 0`, show empty state with `EmptyHomeworkIcon` at 64px, heading "No homework yet", description text, and "Create Homework" CTA `Button` that calls `onCreateHomework()` when clicked. This matches FR-56. If `onCreateHomework` is not provided, do not render the CTA button.

- [ ] 4.0 Build Student Grid Drill-Down Components (`StudentGrid`, `StudentCard`)
  - [ ] 4.1 Create `src/components/homework/StudentCard.css`. Styles for `.student-card`: glass-morphism, cursor pointer. Avatar styles: `.student-card__avatar` (48px circle, centered initials, colored background computed from student name hash). Progress ring: an SVG circle with `stroke-dasharray` and `stroke-dashoffset` to show completion percentage. `.student-card--overdue` uses the same `pulseGlow` animation as target cards (FR-18).
  - [ ] 4.2 Create `src/components/homework/StudentCard.tsx`. Props: `student` (from `useClassStudentStats` output), `onClick: (studentId, studentName) => void`. Render: avatar circle (initials fallback using first letter of name with deterministic background color from name hash). Student name (bold). Stats row: "[CheckCircleIcon] 6/8" + "[OverdueFlashIcon] 2 overdue" (red text). Progress ring SVG: circular ring showing `completionRate` percentage. "Avg: 78%" and "Last: Mar 12" text. Apply overdue animation class if `student.overdueCount > 0`. Sort completed-all students to bottom per FR-19.
  - [ ] 4.3 Create `src/components/homework/StudentGrid.css`. Breadcrumb styles: `.student-grid__breadcrumb` with flex, gap, font-size 0.9rem, color #64748b. Back button: `.student-grid__back` styled as a small round button. Grid layout: same responsive columns as TargetGrid. Skeleton card: `.student-card--skeleton` with shimmer animation `@keyframes shimmer` from PRD FR-23.
  - [ ] 4.4 Create `src/components/homework/StudentGrid.tsx`. Props: `classId: string`, `className: string`, `classHomework: HomeworkAssignment[]`, `onBack: () => void`, `onStudentClick: (studentId, studentName, classId, className) => void`, `searchQuery: string`. Internally uses `useClassStudentStats(classId, classHomework)`. Renders: (a) Breadcrumb bar: `BackArrowIcon` button + "All Targets" (clickable, calls `onBack`) + `ChevronRightIcon` + className (FR-14, FR-15). (b) While `loading`: render 6 skeleton cards with shimmer animation in the grid. (c) When loaded: render student cards in grid, filtering by `searchQuery` against student name. **Copy the `normalizeSearchValue` function** into this file as a local helper (same as Task 2.3 — the implementation is: `function normalizeSearchValue(value: string): string { return value.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase(); }`). Use it to compare `normalizeSearchValue(student.studentName)` against `normalizeSearchValue(searchQuery)`. (d) If 0 students found: show `EmptyStudentsIcon` + "No students found in this class." message (FR-57). (e) Add a `RefreshIcon` button in the breadcrumb bar that calls `refetch()` to re-load student data (FR-24 cache refresh).

- [ ] 5.0 Build Homework List Modal (`HomeworkListModal`, `CompactHomeworkCard`, `KebabActionMenu`, `AdvancedSearchPanel`)
  - [ ] 5.1 Create `src/components/homework/KebabActionMenu.css`. Styles: `.kebab-menu` as relative container. `.kebab-menu__trigger` as a 32x32 button with no background, centered `KebabMenuIcon`. `.kebab-menu__dropdown` absolutely positioned right-aligned, white background, `border-radius: 8px`, `box-shadow: 0 10px 40px rgba(0,0,0,0.15)`, `min-width: 200px`, `z-index: 100`. `.kebab-menu__item` as flex row with icon + label, padding 10px 16px, hover background `rgba(99,102,241,0.06)`. `.kebab-menu__item--danger` with red color for delete actions. Entry animation: `@keyframes dropdownFadeIn { from { opacity: 0; transform: translateY(-8px); } }`.
  - [ ] 5.2 Create `src/components/homework/KebabActionMenu.tsx`. Props: `homework: HomeworkAssignment`, `onEdit`, `onDuplicate`, `onDelete`, `onExtendDeadline`, `onRestore`, `onPermanentDelete`, `onResetComplete?`. Uses `useState` for `isOpen`. Declare `const menuRef = useRef<HTMLDivElement>(null);` and attach it to the `.kebab-menu` container div. Renders trigger button with `KebabMenuIcon`. When open, renders dropdown with menu items: Edit (`EditIcon`), Duplicate (`DuplicateIcon`), Delete/Archive (`DeleteIcon`), Extend Deadline (`ExtendIcon`), Reset Student (`ResetIcon`, only if `onResetComplete` is provided). Conditionally show: Restore (`RestoreIcon`) only if `homework.archived`, Permanent Delete (`PermanentDeleteIcon`, danger style) only if `homework.archived`. Close dropdown on any item click or outside click using this exact pattern (MUST include cleanup to avoid memory leaks):
    ```
    useEffect(() => {
        if (!isOpen) return;
        const handler = (e: MouseEvent) => {
            if (menuRef.current && !menuRef.current.contains(e.target as Node)) {
                setIsOpen(false);
            }
        };
        document.addEventListener('mousedown', handler);
        return () => document.removeEventListener('mousedown', handler); // ← MUST include cleanup
    }, [isOpen]);
    ```
    Stop propagation on trigger click to prevent card click: `onClick={(e) => { e.stopPropagation(); setIsOpen(!isOpen); }}`.
  - [ ] 5.3 Create `src/components/homework/CompactHomeworkCard.css`. Styles: `.compact-hw-card` with `display: flex`, `align-items: stretch`, `min-height: 72px`, `max-height: 88px`, `border-radius: 10px`, `background: rgba(255,255,255,0.85)`, `border: 1px solid rgba(148,163,184,0.12)`, `cursor: pointer`, `overflow: hidden`, hover shadow. `.compact-hw-card__status-border` — 4px wide left border, colors: `--active: #14b8a6` (teal), `--scheduled: #8b5cf6` (purple), `--past_due: #ef4444` (red), `--draft: #94a3b8` (gray), `--closed: #1e293b` (dark). `.compact-hw-card__content` flex-grow with padding. `.compact-hw-card__kebab` flex-shrink-0 right side.
  - [ ] 5.4 Create `src/components/homework/CompactHomeworkCard.tsx`. Props: `homework: HomeworkAssignment`, `onClick: (hw) => void`, action callbacks (same as KebabActionMenu), `availableTags`. Render: Left status border (4px div colored by `homework.status`). Content area: Row 1: `DocumentIcon` + title (truncated) + `HomeworkStatusBadge` + target label with `ClassIcon`/`StudentIcon`. Row 2: `ClockIcon` + formatted due date + `ClockIcon` + timer if set + `RetryIcon` + attempts. Row 3: inline progress bar + "18/24 submitted" + `BarChartIcon` + avg score. Right: `KebabActionMenu`. Clicking the card body (not kebab) calls `onClick(homework)`. Format dates as "Mar 15" using `toLocaleDateString('en-US', { month: 'short', day: 'numeric' })`.
  - [ ] 5.5 Create `src/components/homework/AdvancedSearchPanel.css`. Styles: `.adv-search-panel` with `max-height: 0`, `overflow: hidden`, `transition: max-height 0.3s ease-out, opacity 0.3s ease-out`, `opacity: 0`. `.adv-search-panel--open` with `max-height: 500px`, `opacity: 1`. `.adv-search-panel__toggle` button styles (outline with `FilterIcon`, highlighted accent when active per FR-39). `.adv-search-panel__content` with padding, flex-wrap layout for filter controls.
  - [ ] 5.6 Create `src/components/homework/AdvancedSearchPanel.tsx`. Props: `statusFilter`, `onStatusChange`, `sort`, `onSortChange`, `tagFilter`, `onTagChange`, `allTags`, `showClosed`, `onShowClosedToggle`, `showArchived`, `onShowArchivedToggle`, `bulkModeEnabled`, `onBulkModeToggle`, `selectedCount`, `onBulkExtend`, `onBulkClose`, `onBulkDuplicate`, `onBulkDelete`, `onDeselectAll`. Renders: toggle button with `FilterIcon` / `FilterActiveIcon` (active = any non-default filter). Expandable panel with: Status filter buttons (All, Active, Scheduled, Past Due, Draft, Closed) — copy the exact button pattern from current `TeacherHomeworkListPage.tsx` lines 877-923. Sort `NativeSelect` dropdown. `HomeworkTagChips`. Show Closed / Show Archived toggle buttons. Bulk Select toggle. Track `isOpen` in `useState`. **Bulk action bar placement:** The `AdvancedSearchPanel` renders a "Bulk Select" toggle button. When bulk mode is active AND items are selected, render `<HomeworkBulkActionBar>` as a **sticky bar at the bottom of the modal** (positioned via the modal's CSS), NOT inside the expandable panel. The panel only contains the toggle; the action bar is a separate element below the scrollable body.
  - [ ] 5.7 Create `src/components/homework/HomeworkListModal.css`. Styles: `.hw-modal-overlay` with `position: fixed`, `inset: 0`, `background: rgba(0,0,0,0.5)`, `z-index: 1000`, `display: grid`, `place-items: center`. `.hw-modal` with `width: min(80vw, 960px)`, `max-height: 80vh`, `background: #fff`, `border-radius: 16px`, `display: flex`, `flex-direction: column`, `overflow: hidden`. `.hw-modal__header` with `position: sticky`, `top: 0`, `padding: 1.25rem 1.5rem`, `border-bottom: 1px solid #e2e8f0`, `z-index: 2`, `background: #fff`. `.hw-modal__body` with `flex: 1`, `overflow-y: auto`, `padding: 1rem 1.5rem`. `.hw-modal__close` button top-right. Entry animation `@keyframes modalSlideUp`.
  - [ ] 5.8 Create `src/components/homework/HomeworkListModal.tsx`. Props: `isOpen: boolean`, `onClose: () => void`, `studentId: string`, `studentName: string`, `classId?: string`, `className?: string`, `allHomework: HomeworkAssignment[]`, `onNavigateToDetail: (hw) => void`, action callbacks for edit/duplicate/delete/extend/restore/permanentDelete, **`onResetComplete?: () => Promise<void>`** (for resetting a student's submission — passed through to `CompactHomeworkCard` → `KebabActionMenu`), `availableTags`, `onRefetch`. Internally: uses `useStudentHomeworkModal(studentId, classId, allHomework)` for filtering + pagination. Has own `searchInput`/`searchQuery` state with 300ms debounce (FR-37). Has own `statusFilter`, `sort`, `tagFilter`, `showClosed`, `showArchived`, `bulkModeEnabled`, `selected` state for the advanced search panel. Renders: overlay (clicking closes modal, FR-30) + modal container. Sticky header: title "Homework — {studentName} ({className || 'Individual'})" + close X button (`ClearIcon`) + search `Input` within header. `AdvancedSearchPanel` below header. Scrollable body: list of `CompactHomeworkCard` components. "Load More" button if `hasMore`. Empty state with `EmptyAssignmentsIcon` if 0 homework (FR-58). Clicking a `CompactHomeworkCard` calls `onNavigateToDetail(hw)` then `onClose()` (FR-35). **Note on `onNavigateToDetail`:** This callback receives the existing `handleOpenDetail` function from `TeacherHomeworkListPage.tsx` which navigates to `/teacher/homework/:homeworkId`. Do NOT create a new handler — reuse the existing one passed down as a prop.

- [ ] 6.0 Build Compact Stats Bar (`CompactStatsBar`)
  - [ ] 6.1 Create `src/components/homework/CompactStatsBar.css`. Styles: `.compact-stats-bar` with `display: flex`, `align-items: center`, `gap: 0`, `flex-wrap: wrap`, `background: rgba(255,255,255,0.85)`, `border: 1px solid rgba(148,163,184,0.12)`, `border-radius: 12px`, `padding: 0.5rem 1rem`, `min-height: 60px`. `.compact-stats-bar__stat` with `display: flex`, `align-items: center`, `gap: 0.35rem`, `padding: 0.4rem 0.75rem`. `.compact-stats-bar__divider` as a thin vertical line (1px, height 28px, background `#e2e8f0`). `.compact-stats-bar__actions` with `margin-left: auto`, `display: flex`, `gap: 0.5rem`. Labels: `font-size: 0.8rem`, `color: #64748b`. Values: `font-size: 1.1rem`, `font-weight: 700`. Past Due stat: red accent when value > 0. Responsive: on mobile (`<768px`), stack stats vertically, hide dividers.
  - [ ] 6.2 Create `src/components/homework/CompactStatsBar.tsx`. Props: `totalCount: number`, `visibleCount: number`, `activeScheduledCount: number`, `pastDueCount: number`, `avgCompletionRate: number`, `needsAttentionCount: number`, `onClosePastDue: () => void`, `onCreateHomework: () => void`, `onCreateThcsHomework: () => void`, `userId?: string` (used to disable the "Close All Past Due" button when no user is authenticated: `disabled={pastDueCount === 0 || !userId}`). Render: 5 stat items each with SVG icon prefix + label + value: `TotalIcon` "Total: {totalCount}" with helper "(N visible)", `ActiveIcon` "Active: {activeScheduledCount}", `WarningIcon` "Past Due: {pastDueCount}" (red if > 0), `PercentIcon` "Avg: {avgCompletionRate}%", `AttentionIcon` "Attention: {needsAttentionCount}". Dividers between stats. Right-aligned actions: "Close All Past Due" `Button variant="secondary"` (disabled if pastDueCount === 0 OR !userId), "Create Homework" `Button variant="primary"` with green gradient, "Create THCS Homework" `Button variant="primary"` with purple gradient. All 3 buttons use same styling as current `TeacherHomeworkListPage.tsx` lines 781-805.
  - [ ] 6.3 Add exports for `CompactStatsBar` to `src/components/homework/index.ts`.

- [ ] 7.0 Refactor TeacherHomeworkListPage (Integration)
  - [ ] 7.1 Update imports in `TeacherHomeworkListPage.tsx`. Remove: `import { IconCheckbox, IconClock, IconAlertTriangle, IconLock, IconEdit } from '@tabler/icons-react';` (line 10). Remove `HomeworkAlertBanner` from the homework component import (line 19). Remove `HomeworkSummaryStats` from imports (line 24). Remove `ClassAnalyticsHeader` and `AtRiskStudentList` imports (lines 26-27). Add imports: `import { TargetGrid, CompactStatsBar, HomeworkListModal } from '../components/homework';`, `import { StudentGrid } from '../components/homework';`, `import { ClassIcon, TargetPinIcon, CalendarIcon, BarChartIcon, EmptyHomeworkIcon } from '../components/homework/HomeworkIcons';`, `import { useTargetGrid } from '../hooks/useTargetGrid';`.
  - [ ] 7.2 Change the `ViewMode` type from `'by_class' | 'chronological' | 'by_status'` to `'targets' | 'chronological' | 'by_status'` (FR-47 removes "By Class"). Change default `useState<ViewMode>('chronological')` to `useState<ViewMode>('targets')`. Add new state: `const [drillDownClass, setDrillDownClass] = useState<{ classId: string; className: string; homework: HomeworkAssignment[] } | null>(null);` for tracking class drill-down. Add state for the student modal: `const [modalStudent, setModalStudent] = useState<{ studentId: string; studentName: string; classId?: string; className?: string } | null>(null);`.
  - [ ] 7.3 Integrate `useTargetGrid` hook: `const { targetCards } = useTargetGrid(homework, searchQuery);` after the `useHomeworkList` call. This consumes the already-loaded homework array.
  - [ ] 7.4 Update the `viewTabs` useMemo to use SVG icons and the new tab keys: `[{ key: 'targets', label: 'Targets', icon: <TargetPinIcon size={14} /> }, { key: 'chronological', label: 'Timeline', icon: <CalendarIcon size={14} /> }, { key: 'by_status', label: 'By Status', icon: <BarChartIcon size={14} /> }]`. The "By Class" tab is removed entirely.
  - [ ] 7.5 Add tab-switch handler to reset drill-down state per FR-48: In the `onTabChange` handler for `VanillaTabs`, add `setDrillDownClass(null);` so switching tabs always returns to the top-level grid view.
  - [ ] 7.6 Replace `<HomeworkSummaryStats>` section (lines 746-808) with `<CompactStatsBar>`. Pass all computed stats from existing `useMemo` values: `totalCount={homework.length}`, `visibleCount={visibleHomework.length}`, `activeScheduledCount={(statusCounts.active ?? 0) + (statusCounts.scheduled ?? 0)}`, `pastDueCount={statusCounts.past_due ?? 0}`, `avgCompletionRate={averageCompletionRate}`, `needsAttentionCount={needsAttentionCount}`, `onClosePastDue={handleClosePastDue}`, `onCreateHomework={handleCreateHomework}`, `onCreateThcsHomework={handleCreateThcsHomework}`, `userId={user?.uid}`.
  - [ ] 7.7 Remove `<HomeworkAlertBanner alerts={alertItems} />` (line 810). Delete the entire `alertItems` useMemo block (lines 565-598) since it's no longer needed. Urgency is now communicated via card sorting and glowing borders (FR-54). **IMPORTANT: Keep the `getHomeworkAlertBadge` function (lines 41-73)** — it is still used by `renderHomeworkCards` for the Timeline and By Status views. Do NOT delete it.
  - [ ] 7.8 Simplify the main filter/search area. The search bar currently lives inside a `Card variant="glass"` (lines 812-934). Replace the inner content with: just the `Input` search bar (with placeholder "Search classes, students, or homework..." per FR-44), a clear button (ClearIcon) when value is non-empty (FR-45), and the `VanillaTabs`. Remove: sort dropdown, status filter buttons, tag chips, bulk select button, show closed button, show archived button from the main page — ALL of these are relocated to `AdvancedSearchPanel` inside the modal. The main page search bar is now clean and simple (FR-41). **IMPORTANT: Keep the existing debounce logic** that syncs `searchInput` → `searchQuery` with a 300ms delay. This is the `useEffect` with `setTimeout` that already exists in `TeacherHomeworkListPage.tsx`. Do NOT remove it — the search bar still needs debounced query updates.
  - [ ] 7.9 Replace the `viewMode === 'chronological'` and `viewMode === 'by_class'` rendering blocks (lines 1044-1095). Add new rendering for `viewMode === 'targets'`: if `drillDownClass` is set, render `<StudentGrid classId={drillDownClass.classId} className={drillDownClass.className} classHomework={drillDownClass.homework} onBack={() => setDrillDownClass(null)} onStudentClick={(studentId, studentName, classId, className) => setModalStudent({ studentId, studentName, classId, className })} searchQuery={searchQuery} />`. Otherwise, render `<TargetGrid targetCards={targetCards} onTargetClick={(target) => { if (target.targetType === 'class') { setDrillDownClass({ classId: target.targetId, className: target.targetName, homework: target.homework }); } else { setModalStudent({ studentId: target.targetId, studentName: target.targetName }); } }} onCreateHomework={handleCreateHomework} />`. Note the `onCreateHomework` prop — this connects the empty state CTA button in `TargetGrid` (Task 3.4) to the existing `handleCreateHomework` handler. Keep `viewMode === 'chronological'` and `viewMode === 'by_status'` rendering exactly as-is.
  - [ ] 7.10 Add `<HomeworkListModal>` at the bottom of the component (alongside existing modals). Pass: `isOpen={!!modalStudent}`, `onClose={() => setModalStudent(null)}`, `studentId={modalStudent?.studentId}`, `studentName={modalStudent?.studentName}`, `classId={modalStudent?.classId}`, `className={modalStudent?.className}`, `allHomework={homework}`, `onNavigateToDetail={handleOpenDetail}` (**reuse the existing `handleOpenDetail` handler** that navigates to `/teacher/homework/:homeworkId` — do NOT create a new one), action callbacks (`handleEdit`, `handleDuplicate`, `handleDelete`, `handleExtendDeadline`, `handleRestore`, `handlePermanentDelete`), `onResetComplete={handleResetComplete}` (if it exists; otherwise pass `undefined`), `availableTags={homeworkTags}`, `onRefetch={refetch}`.
  - [ ] 7.11 Update the empty state (lines 980-1011). Replace the 📋 emoji with `<EmptyHomeworkIcon size={80} />`. Update heading to "No homework yet" and description to "Create your first homework assignment to get started." per FR-56.
  - [ ] 7.12 Remove the `homeworkByClass` useMemo (lines 472-488) — it's no longer needed since the "By Class" tab is removed. **Replace the `getStatusIcon` function** (lines 75-84) with a new implementation using SVG icons from `HomeworkIcons.tsx`. The tabler icon imports are removed in Task 7.1, so the old function will not compile. The new implementation is:
    ```typescript
    function getStatusIcon(status: string) {
        switch (status) {
            case 'active': return <ActiveIcon size={16} />;
            case 'scheduled': return <ClockIcon size={16} />;
            case 'past_due': return <WarningIcon size={16} />;
            case 'draft': return <EditIcon size={16} />;
            case 'closed': return <CheckCircleIcon size={16} />;
            default: return null;
        }
    }
    ```
    Add `ActiveIcon`, `WarningIcon`, `CheckCircleIcon` to the imports from `HomeworkIcons` in Task 7.1. `ClockIcon` and `EditIcon` are already imported via that same line.
  - [ ] 7.13 Remove the stale `by_class` rendering block (lines 1046-1095) entirely. This view is fully replaced by the Targets grid + Student drill-down.
  - [ ] 7.14 **Note:** The `pulseGlow` keyframe animation is already defined in `TargetCard.css` (Task 3.1) and `StudentCard.css` (Task 4.1). Adding it again to the inline `<style>` block (lines 1202-1219) in `TeacherHomeworkListPage.tsx` is **redundant** but harmless. Only add it here if the TargetCard/StudentCard CSS files are not being loaded on the page for some reason. In most cases, **skip this task** since the CSS files are imported by their respective components. If you DO add it, it will not cause errors (CSS ignores duplicate keyframe definitions).
  - [ ] 7.15 Update the page title. Replace `📋 Homework Management` (line 740) with `<EmptyHomeworkIcon size={32} style={{ display: 'inline', verticalAlign: 'middle' }} /> Homework Management` or simply remove the emoji and use just text. The page description can remain as-is.
