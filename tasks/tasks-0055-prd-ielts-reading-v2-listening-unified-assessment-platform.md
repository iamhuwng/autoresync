# Task List: PRD 0055 IELTS Reading V2 And Listening Unified Assessment Platform

Status: Task 1 planning completed on 2026-06-20 after explicit product-owner and architecture/security reviewer approval; Task 2 S0 parent acceptance is complete through Task 2.15; later child-specific implementation remains separately gated

Current PRD-0055 remaining-scope correction, 2026-07-01: Task 8.14-8.18 and Task 9.0-9.15 are currently localhost-only implementation/proof work. Use teacher `http://localhost:5173` and student `http://localhost:5174`. Do not use `https://kahut1.web.app` or any live domain as a current unlock path. Deployed/current truth, selected-user rollout, percentage rollout, full rollout, human production acceptance, rollback/recovery production proof, and production-current documentation truth are deferred to PRD-0062 Listening Deployed Truth And Production Rollout Closure or a separately approved future deploy/rollout PRD. Historical live-domain artifacts remain historical evidence only, not current gates. No production deploy, live-domain browser test, selected-user rollout, percentage rollout, full rollout, cleanup/delete, commit, push, or merge is authorized by this correction. Checkbox closure still requires separate current local evidence, task/docs reconciliation, and exact blocker accounting.
Source PRD: `tasks/0055-prd-ielts-reading-v2-listening-unified-assessment-platform.md`
Generated under: `documentation/tasks/generate-tasks.md`

## Relevant Files

- `tasks/0055-prd-ielts-reading-v2-listening-unified-assessment-platform.md` - Approved parent product and architecture authority; OQ-1 through OQ-4 are binding, while implementation remains gated by approved child PRDs.
- `tasks/0056-prd-listening-upload-worker-security-gate-s0.md` - Child PRD for the severable Listening upload-worker Security Gate S0; Tasks 2.6 through 2.15 and parent Task 2.0 are complete, with deployed/current S0 truth, independent review, rollback/version-pin proof, final acceptance evidence, and unique proof-object cleanup recorded.
- `tasks/0056a-prd-listening-upload-session-bridge.md` - Approved Spark-safe Worker-only bridge for backend-issued Listening upload sessions/assets and the `temp/listening-audio/` to `temp/listening/` transition; deployed/current S0 proof is recorded, and deployed/current PRD-0056A Worker proof is accepted on `r2-upload-signer` version `3687d2e0-4718-4c0b-9c84-7f81749c31fb` with current RTDB rules, cleanup, recovery rehearsal, and local/emulator proof.
- `tasks/0057-prd-listening-authoring-draft-publish-version-behavior.md` - Draft child PRD for Listening draft, publish, immutable version, revision, and legacy transition behavior; B2 Option B data contract and Task 1 planning are approved. Production authoring mutations now target the Spark-safe Cloudflare Worker + Firebase RTDB REST route; prior `functions/src/listening-authoring/**` proof is reusable local/shared core only unless the product owner reverses the Spark-tier constraint. A single selected-teacher Worker HTTP proof passed on 2026-06-29 and creates real authoring sample data for Task 6.3 dependency use; natural browser UI write proof and wider rollout remain separate gates.
- `tasks/0058-prd-r2-asset-lifecycle-registry-reconciliation-cleanup-delivery.md` - Draft child PRD for R2 asset lifecycle, registry, reconciliation, cleanup, and delivery; B1 Option B bridge ownership, Task 1 planning, Task 4.2 foundation, Task 4.3 scope confirmation, Task 6.3 local reconciliation dry-run foundation, Task 6.4 local historical inventory foundation, Task 6.5 local backup-governance foundation, Task 6.6-6.8 local authorized delivery/result-review client and Worker route proof, Task 6.9-6.11 local rollout/metrics/rollback proof, Task 6.12 independent verification record, Task 6.13 parent acceptance, and parent Task 6.0 are accepted; cleanup execution, solo/live private cutover, deployment, and remote mutation remain separately gated.
- `tasks/0059-prd-listening-solo-homework-runtime-alignment.md` - Draft child PRD for Listening solo/homework runtime alignment, submit idempotency, resume, mobile state, and host-bounded private delivery; Task 1 planning is complete, Task 7.1 through Task 7.15 and parent Task 7.0 are complete locally for the authorized internal-fixture scope. Selected solo/homework traffic, percentage rollout, live private cutover, deployment, and remote mutation remain unclaimed and separately gated.
- `tasks/0060-prd-listening-live-session-authority-runtime-load-test-plan.md` - Draft child PRD for canonical Listening live-session authority, teacher/student runtime recovery, private-delivery handoff, and the 100-student-by-20-session load-test plan; Task 1 planning is complete; implementation remains blocked pending dedicated authority tests/harness, applicable authoring/delivery dependencies, child-specific product-owner plus architecture/security approval, and remaining proof gates.
- `tasks/0061-prd-reading-v2-runtime-visual-alignment.md` - Draft child PRD for projection-bound Reading V2 runtime visual alignment, protected host/data contracts, responsive/accessibility proof, and bounded presentation decomposition; Task 1 planning is complete; implementation remains blocked pending Task 3 shared-authoring stability, dedicated Reading V2 runtime tests, child-specific product-owner review, and explicit authorization.
- `tasks/tasks-0055-prd-ielts-reading-v2-listening-unified-assessment-platform.md` - This implementation tasklist and progress source of truth.
- `tasks/findings-of-tasks-0055-prd-ielts-reading-v2-listening-unified-assessment-platform.md` - Append-only implementation findings/evidence file to create before implementation.
- `tasks/traceability-0055-prd-ielts-reading-v2-listening-unified-assessment-platform.md` - Requirement-to-task/child-PRD/test matrix; zero unmapped rows required before Task 1 STOP.
- `tasks/large-file-maps-0055/` - Per-file structural comprehension maps required before editing or boundary-verifying large/named source files.
- `documentation/ielts-reading-v2-listening-unification-strategy.md` - Product direction and sequencing.
- `documentation/ielts-reading-v2-listening-unification-research.md` - Current architecture research.
- `documentation/ielts-reading-v2-listening-unification-audit.md` - Boundary and risk audit.
- `documentation/ielts-reading-v2-listening-unification-implementation-log.md` - Existing shared-primitive patch evidence.
- `documentation/architecture/ielts-reading-v2-listening-unification.md` - Canonical module-boundary authority.
- `documentation/architecture/upload-storage-authority.md` - R2-only upload authority and storage lifecycle contract.
- `documentation/architecture/ui-design-standards.md` - Required teacher/shared UI design rules.
- `documentation/architecture/mobile-ielts-listening-audio-navigation.md` - Listening mobile navigation authority.
- `documentation/architecture/mobile-ielts-listening-runtime-diagnostics.md` - Listening runtime diagnostics authority.
- `documentation/rules/infrastructure.md` - Required rules for Firebase paths, workers, backups, and deployments.
- `documentation/rules/codebase-hygiene.md` - Required rules for producer/consumer storage changes and Mantine removal.
- `documentation/rules/react-patterns.md` - Required rules for new shared components and async React state.
- `documentation/rules/mobile-portability.md` - Required rules for browser APIs and client persistence.
- `documentation/rules/observability.md` - Required page/action tracking contract.
- `documentation/rules/announcements.md` - Required save/publish/delete user announcement behavior.
- `documentation/SOP/R2_WORKER_UPDATE_GUIDE.md` - Current manual deployment guide for the deployed `r2-upload-signer` native `env.R2_BUCKET` worker; differs from checked-in upload-worker source.
- `src/features/assessment/shared/components/AssessmentStatusState.tsx` - Existing neutral loading/error/empty/status primitive.
- `src/features/assessment/shared/components/AssessmentStatusState.test.tsx` - Component semantics and accessibility tests.
- `src/features/assessment/shared/components/AssessmentValidationSummary.tsx` - Existing neutral calculated-validation display.
- `src/features/assessment/shared/components/AssessmentValidationSummary.test.tsx` - Validation summary semantics tests.
- `src/features/assessment/shared/components/AssessmentAuthoringSection.tsx` - Existing neutral authoring section primitive.
- `src/features/assessment/shared/components/AssessmentAuthoringSection.test.tsx` - Section roles, heading, action, and slot tests.
- `src/pages/ReadingV2StudioPage.tsx` - Existing Reading V2 route-level shared status adoption.
- `src/pages/ReadingV2StudioPage.test.tsx` - Reading V2 route-state regression tests.
- `src/components/reading-v2/studio/ReadingV2SettingsPanel.tsx` - Existing shared section/validation adoption.
- `src/components/reading-v2/studio/ReadingV2SettingsPanel.test.tsx` - Reading V2 display-section regression tests.
- `src/skills/listening/builders/ListeningTestBuilder.tsx` - Current Listening builder, upload flow, questions, review, and single save/publish UI.
- `src/skills/listening/builders/ListeningTestBuilder.test.tsx` - Listening builder authoring tests.
- `src/pages/TestBuilderRouter.tsx` - Routes Listening authoring to `ListeningTestBuilder`.
- `src/services/listeningTestStorage.ts` - Current Listening persistence owner; currently hard-blocks missing audio and writes `isPublished: true`.
- `src/services/r2Storage.ts` - Current R2 browser upload/move service, exported as `r2StorageService`.
- `src/services/r2Storage.test.ts` - Current R2 client-service tests.
- `cloudflare/worker.js` - Current upload/move worker; security gate S0 target.
- `cloudflare/package-lock.json` - Existing upload-worker dependency lock; no checked-in `package.json` currently exists.
- `cloudflare/wrangler.toml` - Proposed only if Task 2 chooses Wrangler-managed upload-worker deployment; does not currently exist.
- `database.rules.json` - RTDB rules for any new Listening draft/version/asset registry paths.
- `firebase.json` - Firebase emulator/deployment configuration; Task 4 must verify executable RTDB emulator wiring before rule-test work.
- `src/__tests__/security/firebaseRules.test.ts` - Existing non-emulator RTDB contract test; not sufficient as the Task 4 emulator anchor.
- `src/__tests__/security/prd0040-security.emulator.test.ts` - Existing `@firebase/rules-unit-testing` emulator-backed rule-test pattern for Task 4.
- `.github/workflows/assessment-unification-guardrails.yml` - Proposed low-cost CI guardrail workflow for shared-boundary grep and focused shared tests.
- `r2-backup-worker/src/auth/firebase-auth.ts` - Existing worker-side Firebase identity verification pattern.
- `r2-backup-worker/src/index.ts` - Existing scheduled worker/router and potential reconciliation integration owner.
- `r2-backup-worker/src/backup/media-delta.ts` - Existing media backup behavior relevant to deletion retention.
- `r2-backup-worker/src/backup/retention.ts` - Backup retention owner.
- `r2-backup-worker/src/restore/gdpr-filter.ts` - Existing restore/deletion filtering behavior.
- `r2-backup-worker/wrangler.toml` - Existing scheduled worker bindings and cron configuration.
- `r2-backup-worker/package.json` - Worker test/deploy commands.
- `src/components/practice/ListeningPracticeView.tsx` - Solo/homework Listening runtime owner.
- `src/components/practice/ListeningPracticeView.test.tsx` - Solo/homework runtime regression tests.
- `src/components/test/mobile/MobileListeningExamScaffold.tsx` - Student mobile Listening shell.
- `src/components/test/mobile/MobileListeningExamScaffold.test.tsx` - Mobile shell tests.
- `src/components/test/mobile/mobileListeningState.ts` - Solo/homework mobile playback persistence owner.
- `src/components/test/mobile/mobileListeningState.test.ts` - Mobile state tests.
- `src/skills/listening/components/AudioPlayer.tsx` - Shared Listening playback component; protected for live/runtime work.
- `src/skills/listening/components/AudioPlayer.test.tsx` - Playback, source, and synchronization tests.
- `src/skills/listening/components/ListeningTestPage.tsx` - Live Listening student runtime owner.
- `src/__tests__/integration/ListeningTestPage.test.tsx` - Live Listening integration test anchor.
- `src/pages/TestPageRouter.tsx` - Student/live test runtime router.
- `src/pages/TestPageRouter.test.tsx` - Runtime routing tests.
- `src/pages/TeacherTestMonitorPage.tsx` - Teacher live-monitor and authority shell.
- `src/pages/TeacherTestMonitorPage.test.tsx` - Teacher monitor tests.
- `src/components/test/AudioProgressPanel.tsx` - Rich teacher audio-state UI and broadcast owner.
- `src/components/test/TeacherTestControlBar.tsx` - Teacher monitor action bar.
- `src/components/test/HeadphoneRequestPanel.tsx` - Headphone readiness approval/denial UI.
- `src/hooks/monitor/useMonitorControls.ts` - Teacher live-session command writer.
- `src/hooks/monitor/useMonitorControls.test.ts` - Monitor command tests.
- `src/hooks/audio/useMasterAudioState.ts` - Master audio-state synchronization owner.
- `src/hooks/audio/useAudioSync.ts` - Student drift-correction owner.
- `src/config/featureRegistry.ts` - Visible action/page observability registry.
- `src/config/featureRegistry.test.ts` - Registry coverage tests.
- `src/components/results/ReviewTab.tsx` - Existing saved-result review surface relevant to Listening audio compatibility.
- `src/components/results/ReviewTab.test.tsx` - Result review regression tests.
- `src/components/results/ResultSlidePanel.tsx` - Existing result detail/review shell.
- `src/components/results/ResultSlidePanel.test.tsx` - Result detail/review tests.

### Notes

- Follow `documentation/tasks/process-task-list.md` while implementing. Complete tasks in order unless this tasklist explicitly permits parallel work.
- Create the findings file before implementation. Keep it strictly append-only: append after each completed subtask; never edit, remove, reorder, or combine prior entries.
- Corrections to findings are appended as new entries that supersede earlier entries; prior entries are never mutated. Traceability matrix is the mutable status/evidence surface.
- PRD-0055 authorizes planning, not one broad implementation patch. Task 1.12 approval was recorded on 2026-06-20. No implementation task may start in the same run that completes Task 1.
- Task 3 may proceed only in a separate explicitly scoped and approved run because it preserves presentation-only boundaries and reconciles existing shared work. Task 1 approval does not authorize Task 3 or bypass storage, authoring, runtime, or child-specific gates.
- Execution order is not strict numeric order after the Task 1 planning STOP. Any later task requires separate explicit scope and authorization. Backbone dependency order remains Task 2 -> Task 4 -> Task 5, but no task is automatically authorized or automatically next.
- Tasks 2 and 4 through 8 are provisional scaffolding. Before each starts, regenerate/reconcile its subtasks against the approved child PRD. If the child PRD differs, the child PRD wins and this tasklist must be rewritten before implementation.
- OQ-1 through OQ-4 were formally approved on 2026-06-19 under `PRD-0055-PACKET-1B-OQ-APPROVAL-2026-06-19`. Junior developers must follow the approved binding decisions and must not alter them silently.
- OQ-1 binding decision: no Google Drive migration, current playback removal, new behavior, or new Google Drive-specific error state; upload-code removal and disposition/deletion of Google Drive-backed tests occur in a separate task. OQ-1 does not block Task 3.
- OQ-3 binding decision approves urgent severable S0 child-PRD planning only. Keep S0 narrow: authentication, ownership, prefix/raw-key validation, CORS, rate limits, tests, deploy, and rollback. Canonical-worker selection remains unresolved until the S0 child PRD.
- Confirmed deployment drift must be resolved before S0 design: checked-in `cloudflare/worker.js` uses `aws4fetch` plus S3 credentials, while `documentation/SOP/R2_WORKER_UPDATE_GUIDE.md` documents deployed `r2-upload-signer` code using native `env.R2_BUCKET`. No upload-worker `wrangler.toml`, checked-in deploy command, or checked-in rollback command currently exists.
- Private-delivery order is fixed: Task 6 builds server issuance/range support and result-review integration while solo/live stay public; Task 7 may cut solo only through a proven host/adapter path without `AudioPlayer` internal edits; Task 8 owns every `AudioPlayer` internal refresh/source-handoff change and the live cutover.
- Rollout is phase-local, not deferred to Task 9: Task 5 produces real draft/publish traffic before Task 6 reconciliation; Tasks 6, 7, and 8 each run internal/selected cohorts before their dependents or final rollout.
- File-architecture anti-goal: do not reproduce the Reading V2 monolith pattern in Listening. New behavior must be born in small single-responsibility modules behind thin existing facades; safe-edit protocols for large files do not authorize growing them.
- Existing shared components/adoptions must be reconciled and tested before being marked complete. Current dirty/untracked state is not completion evidence.
- Google Drive handling is outside this tasklist. Do not add, remove, migrate, test as new supported behavior, or change existing Google Drive playback. Separate cleanup/deletion task owns it.
- Root tests use Vitest, not Jest. Use focused commands such as `npx vitest run <files> --reporter=basic`.
- Browser, deployment, multi-browser, iOS Safari, and live-session proofs are human-assisted gates. Agent must prepare commands/fixtures/evidence template and stop when human access or approval is required.
- Every Playwright run must emit JSON: `npx playwright test <optional-spec> --reporter=json > report.json`. Preserve `report.json` or record its artifact path in findings before another run overwrites it.
- Worker tests use the owning worker package command after its test harness is defined. `r2-backup-worker` uses `npm --prefix r2-backup-worker test`.
- Before Firebase path/rule work, read `documentation/rules/infrastructure.md`; before modifying producer/consumer storage shapes, read `documentation/rules/codebase-hygiene.md`.
- Before UI/action work, read `DESIGN.md`, `documentation/architecture/ui-design-standards.md`, `documentation/rules/observability.md`, and `documentation/rules/announcements.md`.
- Each implementation parent lands as one or more independently reviewable and independently revertible packets/PRs. Parent completes only after every packet is merged, tested, traceable, and has rollback evidence. Approved child PRD defines packet boundaries; one mega-commit per parent is prohibited.
- Serialize merge-conflict hotspots: Task 3 `ListeningTestBuilder.tsx` presentation work lands before Task 5 authoring behavior work; `listeningTestStorage.ts` write-model packets do not overlap; Task 7 does not edit `AudioPlayer.tsx` internals before Task 8.
- Every completed subtask/parent must update its traceability rows with implementation status and verification evidence. A checked task without updated traceability is incomplete.
- Stop if implementation needs a storage path, schema, authority owner, migration rule, or runtime behavior not fixed by PRD-0055 or an approved child PRD. Record the gap; do not invent it.
- Spark-tier is intentional for project `temp-a1437`. Any trusted backend need that cannot run on Firebase Spark, including production authoring mutations, backend secrets, or scheduled/reconciliation work, routes to the approved Cloudflare Worker alternative unless the product owner explicitly approves Blaze/Firebase Functions.

### Verification Standard

This standard applies to every numbered subtask. A checkbox may change to `[x]` only when its own evidence record satisfies this standard; parent acceptance cannot retroactively prove incomplete subtasks.

Per-subtask Definition of Done:

1. Preconditions and declared scope are satisfied.
2. Required output exists in the named file/path.
3. Exact acceptance claims are listed as binary statements in findings.
4. Focused verification names the tests/checks that prove each claim.
5. Traceability rows are updated with status, verification technique, and artifact links.
6. Diff audit shows no undeclared files/regions or unrelated behavior changes.
7. Any blocker, partial result, or deferred requirement remains unchecked and is recorded explicitly.

Behavioral code/data/contract subtasks additionally require:

1. Characterization or target test written/identified before production edit.
2. RED evidence captured: exact command, failing test name, failure count, and expected failure reason.
3. GREEN evidence captured: exact command, passing test names/counts, and relevant output.
4. Mutation proof captured:
   - temporarily break the exact claimed behavior using a local test-only/source mutation;
   - run the focused test and record the expected failure;
   - restore the production file byte-exact or prove the intended diff is restored;
   - rerun the focused test and record GREEN;
   - never perform mutation proof against deployed production data or irreversible infrastructure.
5. Browser/deploy proof added when unit/integration tests cannot prove the claim.

Required evidence schema for each completed subtask in the append-only findings file:

```text
Subtask:
Claims proven:
Files and declared touch regions:
Lines before -> after and responsibility delta:
Created/preserved decomposition seams:
Traceability row IDs:
Characterization/baseline:
RED command and result:
GREEN command and result:
Mutation proof and restoration evidence:
Static/boundary/diff checks:
Browser/deploy artifacts:
Residual risks or deferred items:
Verifier and verification outcome:
```

For documentation/planning-only subtasks, mark RED/GREEN/mutation as `not applicable - non-behavioral` and provide structural checks, contradiction scans, source citations, and independent review evidence instead.

Independent verification:

- Tasks 2, 4, 5, 6, 7, and 8 each require a fresh-context verification pass after implementation and before parent acceptance.
- Verifier must not be the same implementation pass. Use a separate agent/thread or human reviewer with only the approved PRD/child PRD, traceability rows, comprehension maps, diff, and evidence packet.
- Verifier must search for omitted PRD details, inspect undeclared files/regions, rerun focused tests, rerun mutation proofs, confirm protected boundaries, and challenge claimed deferrals.
- Implementer fixes findings, updates evidence, and obtains a clean re-review before parent completion.

### Large-File Protocol

Trigger:

- Any human-maintained source, test, or style file with 800 or more lines.
- Any named file below, regardless of current line count:
  - `src/skills/listening/builders/ListeningTestBuilder.tsx`
  - `src/skills/listening/components/ListeningTestPage.tsx`
  - `src/skills/listening/components/AudioPlayer.tsx`
  - `src/components/practice/ListeningPracticeView.tsx`
  - `src/services/listeningTestStorage.ts`
  - `src/components/test/AudioProgressPanel.tsx`
  - `src/pages/TeacherTestMonitorPage.tsx`
  - `src/components/reading-v2/studio/ReadingV2BuildWorkspace.tsx`
  - `src/components/reading-v2/studio/ReadingV2StudioShell.tsx`
  - `src/components/reading-v2/runtime/ReadingV2RuntimeShell.tsx`
  - `src/components/reading-v2/studio/ReadingV2TableCompletionBuilder.tsx`
  - `src/components/reading-v2/studio/ReadingV2TaskGroupEditor.tsx`

No edit or boundary-verification claim for a triggered file may begin until:

1. Full-read evidence:
   - read every line/page end-to-end;
   - record `lines read N / total N`, command/tool, and completion timestamp;
   - partial read is a hard stop.
2. Structural comprehension map:
   - create/update `tasks/large-file-maps-0055/<path-slug>.md`;
   - map every export and top-level symbol;
   - map every state variable;
   - map every `useEffect` and dependency list;
   - map every side-effecting network, Firebase, R2, audio, storage, navigation, timer, and session call;
   - map branch conditions and their line ranges;
   - identify callers/consumers relevant to the declared change.
   - record proposed future decomposition seams: named regions/responsibilities that should become separate modules under a later approved refactor PRD.
3. Doc-vs-code parity:
   - compare PRD, audit, architecture docs, implementation log, and current code;
   - record matching claims, drift, and which source wins before editing.
4. Characterization baseline:
   - add or identify tests pinned to the exact current behavior of the region being changed;
   - run GREEN before production edit;
   - if no focused test can characterize it, stop and add one first.
5. Declared touch region:
   - record exact functions/symbols and pre-edit line ranges allowed to change;
   - record protected neighboring functions/regions that must not change.
6. Surgical edit:
   - use verified-unique anchors and scoped patches;
   - never overwrite/rewrite the whole large file;
   - never reflow or format unrelated regions;
   - decomposition is allowed only inside the active seam, behind green characterization tests, and only when it reduces risk for the approved change.
7. Intra-file diff audit:
   - inspect every changed hunk;
   - prove each hunk belongs to the declared touch region;
   - record unexpected formatting/reflow as failure and restore it.
8. Context-budget guard:
   - check remaining context before loading a 3,000-plus-line file;
   - when context is insufficient, use a dedicated read-only exploration/sub-agent pass to produce the full comprehension map;
   - editing pass must still fully read and understand every region it edits, validate the map against current bytes, and cannot rely blindly on another agent's summary.

Large-file evidence is linked from the subtask findings record and traceability matrix. Missing map, partial read, absent characterization test, or undeclared changed hunk blocks completion.

### File-Architecture Principles (MANDATORY)

Anti-goal: do not recreate the Reading V2 large-file pattern represented by `ReadingV2BuildWorkspace.tsx`, `readingV2ImportNormalization.service.ts`, and `ReadingV2StudioShell.tsx`. Healthy reference is `src/features/assessment/shared/components/`: one responsibility per file, focused tests, explicit boundaries, and small composition surfaces.

1. New behavior is born in new single-responsibility modules, not appended to `src/services/listeningTestStorage.ts`, `src/skills/listening/builders/ListeningTestBuilder.tsx`, or `src/services/r2Storage.ts`.
2. Those existing files remain public facades/orchestrators. They may receive narrow imports, delegation calls, wiring, and compatibility adapters, but must not gain a new domain responsibility. Findings record `lines before -> after`, responsibilities before/after, and justification for every net increase.
3. Default feature home follows PRD section 9: new Listening authoring, storage, audio/media, adapter, and type modules live under an approved coherent bounded package, preferably `src/features/assessment/listening/**`. A child PRD may select an existing Listening root only when it keeps one coherent package and does not create a third scattered ownership root.
4. Soft size budget: each new human-maintained production source file targets 400 lines or fewer. Exceeding 400 lines requires written responsibility analysis, split alternatives, and reviewer approval in findings. Generated artifacts and large declarative fixtures are excluded but still require focused ownership.
5. Extract-on-touch: when a triggered large file must be edited to add behavior, default action is to place new behavior in a new module behind green characterization tests and add only thin delegation in the large file. Appending behavior in place requires explicit child-PRD justification and architecture-review approval.
6. Dependency direction for new feature modules is `listening -> shared`. New Listening modules never import Reading V2 internals and must not create cycles back through facade internals.
7. Task 3.4 CI enforces dependency direction and size-budget reporting for new/changed assessment feature modules. CI is guardrail, not substitute for child-PRD ownership review.
8. Every parent touching a named monolith records created/preserved extraction seams, before/after line counts, and responsibility deltas in its comprehension map, findings, and traceability rows. Future refactor PRDs inherit these maps.

## Tasks

- [x] 1.0 Resolve PRD-0055 approval gates and establish child-PRD execution boundaries
  - [x] 1.1 Read `AGENTS.md`, PRD-0055, all four unification docs, canonical unification architecture, upload-storage authority, `process-task-list.md`, and every triggered rule document before implementation.
  - [x] 1.2 Create `tasks/findings-of-tasks-0055-prd-ielts-reading-v2-listening-unified-assessment-platform.md` with sections for working tree, decisions, current owners, completed subtasks, verification, blockers, rollback notes, and next subtask.
  - [x] 1.3 Record `git status --short`, current branch/upstream, tracked/untracked state of PRD/shared-component files, and existing unrelated user changes. Do not clean, stage, revert, or absorb unrelated work.
  - [x] 1.4 Re-run current-state inspection proving: Listening save hard-blocks missing audio; save writes `isPublished: true`; no Listening draft lifecycle exists; current R2 service path is `src/services/r2Storage.ts`; live audio remains dual-path; existing shared primitives/adoptions match the implementation log.
  - [x] 1.5 Present OQ-1 through OQ-4 to the product owner exactly as written in PRD section 29 and record explicit approval/revision for each:
    - OQ-3 worker security severability first because it is a live security issue.
    - OQ-1 Google Drive scope: recommendation already settled in conversation; obtain formal sign-off and do not treat it as a new product decision.
    - OQ-2 legacy published transition.
    - OQ-4 public/private R2 transition.
  - [x] 1.6 Update PRD-0055 from Draft to Approved only after all four questions are resolved using non-destructive documentation history: retain each provisional `Proposed under OQ-*` statement, mark it obsolete in a blockquote with the approval date/decision reference, append the approved binding text, and run contradiction scans. Do not erase or silently rewrite prior decision history.
  - [x] 1.7 Create separate child PRDs, using the next available task numbers at creation time, for:
    - urgent upload-worker security S0 if OQ-3 approves it; this child PRD must first decide whether checked-in `aws4fetch`/S3 code or deployed native `env.R2_BUCKET` code is canonical, then capture worker name, non-secret binding/secret names, exact deploy mechanism, exact rollback/version-pin mechanism, and mechanism-matched test harness;
    - approved Packet 1J bridge PRD-0056A between S0 and storage lifecycle, owning backend-issued upload sessions/assets and the temp-prefix transition;
    - Listening authoring draft/publish/version behavior;
    - R2 asset lifecycle, registry, reconciliation, cleanup, and delivery;
    - Listening solo/homework runtime alignment;
    - Listening live-session authority/runtime and load-test plan;
    - Reading V2 runtime visual alignment, explicitly deferred until shared authoring stability and dedicated Reading V2 runtime tests exist.
  - [x] 1.8 Require each child PRD, including approved bridge PRD-0056A, to list exact owned files, protected files, data paths, schemas, allowed changes, prohibited changes, tests, browser proof, rollback, observability, and stop conditions. Each child PRD that adds Listening behavior must define its coherent bounded module home, facade boundaries, allowed dependency direction, per-file size budget, before/after line-count evidence, and decomposition seams under the File-Architecture Principles. Storage/delivery child PRD must define delivery-read authorization from retained reference/assignment/result access and cross-user issuance denial. Do not allow placeholders or "developer decides."
  - [x] 1.9 Create `tasks/traceability-0055-prd-ielts-reading-v2-listening-unified-assessment-platform.md` before finalizing child PRDs:
    - one row for every PRD functional requirement, including lettered requirements;
    - one row for every section 17 edge case;
    - one row for every section 19 data/storage constraint and required lifecycle/security item;
    - one row for every section 20 accessibility requirement;
    - one row for every section 21 mobile/desktop requirement;
    - one row for every section 25 success metric;
    - one row for every section 26 acceptance criterion;
    - one row for every section 27 regression-checklist item;
    - one row for every section 29 open question and decision-register item;
    - columns: requirement ID, exact summary, owning parent/subtask, owning child PRD, implementation status, verification technique (`test`, `RED/GREEN`, `mutation`, `browser`, `deploy`, `parity`, `diff audit`, or approved combination), verification evidence/artifact links, and explicit defer reason/approval where applicable;
    - during child-PRD drafting, `tasks/<draft-id> (pending finalization)` is a valid interim owning-child-PRD value; `none`, blank owner, vague `later`, or unmapped rows remain hard failures;
    - before Task 1.11, every interim draft owner must resolve to a finalized child PRD or a named product-owner-approved deferral;
    - every explicit deferral names its future PRD/task, entry gate, and product-owner approval;
    - update the matrix whenever a child PRD or this tasklist changes.
  - [x] 1.10 Define dependency order in findings, traceability matrix, and child PRDs:
    - Task 1.12 approval/HARD STOP was satisfied on 2026-06-20 by separate explicit product-owner and architecture/security reviewer approvals. This completes planning only and does not authorize any implementation node.
    - Task 3 neutral shared presentation may proceed only in a separate explicitly approved run under its existing display-only scope and may then parallelize with separately approved S0 or live-contract test preparation.
    - PRD-0056 S0 canonical Worker/deploy/rollback/harness implementation and deployed/current proof precede PRD-0056A reliance on secured upload/move behavior.
    - PRD-0056A is the mandatory bridge between PRD-0056 and PRD-0058; no direct PRD-0056 -> PRD-0058 implementation edge exists.
    - PRD-0056A precedes minimum PRD-0058 registry-backed lifecycle: commit, references, immediate discard cleanup, fallback cleanup, backup/restore coverage, and orphan metrics.
    - no audio-bearing Save draft may ship until that minimum PRD-0058 foundation is accepted.
    - PRD-0057 authoring write-model work waits for approved B2 plus Task 3 shared-presentation stability and minimum PRD-0058 foundation.
    - PRD-0058 advanced reconciliation, result-review private delivery, and issuance/range proof follow minimum storage; Task 5 selected-teacher traffic must exist before Task 6 reconciliation conclusions.
    - PRD-0059 solo runtime waits for Task 5 authoring stability; solo private cutover waits for Task 6 issuance/range/result proof and never modifies `AudioPlayer` internals.
    - PRD-0060 live runtime waits for dedicated authority contract tests and test harness; Task 8 owns shared `AudioPlayer` internal refresh/source-handoff and live cutover.
    - PRD-0061 Reading V2 runtime waits for Task 3 shared-authoring stability and dedicated Reading V2 runtime tests.
    - Task 9 full rollout waits for all applicable phase-local acceptance gates.
  - [x] 1.11 Parent acceptance: PRD status/decision text is internally consistent, child PRDs exist, dependency graph is recorded, traceability is 100 percent with zero orphan requirements, every interim draft owner is resolved to a finalized child PRD or named approved deferral, every deferral is named/approved, no implementation ambiguity is delegated to a junior developer, and document checks pass.
  - [x] 1.12 HARD STOP: presented OQ decisions, draft child PRDs, complete traceability matrix, dependency graph, and Task 1 evidence to the product owner plus architecture/security reviewer. Separate explicit approvals were recorded on 2026-06-20 under `PRD-0055-TASK-1.12-PRODUCT-OWNER-APPROVAL-2026-06-20` and `PRD-0055-TASK-1.12-ARCHITECTURE-SECURITY-APPROVAL-2026-06-20`. End the run. Do not continue into Task 2 or any later implementation task without separate explicit authorization.

- [x] 2.0 Ship urgent severable upload-worker security hardening gate S0
  - [x] 2.1 Record explicit product-owner plus architecture/security reviewer sign-off in findings. Reconcile/regenerate this provisional scaffold against the approved S0 child PRD; child PRD wins. Do not continue on mismatch.
  - [x] 2.2 Resolve canonical upload-worker and deployment truth before code changes:
    - read `documentation/rules/infrastructure.md`, `documentation/SOP/R2_WORKER_UPDATE_GUIDE.md`, checked-in `cloudflare/worker.js`, and deployed `r2-upload-signer` source/configuration;
    - record deployed worker name, route/domain, native bindings, secret names without values, deployment history, and current rollback mechanism;
    - decide whether canonical implementation uses checked-in `aws4fetch`/S3 credentials or native `env.R2_BUCKET`;
    - record exact source-of-truth, deploy, version-pin, and rollback procedure;
    - if Wrangler becomes canonical, record that approved `cloudflare/wrangler.toml` must be checked in during the approved harness/implementation subphase before deploy; if dashboard deployment remains canonical, document exact dashboard deployment/version rollback steps;
    - stop if deployed source/configuration cannot be obtained or reconciled.
  - [x] 2.3 Establish a checked-in test harness only after 2.2 resolves mechanism. For `aws4fetch`/S3 style, use focused fetch/signing mocks. For native `env.R2_BUCKET`, use Miniflare or `@cloudflare/vitest-pool-workers` and add the required dev dependency/config. Restore/create `cloudflare/package.json` consistently, add focused worker tests, and document exact local command. Treat harness/bootstrap as a non-trivial S0 subphase.
  - [x] 2.4 Write negative tests first and prove insecure-baseline failures for:
    - missing/invalid Firebase identity;
    - cross-owner upload;
    - cross-owner move;
    - arbitrary `sourceKey`/`destKey`;
    - forbidden prefixes;
    - wildcard/unapproved CORS origin;
    - unsupported method;
    - over-limit upload request;
    - replayed or expired authorization.
  - [x] 2.5 Reuse or extract the verified Firebase-token pattern from `r2-backup-worker/src/auth/firebase-auth.ts` only if compatible with the upload worker runtime; do not copy service secrets or introduce client-trusted ownership.
  - [x] 2.6 Require Firebase authentication on every non-`OPTIONS` upload/move route; use verified token `sub` as owner identity; reject browser `ownerId`, `uid`, email, and role authority; and reject cross-owner operations before R2 access. Browser-supplied raw keys must never select or authorize an R2 operation. Packet 2I integrated Task 2.6/2.7/2.9 proof establishes full raw-key non-authority and closes the approved checkpoint exception.
  - [x] 2.7 Restrict operations to child-PRD-approved prefix families and server-derived canonical owner/path structure; reject traversal, alternate encoding, absolute paths/URLs, duplicate separators, control characters, unsupported/forbidden prefixes, cross-prefix movement, and overwrite outside allowed scope. Task 2.7 implementation is explicitly permitted while Task 2.6 remains provisionally incomplete.
  - [x] 2.8 Replace wildcard CORS with exact approved production and localhost origins; return correct preflight headers only to allowed origins. Task 2.8 may proceed after Task 2.7 focused proof while Task 2.6 remains provisionally incomplete.
  - [x] 2.9 Issue and verify opaque upload/move grants; bind UID, operation, canonical source/destination, content type, size, expiry, and nonce; treat browser `key`, `sourceKey`, and `destKey` only as optional non-authoritative assertions; enforce expiry, tamper/replay rejection, request/rate controls, and the 50 MB per-request/per-file ceiling. Task 2.9 may proceed after Task 2.8 focused proof while Task 2.6 remains provisionally incomplete. Full raw-key non-authority is satisfied only after Tasks 2.7 and 2.9 integrate with Task 2.6 authentication/owner scope; then Task 2.6 may be checked and normal strict order resumes at Task 2.10. Do not implement the 10-files-per-test rule here because S0 has no authoritative test-level state; Task 4 upload-session/application logic owns that rule. Do not implement registry, heartbeat, deletion, cleanup, or private-delivery behavior in S0.
  - [x] 2.10 Run the negative suite after hardening and prove all authorization/prefix/CORS cases pass locally. Packet 2J-R supersedes the original Packet 2J case-22 log-secrecy proof: the corrected case uses valid sentinel Firebase auth, captures the actual issued grants/URLs/canonical key, and reaches a controlled post-verification R2 failure. Its targeted log-leak mutation fails exactly case 22; restored local and clean-copy proof is 22/22, full Worker GREEN is 120/120, and the insecure baseline remains 18 expected RED/four safe. Task 2.10 remains checked only after this corrected proof passed.
  - [x] 2.11 Human-assisted deploy gate: deploy only with explicit approval through the exact mechanism captured in 2.2. Pre-deploy prerequisite Packet 2K records the approved SQLite Durable Object replay ledger decision and local-only `UPLOAD_GRANT_REPLAY_LEDGER` implementation. Packet 2L records Option A local adapter/canary readiness only: thin browser facade, authenticated grant client, `VITE_R2_UPLOAD_WORKER_URL`, and dry-run-only `r2-upload-signer-s0-canary` config. Packet 2M records approved Phase A canary provisioning only: `r2-upload-signer-s0-canary` deployed to workers.dev with required `UPLOAD_GRANT_SECRET`, `UPLOAD_GRANT_REPLAY_LEDGER`, `UPLOAD_RATE_LIMITER`, `R2_BUCKET`, `PUBLIC_URL`, and `FIREBASE_PROJECT_ID` verification; production `r2-upload-signer` remained on version `20dd8429-5be1-4105-baed-f6dc5af68098`. Packet 2O Phase C readiness audit recorded production rollout blockers. Packet 2P production config prep replaces only production `UPLOAD_RATE_LIMITER.namespace_id` with dedicated integer string `205512`; bundled-x64 local proof passed Worker 129/129, hardened 22/22, immutable insecure baseline 18 expected RED/four safe, and production Wrangler dry-run with all five required non-secret bindings. Packet 2Q Phase C bridge rollout closure records live Hosting-to-canary proof, `SAFE_CANARY_HOSTING_VERSION_ID=485aefde01ee7133`, production `UPLOAD_GRANT_SECRET` by name only, production Worker version `11af545a-479b-4063-a899-d475dd57d2b5` at 100% with migration `v1-upload-grant-replay-ledger` and rate namespace `205512`, live Hosting version `05cb152a2932b261` pointing to `https://r2-upload-signer.iamhuwng.workers.dev`, deployed negative probes, authorized production browser upload/move/content proof, unique proof-object cleanup, historical rollback target capture, and Firebase token-exposure containment. Packet 2R supersedes the old Worker rollback target `20dd8429-5be1-4105-baed-f6dc5af68098` as invalid after the Durable Object migration; Hosting rollback targets remain separately recorded but do not solve Worker DO migration rollback. Version-pin proof, final S0 acceptance, and independent review remain later gates. Verify configured bindings/secrets by name, deployed negative tests, and one authorized upload/move path without exposing token values, signed URLs, or raw keys in logs. Any Playwright/browser proof uses `--reporter=json > report.json`.
  - [x] 2.12 Document and drill rollback using the real mechanism: preserve previous deployment version, identify Wrangler rollback/version command or exact dashboard version-pin steps, restore that version in a non-destructive proof, and verify rollback does not delete or move existing audio. Packet 2R records the first rollback drill as BLOCKED because Cloudflare rejected pre-S0 Worker version `20dd8429-5be1-4105-baed-f6dc5af68098` with API code `10210` after the S0 Durable Object migration changed from empty migration state to `v1-upload-grant-replay-ledger`. Packet 2S records the required post-migration rollback strategy: create or identify a rollback-compatible S0 recovery Worker version with the same Durable Object migration/resource shape and bindings as current S0, activate it to 100%, verify it, restore hardened S0 version `11af545a-479b-4063-a899-d475dd57d2b5` to 100%, verify it, and keep Hosting rollback separate. Packet 2T created non-active recovery Worker version `959065cd-8399-4000-b479-d8303a2f18ad` with the same S0 migration/resource shape; active production remained `11af545a-479b-4063-a899-d475dd57d2b5` at 100%. Packet 2U activated recovery version `959065cd-8399-4000-b479-d8303a2f18ad` to 100%, verified required bindings, restored hardened version `11af545a-479b-4063-a899-d475dd57d2b5` to 100%, verified required bindings, and recorded that no Worker code deploy, Hosting deploy, secret mutation, R2 object mutation, Firebase auth mutation, or push occurred.
  - [x] 2.13 Update upload-storage authority, implementation log, and findings with exact deployed behavior and remaining lifecycle gaps. Packet 2V records read-only production truth after Task 2.12: active `r2-upload-signer` version `11af545a-479b-4063-a899-d475dd57d2b5` at 100%, rollback-compatible recovery version `959065cd-8399-4000-b479-d8303a2f18ad`, pre-S0 Worker version `20dd8429-5be1-4105-baed-f6dc5af68098` historical only after Durable Object migration `v1-upload-grant-replay-ledger`, and remaining lifecycle gaps for registry-backed cleanup/delete/lifecycle configuration. At Packet 2V close, parent Task 2.0 remained unchecked; Task 2.14 was later checked by Packet 2W; Task 2.15 remained unchecked until Packet 2X.
  - [x] 2.14 Run the mandatory fresh-context independent verification pass from the Verification Standard. Packet 2W records a clean independent review with no findings after inspecting S0 child-PRD coverage, Worker diff, authorization boundaries, raw-key non-authority, exact-origin CORS, opaque grants, replay ledger, rate limits, deployed proof, rollback drill, Task 2.13 docs truth, local proof reruns, and remaining lifecycle gaps. At Packet 2W close, parent Task 2.0 and Task 2.15 remained unchecked until Packet 2X.
  - [x] 2.15 Parent acceptance: local and deployed cross-owner/raw-key/CORS tests pass, authorized existing upload/move still works, no lifecycle behavior changed, independent verification passes, and S0 is independently revertible. Packet 2X records final acceptance: hard gates passed at HEAD `1c988cdb12b3e733875b82ad74ae34b51b66d97d`; local Worker proof passed 129/129, hardened negative proof passed 22/22, insecure baseline stayed exact at fixture SHA `93e046d0986811a2c91c3ceb7b48bca7215f75064153cff370750d5e2776a05c` with 18 expected RED failures and four already-safe passes, and browser adapter proof passed 33/33. Read-only production Worker proof kept `r2-upload-signer` on deployment `0c0bca87-6bca-4a42-934d-509299b7e3c9`, version `11af545a-479b-4063-a899-d475dd57d2b5`, `100%` traffic, with required S0 bindings/migration; recovery version `959065cd-8399-4000-b479-d8303a2f18ad` remains same-shape and independently revertible. Deployed proof ID `prd0055-task215-prod-1782401801998-f1c47dc9eed2` returned no-auth authorize `401`, evil-origin preflight `403`, allowed localhost preflight `204`, valid-token cross-owner upload `403`, raw-key-only move `400`, authorize/upload/move/public content `200`, matching content SHA-256 `f7ea8ae7cb25e72717555eef78234d00f1e98a3c1c868d4036d9009614288b71`, destination cleanup `200`, public/API source and destination rechecks `404`, and zero remaining Task 2.15 proof objects. No lifecycle behavior, Worker code, Worker traffic, secret, Firebase Hosting, Firebase auth, existing R2 object, push, or Task 3 state changed.

- [x] 3.0 Complete neutral shared assessment authoring primitives and safe cross-module visual adoption
  - [x] 3.1 Reconcile existing uncommitted/tracked foundation against implementation log:
    - `AssessmentStatusState`;
    - `AssessmentValidationSummary`;
    - `AssessmentAuthoringSection`;
    - Reading V2 Studio/Settings adoptions;
    - Listening Questions/empty-state adoptions.
    - Treat current source/tasklist paths as authoritative where the implementation log has drift. Record, but do not copy, stale `src/hooks/useMasterAudioState.ts` / `src/hooks/useAudioSync.ts` references; current owners are under `src/hooks/audio/`. Record duplicate Patch 2/Patch 3 headings as documentation drift.
  - [x] 3.2 Run existing focused tests and boundary grep before marking any foundation subtask complete. Record failures or drift rather than recreating components.
  - [x] 3.3 Track/commit the existing shared primitives and current Reading/Listening adoptions in a focused green patch before adding another primitive. Do not build new shared work on an uncommitted or failing foundation.
  - [x] 3.4 Add a low-cost CI guardrail before additional shared extraction:
    - create `.github/workflows/assessment-unification-guardrails.yml`;
    - fail on prohibited Reading V2/Listening imports or module-specific authority symbols under `src/features/assessment/shared/` production source and local CSS;
    - enforce new `src/features/assessment/listening/**` dependency direction: Listening may import neutral shared contracts, never Reading V2 internals, and never create a cycle through `ListeningTestBuilder.tsx`, `listeningTestStorage.ts`, or `r2Storage.ts`;
    - report new/changed human-maintained assessment production files over the 400-line soft budget and fail when required structured findings evidence is absent;
    - require each 400-line exception to use this exact per-file findings record; `line-count` must equal the current measured logical line count and `status` must be exactly `approved`:
      ```text
      <!-- assessment-line-budget-exception
      path: src/exact/production-file.ts
      line-count: 401
      responsibilities: exact responsibility one; exact responsibility two
      split-alternatives: exact split option one; exact split option two
      rejection-reason: exact split option one => why that split is rejected; exact split option two => why that split is rejected
      approver: Approver Name
      approver-role: Independent Architecture Reviewer
      status: approved
      -->
      ```
    - guardrail validates the exact structured record mechanically; human review still owns reviewer authenticity, technical truth, and approval;
    - exclude deterministic generated artifacts and declarative fixtures from 400-line enforcement only when the explicit path or top-of-file header matches the narrow allowlist (`/fixtures/`, `/__fixtures__/`, `.generated.` filename, or a standard generated/do-not-edit header near file start); markers deep in file content do not bypass the check and human review still owns focused ownership for every file;
    - document the exact local equivalent as either an explicit `--changed-files` list or the branch-aware `GITHUB_BASE_REF` path; the local default remains working-tree/last-commit convenience only and does not claim arbitrary multi-commit branch coverage;
    - run focused Vitest suites for `src/features/assessment/shared/**`;
    - optionally annotate changes to protected live/storage files for reviewer attention without pretending annotation replaces child-PRD gates;
    - document exact local equivalents and prove CI fails under one temporary prohibited-import/test mutation, then restore and prove green.
  - [x] 3.5 Define one small next neutral primitive only when Reading V2 and Listening both have immediate same-PR or explicitly adjacent-PR consumers. Candidate order:
    - authoring card;
    - authoring header;
    - action row;
    - metadata display panel;
    - review/publish display wrapper;
    - question-card wrapper;
    - mobile layout primitive for neutral spacing, stacking, and touch-target sizing.
    - Task 3.5/3.6 candidate-selection packet selected `authoring header` as the first safe next neutral primitive candidate after rejecting `authoring card` as too broad or behavior-coupled in current Reading V2 and Listening surfaces. Selected adjacent adopters are Reading V2 Studio mounted panel headers and one non-Step-4 Listening builder step header; title/copy/status/action values remain module-owned. No shared component implementation or adopter patch occurred in this packet.
  - [x] 3.6 For each candidate, document both concrete adopters, existing semantics, heading structure, action placement, mobile behavior, and tests before creating the shared component. If a question-card/mobile primitive lacks two identical neutral contracts, explicitly defer it in the traceability matrix to the named Reading V2/Listening runtime child PRD; do not silently omit or speculatively extract it. Task 3.5/3.6 candidate-selection evidence is recorded in the findings, traceability matrix, implementation log, and architecture note; candidate 3.7+ implementation remains unstarted.
  - [x] 3.7 Write component tests first for children, title/description, heading level, status/action slots, accessible region naming, neutral layout props, and absence of module behavior. Task 3.7 proof added `AssessmentAuthoringHeader.test.tsx` first; RED failed on missing `./AssessmentAuthoringHeader`, then GREEN passed 7/7 after implementation and reviewer-requested falsy-slot coverage.
  - [x] 3.8 Implement the primitive under `src/features/assessment/shared/components/` using local CSS and no new Mantine dependency. Task 3.8 added display-only `AssessmentAuthoringHeader.tsx` plus local CSS only; no Reading V2, Listening, audio, parser, storage, runtime/live, Mantine, adopter, or protected runtime path changed.
  - [x] 3.9 Adopt in one Listening authoring display-only surface without moving parser, validation calculation, audio, persistence, or event handlers. Task 3.9 adopted `AssessmentAuthoringHeader` only in the Listening mode-select display header; parser, validation calculation, audio upload/validation, persistence, navigation, and event handlers remain in `ListeningTestBuilder`.
  - [x] 3.10 Adopt in one Reading V2 authoring display-only surface without moving import normalization, projection, publish workflow, review navigation, or runtime behavior. Task 3.10 adopted `AssessmentAuthoringHeader` only in the Reading V2 Settings panel header; import normalization, projection, publish workflow, review navigation, and runtime behavior remain in their existing owners.
  - [x] 3.11 Reassess `AssessmentValidationSummary` for one Listening branch. Adopt only if heading, ready/blocked status, issue count, copy, and existing behavior remain exact; otherwise record deferral. Task 3.11 reassessed the current Listening authoring branches and deferred adoption because no candidate preserves the exact `AssessmentValidationSummary` contract without changing heading semantics, ready/blocked status, issue count, copy, or behavior ownership. The narrow candidate reviewed was the Review & Save audio-section display; it has heading `Audio Sections`, per-section configured/missing copy, no aggregate ready/blocked status, no issue count, and sits inside editable save workflow ownership. Audio/parsing/save errors and upload/image success displays were rejected as error/progress/success messages rather than existing ready/blocked validation summaries.
  - [x] 3.12 Keep shared copy module-supplied. Shared components must not contain Reading, Listening, audio, passage, parser, storage, teacher, or live-session wording/conditions. Task 3.12 confirmed the existing shared primitives already satisfy the copy boundary: `AssessmentAuthoringHeader`, `AssessmentAuthoringSection`, `AssessmentStatusState`, and `AssessmentValidationSummary` render module-supplied copy/slots, while shared-owned defaults remain neutral semantics such as heading level, status/alert roles, alignment, stacking, and the overrideable `Issues` label. No source/test/runtime change was required.
  - [x] 3.13 Do not create shared answer inputs until two modules prove identical semantic, validation, accessibility, and persistence contracts in a later approved child PRD. Task 3.13 confirmed the existing shared assessment layer remains display-only and contains no shared answer input primitive. Reading V2 answer-rule/edit controls and persistence remain in Reading V2 Studio/runtime owners, Listening answer-key/runtime controls and persistence remain in Listening owners, and no neutral shared answer-input contract exists today. Shared answer inputs remain deferred to a later approved child PRD after two modules prove identical semantic, validation, accessibility, and persistence contracts.
  - [x] 3.14 Handle Listening Mantine `AppShell` removal as a dedicated authoring-shell patch after primitive stability; read codebase hygiene/UI architecture first and do not combine with storage or save-model work. Task 3.14 removed the `@mantine/core` `AppShell` wrapper from `ListeningTestBuilder`, replaced it with a native `<main>`, reduced the touched authoring chrome from gradient/glass styling to a neutral white-card frame, and corrected the mode-select tiles into native `aria-pressed` buttons with phrasing-safe content and scoped transitions. Existing shared primitive adoptions remained unchanged; parser, audio, save, persistence, storage, runtime/live, teacher monitor, Firebase, R2, Cloudflare, production config, deploy, push, and remote state were not changed.
  - [x] 3.15 Run focused component/adopter tests and boundary grep:
    - no imports from Reading V2 internals in shared;
    - no imports from Listening internals in shared;
    - no `audioCommand`, `masterAudioState`, parser, storage, passage, or published-payload behavior in shared.
  - [x] 3.16 Update implementation log and findings after each primitive/adoption patch, including exact tests and deferred candidates. Task 3.15/3.16 closure reconciled the implementation log, findings ledger, traceability matrix, and canonical architecture with current Task 3 source/test truth after focused tests, shared-boundary grep, protected-path scan, taskbox scan, UTF-8, and diff-check proof passed.
  - [x] 3.17 Parent acceptance: CI guardrails are green and mutation-proven for shared boundaries plus new Listening dependency/size rules; each new shared primitive has two real consumers; both modules retain behavior ownership; tests pass; boundary grep is clean; runtime/live/storage files remain untouched. Task 3.17 accepts Task 3 as presentation-only shared assessment work: guardrails passed, guardrail unit proof covers 34 tests, focused shared/adopter proof passed 6 files and 26 tests, boundary/protected scans were clean, no runtime/live/storage/Firebase/R2/Cloudflare behavior changed, and Task 4 remains unchecked.

- [x] 4.0 Establish minimum registry-backed draft-audio retention foundation
  - [x] 4.1 Record explicit product-owner plus architecture/security reviewer sign-off in findings. Reconcile/regenerate this provisional scaffold against the approved storage child PRD plan/text; child PRD wins. Do not continue on mismatch.
  - [x] 4.2 Foundation gate: deployed/current PRD-0056A bridge proof is accepted after S0 on Worker version `3687d2e0-4718-4c0b-9c84-7f81749c31fb`, with current RTDB rules, full deployed proof, cleanup, recovery rehearsal, local/emulator proof, and authority docs updated. Before changing storage ownership, open the Task 4.2 implementation packet, run child-specific review, confirm PRD-0058/traceability/upload-storage-authority/implementation-log dependencies are current, preserve baseline tests for current upload, publish-time promotion, playback, and failure behavior, and confirm the storage child PRD satisfies the File-Architecture Principles: it names the bounded home for registry/upload-session-lifecycle/commit/cleanup/heartbeat/metrics modules and keeps `r2Storage.ts` plus `listeningTestStorage.ts` as thin compatibility facades.
    - Closure: Task 4.2 completed as a foundation gate/baseline packet only. Added `src/services/listeningTestStorage.test.ts` for current publish-time audio/stream promotion and failure fallback baselines; tightened `AudioPlayer.test.tsx` media mock cleanup; reran existing upload/playback/Worker bridge baselines; updated PRD-0058 module homes for upload-session-lifecycle, heartbeat, and metrics. No registry/rules/Worker/Firebase/R2 lifecycle implementation started.
  - [x] 4.3 Scope-confirmation gate: verify the approved storage child PRD plan/text includes exactly the minimum capabilities below before implementation starts. PRD-0056A owns backend-issued create-time upload-session/bootstrap identity; PRD-0058 consumes those identities and owns lifecycle fields, registry, commit, references, cleanup, reconciliation, backup/restore, and delivery. If any capabilities are absent or materially different, rewrite Tasks 4.4-4.19 and obtain approval; do not implement from this summary:
    - backend-issued immutable `assetId` from PRD-0056A, consumed by PRD-0058;
    - authenticated owner-scoped upload session bootstrap from PRD-0056A;
    - PRD-0058 lifecycle fields only on upload sessions;
    - trusted registry entry;
    - draft/test reference tracking;
    - idempotent commit;
    - immediate discard cleanup;
    - scheduled/fallback cleanup;
    - backup and restore coverage;
    - orphan/reconciliation metrics;
    - PRD-0056A owns create-time upload-session/bootstrap identity;
    - PRD-0058 owns lifecycle fields, registry, commit, references, cleanup, reconciliation, backup/restore, and delivery;
    - `r2Storage.ts` and `listeningTestStorage.ts` remain compatibility facades.
    - Closure: Task 4.3 completed as a docs/scope gate only. PRD-0058 sections 9-24, 26, 28, 38, and 39 already satisfy the minimum capability set and accepted PRD-0056A split ownership, so no Task 4.4-4.19 rewrite was required. No runtime source, Worker, `database.rules.json`, or deployed-state mutation started; Task 4.4+ remains unstarted.
  - [x] 4.4 Add minimal asset states required for safe draft retention: `temp`, `committing`, `committed`, and `pending-delete`. Do not add fields or paths not specified by the approved storage child PRD plan/text.
    - Closure: Task 4.4 shipped only the minimal state foundation in bounded storage modules under `src/features/assessment/listening/storage/`. The exact approved states now exist as code and tests, checksum metadata is required at registry-record creation time, and cleanup remains fail-closed until restore and integrity proof are both resolved. `r2Storage.ts`, `listeningTestStorage.ts`, PRD-0056A bridge identity/bootstrap ownership, canonical temp-key shape, and public R2 compatibility remained unchanged.
  - [x] 4.5 Ship registry durability with first registry creation:
    - use `src/__tests__/security/prd0040-security.emulator.test.ts` as the emulator-backed pattern; do not treat `firebaseRules.test.ts` as emulator proof;
    - confirm executable RTDB emulator wiring exists through a checked-in `firebase.json` emulator block or `firebase emulators:exec` wrapper before writing registry rule tests; stop if tests cannot prove they run against emulator rules;
    - add child-PRD-approved registry rules/indexes and emulator-backed negative tests proving owner scope, cross-owner denial, browser inability to forge committed/delete state, and separation of teacher/service authority;
    - include registry path in backup coverage;
    - obtain sign-off from the named `r2-backup-worker/` disaster-recovery owner, distinct from the storage implementer/reviewer;
    - implement/test registry restore behavior and complete an end-to-end restore drill: back up a test `media_assets` registry node, wipe the test node, restore it, and verify references/integrity;
    - prove existing scheduled backup cron paths still succeed after the registry coverage change;
    - require checksum metadata for integrity/future analysis while prohibiting checksum-based deduplication in this implementation;
    - prove cleanup remains disabled or fail-closed while registry restore/integrity is unresolved;
    - document restore ordering before any cleanup job trusts the registry.
    - Closure: Task 4.5 added checked-in RTDB emulator wiring in `firebase.json`, `media_assets/**` owner-read/service-write-denied rules plus approved indexes in `database.rules.json`, emulator-backed negative proof in `src/__tests__/security/prd0058-media-asset-rules.emulator.test.ts`, backup coverage proof in `r2-backup-worker/src/backup/data-backup.test.ts`, explicit restore ordering and restore-drill proof in `r2-backup-worker/src/restore/restore-execute.ts` plus `restore-execute.test.ts`, and scheduled-cron proof in `r2-backup-worker/src/backup/auto-backup.test.ts`. DR-owner sign-off was recorded in findings from the current Codex thread before implementation. Cleanup remains fail-closed, checksum metadata is present without deduplication, `r2-backup-worker/src/index.ts` remained untouched, Task 4.6+ stayed unstarted, and no remote/deployed state was mutated.
  - [x] 4.6 Consume PRD-0056A authenticated upload-session creation and add only PRD-0058-approved lifecycle/session continuation:
    - do not create bootstrap identity, replace issued `assetId`, change owner/session identity, derive a different temp key, or silently change PRD-0056A-owned fields;
    - signed upload authorization expires after 10 minutes;
    - authorization is scoped to one owner, upload session, asset, size limit, and approved media contract;
    - enforce MP3, M4A, AAC, WAV, or OGG;
    - enforce the application-level maximum of 10 active audio files per test;
    - enforce 50 MB per file in client/session validation and the S0 worker boundary;
    - validate extension, declared MIME, magic bytes, decodability, file size, and duration metadata before commit;
    - reject spoofed, corrupt, unsupported, expired, or replayed uploads with tests.
    - Closure: Task 4.6 added PRD-0058-only lifecycle/session continuation in `src/features/assessment/listening/storage/listeningAssetRegistry.ts` without changing PRD-0056A bootstrap identity, `uploadSessionId`, `assetId`, owner identity, or temp key shape. Focused tests prove same-owner continuation, 3-minute heartbeat staleness, 8-hour eligibility cutoff, expired cleanup queue marking, and unchanged create-time bridge fields. Worker bridge proof still enforces 10-minute signed authorization, owner/session/asset/media-contract scoping, 50 MB maximum, and now rejects WebM so the allowed contract is MP3, M4A, AAC, WAV, and OGG only.
  - [x] 4.7 Add checked-in, prefix-scoped R2 lifecycle configuration or an idempotent checked-in deployment script for `temp/` expiration at 24 hours:
    - dashboard-only configuration is insufficient;
    - durable prefixes must be excluded;
    - add an exact verification command and expected output;
    - prove active registry-backed committed audio is not covered.
    - Closure: Task 4.7 added checked-in R2 lifecycle configuration `cloudflare/r2-lifecycle.temp-24h.json`, scoped only to `temp/` with age `86400` seconds. `cloudflare/scripts/verify-r2-lifecycle-config.mjs` and `cloudflare/test/r2-lifecycle-config.test.ts` prove the rule is enabled, prefix-scoped, non-dashboard-only, excludes durable prefixes such as `assessment-assets/`, and has non-deploy verification command `npm --prefix cloudflare run verify:r2-lifecycle` with expected output `R2 lifecycle config OK: expire-temp-prefix-after-one-day temp/ 86400s`.
  - [x] 4.8 Implement idempotent registry-backed commit so Save draft/Publish can:
    - validate temp object and ownership;
    - create immutable durable object;
    - verify object;
    - write owning reference;
    - mark committed;
    - delete temp source only after durable success.
    - publishing an already committed draft adds/reuses the published reference and does not copy the bytes again.
    - while public delivery remains active, write both canonical `assetId` and a derived legacy-compatible public `audioUrl`/`streamUrl` into the owning published payload so unmodified solo, live, and result readers continue to work.
    - prove a newly committed/published asset plays through the current public reader without Task 6, 7, or 8 runtime changes.
    - Closure: Task 4.8 added bounded commit orchestration in `src/features/assessment/listening/storage/listeningAssetCommit.ts` and tests in `listeningAssetCommit.test.ts`. The service validates owner/session/asset/temp object, extension, declared MIME, temp object MIME, magic bytes, size, checksum, decodability metadata, duration metadata, and active-file count before copy; creates immutable durable keys under `assessment-assets/listening/{ownerId}/{assetId}/...`; verifies the durable object before reference write; marks committed; deletes temp only after durable success and owning reference success; and returns idempotent committed retries without recopying bytes. `src/services/listeningTestStorage.ts` now accepts an optional registry-backed committer for bridge metadata while preserving legacy public-reader compatibility by writing canonical `assetId` plus derived `audioUrl` and `streamUrl`. Existing public reader/runtime files were not changed.
  - [x] 4.9 Implement replacement before Task 5 authoring can expose it:
    - upload replacement under a new `assetId`;
    - keep old committed reference authoritative while replacement commits;
    - save/swap the new reference only after surrounding draft/test save succeeds;
    - remove old reference only after success;
    - failed/cancelled replacement cleans the new temp asset and preserves old playback;
    - block a second replacement while the first commit is unresolved.
    - Closure: Task 4.9 added bounded replacement orchestration in `src/features/assessment/listening/storage/listeningAssetReplacement.ts` and tests. Replacement requires a new `assetId`, keeps the old playback reference authoritative until commit plus surrounding save success, swaps to the new reference only after save success, removes the old reference only after success, queues cleanup for failed/cancelled replacement temp assets, blocks a second replacement while the first commit is unresolved, and returns terminal `nextState` so a later replacement can start only after success/failure/cancel resolution. No authoring UI exposure or facade algorithm was added.
  - [x] 4.10 Implement immediate best-effort cleanup for explicit remove, builder cancel, confirmed navigation, logout, auth loss, failed save/publish, replacement cancellation, and detected abandonment.
    - Closure: Task 4.10 added `queueImmediateListeningTempCleanup(...)` in `src/features/assessment/listening/storage/listeningAssetLifecycle.ts` with focused tests for explicit remove, builder cancel, confirmed navigation, logout, auth loss, failed save/publish, replacement cancellation, and detected abandonment. The helper queues temp cleanup without durable-delete authority and preserves committed assets.
  - [x] 4.11 Implement minimum heartbeat/fallback safety before authoring consumes it:
    - 60-second heartbeat;
    - stale after 3 minutes;
    - maximum 8 hours;
    - heartbeat never creates retention;
    - surviving uncommitted temp object removed by 24-hour fallback.
    - Closure: Task 4.11 added heartbeat constants and helpers for 60-second heartbeat due time, 3-minute stale deadline, 8-hour maximum eligibility, no retained reference/draft creation, and 24-hour fallback only for uncommitted temp/committing assets. Heartbeat helper returns next-due/stale values without persisting them as retention authority; persisted session fields stay limited to approved lifecycle data. Boundary tests cover equality at the 8-hour and 24-hour thresholds.
  - [x] 4.12 Implement same-owner/same-draft multi-tab lease aggregation so closing one valid tab cannot delete audio still leased by another valid tab.
    - Closure: Task 4.12 added owner/draft-scoped lease records and close handling using PRD-approved session `leaseIds` plus separate lease records. Tests prove one tab close does not queue cleanup while another same-owner/same-draft lease is fresh, stale-only remaining leases queue cleanup, and a different draft cannot retain the session.
  - [x] 4.13 Implement reference removal and safe `pending-delete` entry for discarded drafts. Authoritative references, not timestamps, decide whether the object remains.
    - Closure: Task 4.13 added `removeListeningAssetReference(...)`. Tests prove removing one of multiple retained references keeps the asset committed, removing the final reference moves it to `pending-delete` with seven-day `deleteAfter`, repeated no-op removal preserves the original `pendingDeleteAt`/`deleteAfter`, stale timestamps with live references stay protected, and fresh timestamps with zero references still enter `pending-delete`.
  - [x] 4.14 Define cross-test reuse explicitly:
    - no filename, URL, or checksum match may imply reuse;
    - if cross-test reuse is supported, it must use an explicit trusted registry-reference operation with ownership/reference tests;
    - if no product workflow needs it in this program, record explicit product-owner-approved deferral in the traceability matrix while preserving the prohibition on implicit reuse.
    - Closure: Task 4.14 records the current product-owner-approved deferral for cross-test reuse in source and traceability. `LISTENING_CROSS_TEST_REUSE_POLICY` forbids implicit filename/URL/key/checksum/byte-content reuse and requires a future explicit trusted registry-reference operation; tests reject filename, URL, key, checksum, and byte-content reuse while permitting only the trusted-operation token.
  - [x] 4.15 Put orphan-growth and commit-failure metrics live before Task 5 enables audio-bearing drafts:
    - storage child PRD names the concrete secured metrics sink and schema;
    - state whether threshold detection is automated alerting or human dashboard review;
    - for human review, name responsible role, review cadence, evidence location, and escalation runbook;
    - each threshold names owner and exact stop-rollout action consumed by Tasks 5.21 and 9.9;
    - record baseline counts/bytes and a product-owner accepted-risk statement for known untracked permanent audio;
    - default acceptable new untracked-draft-audio count is zero.
    - Closure: Task 4.15 adds secured `media_asset_metrics/{metricEventId}` schema/types, commit-failure and orphan-growth metric creation, human-dashboard-review owner/cadence/evidence/runbook metadata, threshold owner/stop actions consumed by Task 5.21 and Task 9.9, deterministic local baseline summarization, and `media_asset_metrics/**` RTDB rule/index proof. Product-owner accepted-risk text for the known untracked permanent Listening audio baseline is recorded in code/docs from the 2026-06-27 user approval. Default acceptable new untracked-draft-audio count remains zero. No Task 4.17+, Task 5, deploy, remote mutation, cleanup run, staging, commit, or push occurred.
  - [x] 4.16 Add rollback controls: disable new registry writes, stop cleanup/deletion, retain referenced assets, and preserve legacy publish reads without mutating existing audio.
    - Closure: Task 4.16 added bounded rollback controls in `src/features/assessment/listening/storage/listeningAssetRollback.ts` and tests. `commitListeningMediaAsset(...)` blocks new registry writes before registry/R2 mutation when rollback disables writes; `queueImmediateListeningTempCleanup(...)` and replacement cleanup return `cleanup-stopped` when cleanup/deletion is disabled; `removeListeningAssetReference(...)` preserves existing audio references and skips `pending-delete` entry while rollback forbids existing-audio mutation; referenced assets remain retained; legacy publish read fields are preserved; and rollback control metadata explicitly forbids mutating existing audio. No facade algorithm, private delivery, cleanup execution, remote mutation, deploy, staging, commit, or push occurred.
  - [x] 4.17 Run focused registry/commit/replacement/cleanup/lifecycle/rules tests. Human-assisted deployed proof requires explicit approval; any Playwright run uses `--reporter=json > report.json`.
    - Closure: Task 4.17 reran the focused local storage foundation proof without Playwright, deployed proof, cleanup execution, or remote mutation. Current corrective proof passed: storage/facade/builder Vitest 9 files / 85 tests; `r2-backup-worker` backup/cron/restore/protected-route proof 5 files / 32 tests; Cloudflare bridge/lifecycle tests 2 files / 15 tests under bundled x64 Node after ambient Worker-pool startup hit the known `win32 arm64 LE` `workerd` error; `npm --prefix cloudflare run verify:r2-lifecycle` printed `R2 lifecycle config OK: expire-temp-prefix-after-one-day temp/ 86400s`; RTDB emulator rules proof passed 1 file / 7 tests after PATH included the local Temurin JDK; `rtk git diff --check`, UTF-8 over touched text files, Task 5 checkbox scan, and production build/bundle-budget passed.
  - [x] 4.18 Run the mandatory fresh-context independent verification pass from the Verification Standard. Reviewer must audit every registry/storage traceability row, large-file maps, declared touch regions, RED/GREEN and mutation proofs, rules/backup/restore/lifecycle configuration, and cleanup safety; resolve all findings and obtain clean re-review.
    - Closure: Task 4.18 used fresh review after earlier auditor attempts failed from account usage limits before producing output. Corrective review found real blockers in builder metadata carry, legacy temp URL fallback, committed retry/reconciliation behavior, RTDB rules/backup/restore coverage, and authority-surface drift. This packet resolves those findings with source/test fixes and synchronized taskbox, traceability, findings, implementation log, PRD-0058 status, and upload-storage authority; spec re-review passed. A code-quality subagent attempt failed from account usage limits and is not counted as proof.
  - [x] 4.19 Parent acceptance: an audio-bearing draft can only retain audio through tracked immutable asset/reference commit; registry rules/indexes/backup/end-to-end restore/emulator tests ship together with DR-owner sign-off and cron proof; strict validation/upload TTL/lifecycle config pass; replacement and publish reuse are reference-safe; new assets remain playable through old public readers; cleanup fails closed without registry integrity; discarded/abandoned draft audio has immediate cleanup plus fallback; named metrics sink/owner/stop actions are live; new storage/audio behavior lives in approved bounded modules; `r2Storage.ts` and `listeningTestStorage.ts` gained no net new responsibility; before/after line counts and future decomposition seams are recorded; independent verification passes; no untracked legacy permanent promotion is introduced.
    - Closure: Task 4.19 accepts the local PRD-0058 minimum storage foundation after corrective proof and spec re-review. Audio retention is only through immutable `assetId`/reference commit; builder metadata is preserved into storage; temp URLs without canonical registry metadata fail closed before any commit; mixed-section payloads cannot partially commit; committed retries reverify durable objects; reference-write failures queue reconciliation; registry rules/indexes, backup, restore drill, DR-owner sign-off, and cron proof cover upload sessions, assets, events, metrics, and sweeps; upload TTL/lifecycle and strict validation pass; replacement and publish reuse remain reference-safe; new committed assets preserve public `audioUrl`/`streamUrl` for unchanged readers; cleanup remains fail-closed without restore/integrity and rollback can stop cleanup/deletion; immediate cleanup intent plus 24-hour fallback are proven; metrics sink/owner/cadence/stop actions are recorded; behavior lives in bounded storage modules; `r2Storage.ts` and `listeningTestStorage.ts` remain compatibility facades; line counts and decomposition seams are recorded; no Task 5 start, deployed proof, cleanup execution, remote mutation, staging, commit, or push occurred.

- [x] 5.0 Introduce Listening Save draft, Publish, immutable revision, concurrency, and legacy-R2 transition behavior
  - Closure: Task 5 is accepted locally after Batches A-E. Save draft and Publish semantics are split and trusted, immutable versions/revision drafts/concurrency/idempotency/legacy first-edit transition are implemented under PRD-0057 boundaries, audio-bearing draft/version writes use Task 4 registry-backed retention, publish readiness requires byte-range-capable accessible audio, authoring accessibility and desktop/tablet browser proof pass, large-file maps document facade growth and future seams, internal-fixture rollout metrics show zero commit failures and zero new untracked draft audio, and independent GPT-5.5/GPT-5.4-mini read-only verification passed with no blocking findings. Selected-teacher rollout was not run or claimed because separate explicit authorization was not given. No Task 6+, solo/homework runtime, live runtime, `AudioPlayer.tsx`, Reading V2 runtime internals, Google Drive behavior, deployment, staging, commit, push, or remote-state mutation occurred.
  - [x] 5.1 Record explicit product-owner plus architecture/security reviewer sign-off in findings. Reconcile/regenerate this provisional scaffold against the approved authoring child PRD; child PRD wins. Confirm it names bounded authoring/storage module homes and facade limits under the File-Architecture Principles. Do not continue on mismatch.
    - Closure: Task 5.1 records current product-owner implementation authorization from the Task 5 Batch A user prompt and Codex architecture/security review against approved PRD-0057. The only scaffold wording drift was reviewer label text, corrected here to `architecture/security reviewer`; no material PRD-0057 mismatch was found. Bounded homes remain `src/features/assessment/listening/{authoring,storage,adapters,types}/`, and `src/services/listeningTestStorage.ts` remains facade-only.
  - [x] 5.2 Start only after Task 4 parent acceptance. Preserve baseline tests proving current single-save behavior, missing-audio hard block, published writes, and existing R2 reads. Inventory Google Drive code/tests only as obsolete unsupported residue; do not execute them as a supported-behavior baseline or touch them before the separate cleanup task.
    - Closure: Task 4.19 and parent Task 4.0 were accepted at the starting gate. `src/services/listeningTestStorage.test.ts` now explicitly asserts successful legacy save performs one Firebase write and preserves `isPublished: true`, while existing missing-audio hard block, registry-backed public-reader URL, temp-URL rejection, and no-`moveToPermanent` checks remain green. Google Drive residue was not used, tested as supported behavior, or touched.
  - [x] 5.3 Write failing service tests for explicit first Save draft, autosave-after-draft-ID, lenient draft validation, strict Publish validation, immutable published versions, revision drafts, assignment/result pinning, idempotency, and optimistic conflict rejection.
    - Closure: RED failed because `listeningAuthoringWorkflow`, `listeningAuthoringStore`, and authoring types did not exist. GREEN adds focused service tests for first explicit Save draft, autosave after draft ID, lenient draft warnings, strict Publish blockers, immutable version creation, revision draft creation, retained assignment/result pins, idempotent retry, idempotency-key payload mismatch, and recoverable stale conflict rejection.
  - [x] 5.4 Define child-PRD-approved authoring state/contracts in small Listening-owned modules under one coherent bounded package. Prefer `src/features/assessment/listening/{authoring,storage,adapters,types}/` per PRD section 9 unless the child PRD proves an existing Listening root is more coherent. Do not create a third scattered Listening root or place draft/version/publish behavior in neutral shared presentation.
    - Closure: Added PRD-0057 B2 Option B contracts in `src/features/assessment/listening/types/listeningAuthoring.types.ts` and validation/workflow modules under the approved Listening package roots. No neutral shared presentation, Reading V2, runtime, or UI code owns the new draft/version/publish contracts.
  - [x] 5.5 Keep `src/services/listeningTestStorage.ts` as the public Listening persistence entrypoint/facade. Implement create/update draft, publish, revision, archive/delete, and legacy-read responsibilities as separate focused modules, such as a draft store, publish service, revision service, deletion-governance service, and legacy audio resolver, then delegate through the facade. Record facade lines/responsibilities before and after; net growth is limited to narrow delegation/compatibility wiring and requires justification.
    - Closure: `src/services/listeningTestStorage.ts` remains the public facade and only re-exports focused authoring workflow/store/deletion-governance/legacy-resolver APIs while preserving existing compatibility save/read/update/delete behavior. New responsibilities live in `listeningAuthoringWorkflow.ts`, `listeningAuthoringStore.ts`, `listeningAuthoringDeletionGovernance.ts`, `listeningAuthoringValidation.ts`, `listeningLegacyAudioResolver.ts`, and `listeningAuthoring.types.ts`; facade line count is 702 after the narrow exports and existing Task 4 compatibility seams.
  - [x] 5.6 Route every new audio-bearing Save draft and Publish through Task 4 registry-backed commit/reference APIs. Never use the legacy untracked permanent-key move for new draft audio and never persist a temp URL as saved content. During the public-delivery compatibility window, persist canonical `assetId` plus the Task 4 derived public `audioUrl`/`streamUrl` required by unmodified solo/live/result readers.
    - Closure: `createListeningAuthoringWorkflow(...)` commits audio-bearing Save draft references as `drafts` and Publish references as `versions` through the Task 4 `ListeningAssetCommitter` contract. Tests prove draft and publish references use `listening_authoring/**` source paths, idempotency suffixes, canonical `assetId`, derived public `audioUrl`/`streamUrl`, no persisted `/temp/` URL, and no legacy `moveToPermanent` call path.
  - [x] 5.7 Implement optimistic concurrency using approved revision/version tokens; reject stale writes with recoverable conflict state, never last-write-wins.
    - Closure: Draft update and publish paths require the expected conflict token and return recoverable `conflict` results when stale. Tests prove autosave increments token after draft ID exists and stale Save draft/Publish input cannot overwrite current draft state.
  - [x] 5.8 Implement Save draft/Publish idempotency keys so retries return existing operation/version and never duplicate records or audio references.
    - Closure: Operation records under the bounded store key owner, operation type, target, idempotency hash, and request hash. Save draft and Publish retries with the same key/request return the prior logical result; changed payload under the same key fails closed as an idempotency conflict. Tests prove Publish retry returns the same `versionId` and version count stays one.
  - [x] 5.9 Implement the legacy transition and shared Listening audio-resolution dependency early in Task 5:
    - freeze first-edited legacy R2 published record as immutable version 1;
    - create revision draft;
    - preserve existing assignment/result/session pinning;
    - define one Listening-owned legacy audio resolver/read adapter for raw R2 URLs;
    - make Tasks 6 and 7 consume that resolver rather than reimplementing legacy URL handling;
    - land and test the resolver before any private-delivery cutover.
    - Prior implementation evidence: Task 5.9 adds service-level first-edit legacy freeze through `createRevisionDraftFromLegacyTest(...)`, snapshots the unchanged legacy R2 document as immutable version 1 under `listening_authoring/versions`, creates a revision draft under `listening_authoring/revision_drafts`, recovers a partial freeze retry that has version 1 but no revision draft by creating the missing draft, returns approved `authoringVersioning` freeze metadata for the legacy row, preserves assignments/sessions/attempts/results as retained pins, and keeps raw R2 read compatibility in `resolveListeningLegacyAudioReference(...)` without registry side effects. No runtime, private delivery, or on-read migration changed.
    - Historical reopened gate, 2026-06-29: executable RTDB emulator startup rejected the legacy frozen-row rule at `database.rules.json:649:28` with `! only operates on booleans.`
    - Superseding closure, 2026-06-29: Task 5.9 is reclosed after `database.rules.json` frozen-row guards compile and the executable PRD-0057 RTDB emulator proof passes 5/5 with owner/admin/cross-owner/read-denial, canonical browser-write denial, and frozen legacy browser-write/delete denial coverage using the process-local Temurin JDK.
  - [x] 5.10 Implement draft soft-delete/recovery:
    - soft-delete draft by default;
    - preserve recovery for seven days;
    - restore recovers the draft and valid asset references;
    - permanent draft deletion/reference cleanup cannot begin before recovery expiry and reference checks;
    - tests cover delete, restore, expiry, ownership, and retry idempotency.
    - Closure: Task 5.10 adds service-level soft delete and restore operations that preserve the same draft identity, conflict-token checks, idempotent delete retry, valid asset references, and deletion timestamps needed for future retention governance. Restore remains available beyond seven days because no approved permanent-cleanup path exists; this satisfies the seven-day minimum recovery window without inventing an expiry. Permanent cleanup remains fail-closed and was not implemented.
  - [x] 5.11 Implement published-test deletion governance:
    - archive published tests by default;
    - block physical deletion while attempts, results, revisions, assigned sessions, or other retained references exist;
    - preserve immutable versions and pinned learning evidence;
    - expose physical deletion only through the approved audited operation from Task 6.
    - Closure: Task 5.11 adds metadata-only published-version archive, preserves immutable version identity/document/hash/audio references/retained pins, blocks physical deletion while retained references exist, and still blocks physical deletion without the future approved Task 6 audited operation. Existing `deleteListeningTestFromFirebase(...)` has no live `src` consumers in the Batch B producer-consumer audit and now fails closed instead of deleting `tests/{testId}`.
  - [x] 5.12 Add net-new Save draft and separate Publish controls with saving/saved/error/conflict states. Compose new UI from bounded Listening-owned components, such as `ListeningSavePublishBar` and `ListeningDraftStatus`; `ListeningTestBuilder.tsx` only orchestrates data/handlers and must not absorb control-state implementation.
    - Closure: Task 5.12 adds separate `ListeningSavePublishBar` Save draft and Publish controls, `ListeningDraftStatus` state rendering for saving/saved/error/conflict/publish-blocked states, duplicate-action coalescing, and builder orchestration through `createListeningAuthoringWorkflow()`. The builder Save draft path calls trusted `saveDraft(...)`; the builder Publish path requires saved `draftId` and `expectedConflictToken` before calling trusted `publishDraft(...)`; focused tests prove old `Save Test` UI is absent and `saveListeningTestToFirebase(...)` is not called by builder Save/Publish paths.
  - [x] 5.13 Add module-owned teacher copy for first save, missing-audio draft warning, publish blockers, stale conflict, duplicate action, 8-hour expiry, re-upload, navigation-away discard, and completed discard. Keep copy/state rendering in focused components such as `ListeningDraftStatus` or `ListeningConflictRecovery`, not inline branches added to `ListeningTestBuilder.tsx`.
    - Closure: Task 5.13 keeps teacher-facing Save/Publish copy in `ListeningDraftStatus`, including first-save draft identity, missing-audio draft warning, publish blockers, stale conflict, duplicate action, 8-hour stale status, re-upload guidance, navigation-away discard confirmation, and completed discard messaging. Builder tests prove missing audio saves as draft warning, Publish blockers render, duplicate Save draft is coalesced, and `alert()` is not used.
  - [x] 5.14 Add exact upload guidance through a bounded component such as `ListeningUploadGuidance`:
    - `Up to 10 audio files, 50 MB each.`;
    - `MP3 or M4A recommended.`;
    - label all audio counters `audio files`;
    - keep audio count separate from `Questions (0/10)`.
    - Closure: Task 5.14 adds bounded `ListeningUploadGuidance` with exact copy `Up to 10 audio files, 50 MB each.` and `MP3 or M4A recommended.`, a status-region audio counter labeled `audio files`, and focused tests proving the audio counter stays separate from the `Questions (0/10)` heading.
  - [x] 5.15 Use shared announcement system for Save draft/Publish/archive/restore/discard outcomes; keep feature-owned orchestration in a focused action/announcement adapter rather than expanding builder branches. No page banners, `alert()`, or silent success.
    - Closure: Task 5.15 adds `listeningAuthoringAnnouncements.ts` as the feature-owned shared-toast adapter for Save draft, Publish, archive, restore, duplicate, conflict, failure, and discard outcomes. The builder wires Save draft, Publish, conflict, duplicate, and discard outcomes through the adapter and `trackAction(...)`; tests prove shared toast calls and no `alert()` for Save draft, Publish blocked, and trusted Publish.
  - [x] 5.16 Add Publish-time audio accessibility validation in a focused service plus bounded readiness component such as `ListeningPublishReadiness`:
    - verify canonical asset/reference exists;
    - verify current delivery path is reachable;
    - verify byte-range capability rather than URL presence alone;
    - fail closed when range response is missing/malformed or media is non-seekable;
    - preserve draft save with warnings while blocking Publish.
    - Closure: Added `validateListeningPublishReadiness(...)` and `ListeningPublishReadinessPanel` to require canonical `assetId`, reachable `audioUrl`/`streamUrl`, and `206` byte-range delivery before trusted Publish. Builder Publish now fails closed before backend publish on missing/malformed/non-seekable range delivery while Save draft warnings remain allowed.
  - [x] 5.17 Add authoring accessibility verification across the bounded authoring components:
    - correct heading hierarchy and region labels;
    - validation/error/loading semantics use status/alert appropriately;
    - color is not the only ready/blocked/error signal;
    - icon-only controls have accessible names;
    - keyboard focus reaches Save draft, Publish, conflict recovery, archive/restore, and validation targets;
    - visible controls meet the 44px by 44px touch-target floor where applicable.
    - Closure: Focused component/builder tests verify readiness `status`/`alert` semantics, labelled regions, keyboard reachability for Save draft, Publish, stale-conflict recovery, Restore draft, Archive published version, and validation targets, 44px lifecycle action targets, display-mode button semantics, and no `alert()` fallback. Trusted lifecycle operations freeze all authoring navigation/actions while pending. Human-assisted desktop/tablet/screen-reader proof remains Task 5.20.
  - [x] 5.18 Add integration tests for create, first Save draft, reload, autosave, missing-audio draft, blocked Publish, publish range/accessibility failure, replacement/cancel, Publish, revision reopen, stale conflict, duplicate click, parser failure/manual mode, legacy first edit, draft delete/restore/expiry, published archive/delete block, discard cleanup, registry reference state, and a newly published asset-ID record playing through the unchanged current public reader via derived `audioUrl`/`streamUrl`.
    - Closure: Builder integration covers manual create, first Save draft, missing-audio draft, blocked Publish, byte-range/accessibility failure, duplicate click, stale-conflict focus recovery, trusted discard/restore, trusted published archive, canonical asset metadata, readiness semantics, and no legacy save path. A backend in-memory integration sequence now proves create, reload, autosave, stale conflict rejection, first Publish, revision Save/Publish, discard, restore, and archive against one repository. Focused storage suites retain replacement/cancel, registry-reference, cleanup-intent, and derived public-reader URL proof.
  - [x] 5.19 Add observability for Save draft, autosave failure, Publish, conflict, revision creation, archive/restore, discard, commit failure, orphan growth, and legacy transition without logging signed URLs/audio content.
    - Closure: Builder tracking now covers Save draft, Publish, conflict, discard, duplicate action, publish readiness, and Back/Next workflow transitions. Workflow observability emits sanitized ID/status-only events for autosave failure, revision creation, archive, restore, and legacy transition. Task 4 metric modules remain the commit-failure/orphan-growth sink, and Batch D tests/grep prove no audio URL is logged from the touched builder validation path.
  - [x] 5.20 Human-assisted browser/a11y gate: run focused authoring/service/rules tests and teacher desktop/tablet keyboard/screen-reader proof. Any Playwright run uses `npx playwright test <optional-spec> --reporter=json > report.json`. Do not touch solo/live runtime.
    - Closure: Batch E reran the focused frontend authoring/parser, backend authoring, storage/public-reader, and PRD-0057 RTDB emulator suites, plus required behavioral mutation probes for stale conflict, idempotency duplicate version, temp URL persistence, failed byte-range readiness, and legacy frozen content mutation. Playwright browser proof ran exactly as `npx playwright test e2e/prd0055-task5-authoring-a11y.spec.ts --reporter=json > report.json` against `http://localhost:5173`; teacher quick-login used the bottom-right dev settings control and Teacher button, then navigated to `/create-test?skill=Listening`. Chromium desktop 1366x900 and tablet 768x1024 tests passed, with keyboard focus/activation proof and CDP accessibility-tree artifacts under `output/playwright/prd0055-task5-batch-e/`. No Save draft, Publish, solo/live runtime, `AudioPlayer.tsx`, Reading V2 runtime, deployment, push, or remote mutation occurred.
  - [x] 5.21 Perform Task-5-local rollout before Task 6 depends on production-shaped data:
    - internal fixtures first;
    - selected teachers next only with separate explicit authorization;
    - observe child-PRD-approved minimum traffic/sample window;
    - record draft creation, publish, discard, commit failure, and orphan-growth metrics;
    - use Task 4 threshold owner/runbook and stop for unexplained permanent-object growth, failed cleanup, wrong audio, or legacy incompatibility.
    - Closure: Batch E performed the local rollout against internal fixtures only. Authoring lifecycle/publish JSON proof passed 27/27 tests and storage metrics/commit/lifecycle JSON proof passed 40/40 tests; summary artifact `output/prd0055-task5-batch-e/local-rollout-summary.json` records draft creation, publish, discard, commit-failure, and orphan-growth metrics. Observed commit failures were zero, new untracked draft audio count/bytes were zero, no unexplained permanent-object growth, failed cleanup, wrong audio, or legacy incompatibility was observed, and no selected-teacher or production data rollout was attempted because separate authorization was not given.
  - [x] 5.22 Run the mandatory fresh-context independent verification pass from the Verification Standard. Reviewer must compare authoring child PRD/traceability against the diff, inspect large-file maps/touch regions for `ListeningTestBuilder.tsx` and `listeningTestStorage.ts`, rerun behavioral mutation proofs, verify lifecycle/reference integration, and challenge every claimed legacy/deletion/accessibility behavior; resolve all findings and obtain clean re-review.
    - Closure: Required read-only GPT-5.5 medium verifier inspected PRD-0057, taskbox, traceability, findings, implementation log, upload-storage authority, large-file maps, diff, proof artifacts, source boundaries, selected-teacher authorization boundary, Google Drive unsupported-residue claim, and protected-path drift. It reran safe local authoring/storage rollout suites, checked `git diff --check`, and returned PASS with no blocking findings. Required read-only GPT-5.4-mini high explorer checked stale claims, forbidden path drift, checkbox state, changed/untracked inventory, and proof artifact parseability; it returned PASS and the one low historical implementation-log stale line was reconciled before closure.
  - [x] 5.23 Parent acceptance: draft/publish semantics are unambiguous; every audio-bearing draft uses tracked Task 4 retention; replacement/publish reuse are safe; draft recovery and published archive/delete rules pass; Publish proves accessible range-capable audio; authoring accessibility passes; new persistence/UI behavior lives in approved bounded modules/components; `listeningTestStorage.ts` and `ListeningTestBuilder.tsx` remain thin facades with no net new responsibility; before/after line counts and created/preserved decomposition seams are recorded; internal-fixture rollout evidence shows no unexplained orphan growth and selected-teacher rollout remains unclaimed without separate explicit authorization; legacy R2 records/results remain usable; Task 6 migration inventory covers any pre-registry or accidentally interim-promoted audio; independent verification passes; no Google Drive path is used, tested, or represented as supported behavior, and its obsolete residue remains untouched for the separate cleanup task.
    - Closure: Parent acceptance is local-only and proof-backed by focused frontend/backend/storage/rules tests, mutation-kill probes, browser desktop/tablet a11y artifacts, internal-fixture rollout metrics, large-file maps, stale-claim scans, protected-path scans, and clean independent re-review. Selected-teacher production-shaped traffic is not claimed because it requires separate authorization; Task 6 must treat that path as a future approval gate and must not infer production rollout from this local acceptance. No Google Drive implementation behavior is supported or used.

- [x] 6.0 Complete R2 reconciliation, historical cleanup, backup governance, and authorized delivery
  - Closure: Task 6 parent acceptance is complete after local Task 6.1-6.12 evidence, explicit owner acceptance for no-op selected-teacher media reconciliation semantics, and fresh read-only Firebase shallow proof. No cleanup execution, object deletion, R2/Firebase/Cloudflare mutation, deployment, staging, commit, push, Task 7 work, solo/live runtime change, `AudioPlayer.tsx`, Reading V2 runtime internals, or Google Drive behavior occurred. Solo/live playback remains public.
  - [x] 6.1 Record explicit product-owner plus architecture/security reviewer sign-off in findings. Reconcile/regenerate this provisional scaffold against the approved storage child PRD and Task 4/5 live truth; child PRD wins. Do not continue on mismatch.
    - Closure: Product-owner authorization is the 2026-06-29 current-thread request to execute Task 6 Batch A only. Architecture/security reconciliation found no mismatch between PRD-0058, accepted Task 4 local storage truth, accepted Task 5 local authoring truth, and this bounded local deletion-governance slice. PRD-0058 child PRD wins. Later 2026-06-29 selected-teacher Worker HTTP proof supersedes the old dependency blocker for Task 6.3.
  - [x] 6.2 Complete final deletion governance not required for Task 4 minimum safety:
    - full approved state-machine and invalid-transition tests;
    - seven-day zero-reference `pending-delete` grace;
    - immediate reference recheck before deletion;
    - metadata-only deletion tombstone retained for exactly 90 days;
    - tombstone excludes signed URLs, secrets, keys, raw audio, and audio content;
    - separate audited administrative deletion operation;
    - administrative deletion must not reuse the teacher endpoint and must still honor retained-reference rules.
    - Closure: Local-only implementation adds bounded `src/features/assessment/listening/storage/listeningAssetDeletionGovernance.ts` and focused tests. Proof covers the full approved state machine through `deleted`, invalid transitions, seven-day zero-reference grace, immediate same-tick reference recheck, retained-reference denial, metadata-only 90-day tombstone schema, forbidden signed URL/secret/key/raw-audio exclusion, separate audited admin operation, teacher-endpoint reuse denial, idempotent retry/changed-request denial, and rollback stop-delete behavior. No cleanup runner, real object deletion, production data, remote mutation, deploy, selected-teacher rollout, private delivery, solo/homework runtime, live runtime, `AudioPlayer.tsx`, Reading V2 internals, or Google Drive behavior started.
  - [x] 6.3 Start reconciliation validation only after Task 5 selected-teacher rollout has produced the child-PRD-approved minimum real traffic/sample window. Implement bounded/checkpointed hourly temp reconciliation and daily durable pending-delete reconciliation with immediate pre-delete reference recheck. Storage child PRD must set per-run object-operation count, estimated R2 cost, and wall-clock budgets plus abort/report thresholds. Do not close on synthetic data alone or continue a run after a capacity stop.
    - Spark-tier correction gate update 2026-06-29: PRD-0057 production Save draft / Publish / lifecycle authority now has local Cloudflare Worker + Firebase RTDB REST implementation proof. Before Task 6.3 reconciliation dependency proof, selected-teacher authoring writes still must run and be accepted using that Worker path. Firebase Functions, Cloud Functions, Firebase Secret Manager, and Blaze-only scheduled/backend paths remain rejected production targets while Spark-tier is intentional.
    - Selected-teacher Worker proof update 2026-06-29: single selected-teacher Worker HTTP proof passed against deployed Worker version `34970bd6-feb7-4520-87f1-fa6341dc0ba0`; artifact `output/prd0055-task5-selected-teacher-worker-proof/selected-teacher-worker-proof.json`; write flag verified `false` afterward. This unblocked Task 6.3 dependency evidence. At Task 6.3 closure time, natural browser UI write proof, cleanup/deletion, private delivery, staging, commit, push, and Task 6.4+ remained unstarted.
    - Closure: Local Task 6.3 implementation extends `src/features/assessment/listening/storage/listeningAssetReconciliationPlanner.ts` into a repository-backed dry-run/report/checkpoint foundation for hourly temp and daily pending-delete reconciliation. It requires selected-teacher real proof and aborts with report/checkpoint on missing/failed/synthetic-only proof; enforces object, R2, Firebase, wall-clock, and estimated-cost budgets; stops immediately at capacity before continuing candidates or further same-tick rechecks; persists only injected dry-run reports/checkpoints; keeps every candidate `executionAuthorized: false`; and fails closed for missing/stale references, retained references, cross-owner ambiguity, missing owner, rollback stop-delete, and backup/restore uncertainty. Daily pending-delete planning requires same-tick reference recheck before any report-only durable-delete candidate. Focused and adjacent storage tests prove the behavior. In the Task 6.3 packet, no cleanup execution, object deletion, R2/Firebase mutation, scheduled Worker route, private delivery, deployment, staging, commit, push, or Task 6.4+ work occurred.
  - [x] 6.4 Run one-time historical orphan inventory covering:
    - audio left by past Listening-test deletions;
    - pre-registry permanent audio;
    - any audio promoted by an interim or failed rollout scheme;
    - objects with missing/ambiguous owner or reference evidence.
    Require dry run, accepted-risk record, backup review, explicit approval, bounded deletion, rollback evidence, and child-PRD-approved maximum list/copy/delete operation count, estimated cost, and wall-clock duration before deletion. Abort and report when any capacity budget is exceeded.
    - Closure: Local Task 6.4 foundation adds `src/features/assessment/listening/storage/listeningHistoricalOrphanInventory.ts` as a dry-run/report/checkpoint-only historical orphan inventory planner. It classifies deleted-test leftovers, pre-registry permanent audio, interim/failed rollout objects, missing owner evidence, and ambiguous owner evidence; excludes retained live product references; records accepted-risk-required rows for unresolved owner/interim classes; enforces object/list/copy/delete/cost/wall-clock budgets; aborts with report/checkpoint at capacity stops; and has no delete or copy side effect. Focused tests and mutation probes prove retained references cannot be counted as orphans, budget bypass fails, and dry-run deletion side effects fail. No production/R2 inventory access, cleanup execution, object deletion, remote mutation, deployment, staging, commit, push, private delivery, or Task 6.6+ work occurred.
  - [x] 6.5 Document/test audio-object backup governance only: `r2-backup-worker/` media retention, restore authority, GDPR/permanent-deletion filtering, and proof backup copies do not count as live product references. Registry-node backup/restore is already mandatory in Task 4 and must not be deferred here. Obtain distinct DR-worker owner approval, prove existing scheduled backup crons still succeed, and perform an end-to-end test backup/restore/deletion-filter drill before acceptance.
    - Closure: Local Task 6.5 foundation adds `r2-backup-worker/src/backup/listening-media-governance.ts` and tests for audio-object backup governance. It records distinct DR-worker owner approval for this local design/test packet, preserves Task 4 registry backup/restore acceptance, proves backup copies do not count as live product references, filters GDPR-completed, tombstoned, and permanently deleted objects from restore/live retention, blocks teacher-role restore authority, marks permanent-delete restore as requiring an approved path, and performs a local backup/restore/deletion-filter drill. Existing scheduled backup cron, registry backup/restore, Reading V2 trusted submit, and homework assignment route tests pass in the full `r2-backup-worker` suite. No backup restore was run against remote data, no object was deleted, and no deploy/stage/commit/push occurred.
  - [x] 6.6 Build and prove server-side authorized delivery issuance without cutting solo or live runtime traffic:
    - resolve canonical asset IDs through the approved service boundary;
    - authorize issuance from the trusted asset reference graph: asset owner, or a student/result viewer with active retained authorization to the referenced immutable test version;
    - possession of a valid `assetId` is never sufficient; deny cross-user/cross-owner issuance;
    - issue 60-minute authorized URLs;
    - support refresh issuance below 10 minutes;
    - return valid `Range`, `206`, `Accept-Ranges`, and stable `Content-Length`;
    - reject malformed/unsupported range behavior and fail closed before Publish/result/live rollout;
    - never mark non-seekable delivery ready;
    - keep solo and live `AudioPlayer` traffic on public R2;
    - do not modify `AudioPlayer.tsx` in Task 6.
    - Closure: Local Task 6.6 adds `src/features/assessment/listening/storage/listeningAssetDelivery.service.ts` as the trusted-server authorized delivery issuer. It resolves canonical asset IDs only through an injected reference-graph boundary; authorizes only the asset owner or a retained result viewer tied to an active immutable version; denies known-asset-ID-only, cross-user, cross-owner, browser-runtime, noncanonical, noncommitted, and expired/prior-URL authority; issues 60-minute URLs; allows refresh only below the 10-minute threshold while preserving the previous URL until the replacement is ready; and fails closed before signing when byte-range proof is not `206` with `Accept-Ranges: bytes`, exact `Content-Length`, and exact `Content-Range`. It is dependency-injected local proof only and does not deploy or mutate R2/Firebase.
  - [x] 6.7 Integrate private delivery only into the saved-result/review path that does not require live `AudioPlayer` changes. Prove a past Listening result with a legacy raw public R2 URL still plays through the shared resolver and a new asset-ID result resolves through authorized delivery.
    - Closure: Local Task 6.7 adds `src/features/assessment/listening/adapters/listeningResultReviewAudioResolver.ts` as the saved-result/review adapter seam and wires `src/components/results/SharedSavedResultCore.tsx` to consume it when a loaded Listening saved result carries `listeningResultReviewAudio`. Legacy raw public R2 records keep read-only public playback through the existing legacy resolver with `migrationPerformed: false`; new asset-ID records require immutable `versionId` scope and call authorized delivery with `resultId`/`versionId` scope. Production shell calls use the default `src/features/assessment/listening/adapters/listeningResultReviewDeliveryClient.ts` issuer, which sends only `assetId`, `resultId`, and `versionId` to the authenticated Cloudflare Worker route `POST /listening-delivery/result-review`; tests may still inject the issuer boundary. No on-read migration, no Google Drive compatibility claim, no solo/live cutover, and no `AudioPlayer.tsx` change occurred.
  - [x] 6.8 Human-assisted server/result proof matrix: authorized issuance, known-asset-ID cross-user/cross-owner issuance denial, byte ranges, expiration/refresh issuance, Chrome, Edge, desktop Safari, iOS Safari result review, and legacy/new result records. Every Playwright run uses `--reporter=json > report.json`. Solo and live playback remain public in this task.
    - Closure: Local Task 6.8 records `output/prd0055-task6-batch-d/human-assisted-proof-matrix.json` and Playwright JSON proof at `output/playwright/prd0055-task6-batch-d/report.json`. The Playwright run used `--reporter=json > output\playwright\prd0055-task6-batch-d\report.json` and passed Chrome, Edge, desktop-Safari-equivalent WebKit, and iOS-Safari-equivalent WebKit/iPhone result-review range probes for legacy and authorized audio. Focused Vitest proof covers authorized issuance, known-asset-ID denial, cross-user/cross-owner denial, TTL/refresh, malformed/unsupported range fail-closed, non-seekable fail-closed, expired URL denial, legacy public result compatibility, new asset-ID authorized result resolution, default delivery client payload authority, Worker route trusted-server UID derivation, and `SharedSavedResultCore` production/default-issuer consumption. Solo/live playback remain public; `AudioPlayer.tsx` remains untouched.
  - [x] 6.9 Perform Task-6-local rollout for reconciliation and result-review delivery:
    - internal fixtures;
    - selected-teacher/result-review traffic;
    - observe cleanup and result-playback metrics;
    - do not switch solo or live traffic;
    - stop on missing references, premature deletion, result audio failure, or backup-policy conflict.
    - Closure: Local Task 6.9 adds `src/features/assessment/listening/storage/listeningTask6LocalRollout.ts` as a proof-only evaluator for accepted prior selected-teacher Worker proof, local result-review browser proof, dry-run reconciliation reports, required metrics, and hard boundaries. It blocks on missing/stale selected-teacher proof, missing result-review proof, missing metrics, blocked references, write/delete operations, result audio failure, backup-policy conflict, cleanup execution, new production-data reads, new remote mutation, or solo/live traffic switching. This packet did not run new selected-teacher/result-review remote traffic; it consumes already accepted proof artifacts only.
  - [x] 6.10 Extend metrics/alerts for temp age, reconciliation, delete failure, issuance/refresh failure, reclaimed bytes, auth denial, and assets blocked by references. Preserve Task 4 orphan metrics continuously through migration.
    - Closure: Local Task 6.10 extends `src/features/assessment/listening/storage/listeningAssetMetrics.ts` with Task 6 lifecycle metric operations for temp age, reconciliation, delete failure, issuance failure, refresh failure, reclaimed bytes, auth denial, assets blocked by references, and result-playback failure. Stop actions preserve public delivery on issuance/refresh failure, stop cleanup on delete failure or reference spikes, and preserve Task 4 orphan-growth metrics unchanged. No production alerting deployment occurred.
  - [x] 6.11 Add rollback controls: stop cleanup/deletion, preserve old/new readers, retain referenced assets, and return result review to public R2 without data mutation.
    - Closure: Local Task 6.11 extends rollback behavior so active rollback controls return asset-ID result-review records to the public R2 resolver without invoking authorized delivery or mutating records. Existing Task 4.16 rollback controls still disable registry writes, stop cleanup/deletion, retain referenced assets, preserve legacy/public read fields, and prohibit existing-audio mutation.
  - [x] 6.12 Run the mandatory fresh-context independent verification pass from the Verification Standard. Reviewer must audit reconciliation against real Task-5 data, deletion/admin/tombstone rules, result-resolution behavior, backup governance, range proofs, rollback, RED/GREEN/mutation evidence, and any triggered large-file maps; resolve all findings and obtain clean re-review.
    - Closure: Fresh-context independent verification ran in Batch F and is recorded at `output/prd0055-task6-batch-f/independent-verification-summary.json`. Reviewer `019f1416-21a3-78a0-b298-a8846e2aff5c` audited Task 6 source/tests/docs/proof and returned BLOCKED for parent acceptance, not for recording the review itself. The blocking finding is that current selected-teacher reconciliation evidence has `/media_assets = null`, `inputAssets: []`, `executionAuthorized: false`, zero R2 delete operations, and zero Firebase write operations, proving a safe no-op dry run rather than substantive real Task-5 media-asset reconciliation. A second stale/drift audit `019f1416-685e-7c51-96a5-8baa25ee3273` confirmed active 6.9-6.11 status surfaces are aligned before this Batch F record.
  - [x] 6.13 Parent acceptance: real Task-5 traffic has been reconciled within approved operation/cost/time budgets; historical/interim assets are inventoried; deletion is reference-safe; registry/audio backup policies have DR-owner approval, cron proof, and restore-drill evidence; old/new result review audio works; server issuance/range behavior and cross-user denial pass; any touched monolith has before/after line counts, unchanged responsibility boundaries, and future decomposition seams recorded; independent verification passes; solo/live remain on public delivery.
    - Closure: Owner acceptance on 2026-06-29 explicitly accepts selected-teacher proof `prd0055-selected-teacher-1782727843357` with `/media_assets = null`, `inputAssets: []`, `executionAuthorized: false`, and zero R2/Firebase writes/deletes as satisfying Task 6.13 real Task-5 traffic reconciliation because there were no media-asset rows to reconcile in that accepted Task-5 traffic sample. Fresh read-only Firebase CLI shallow proof against project `temp-a1437` confirmed `/media_assets` is `null`, the selected proof draft row exists, the selected proof version row exists, and the legacy proof row printed shallow keys. `output/prd0055-task6-batch-f/independent-verification-summary.json` records the acceptance and commands. No cleanup, deletion, mutation, deploy, staging, commit, push, Task 7, solo/live runtime change, `AudioPlayer.tsx`, Reading V2 runtime internals, or Google Drive behavior occurred.

- [x] 7.0 Align Listening solo/homework runtime presentation after authoring stabilization
  - [x] 7.1 Record explicit product-owner plus architecture reviewer sign-off in findings. Reconcile/regenerate this provisional scaffold against the approved solo/homework child PRD; child PRD wins. Do not continue on mismatch.
    - Closure: Product-owner authorization is the current Codex goal objective file `C:\Users\The Lord\.codex\attachments\0280dcf0-40aa-4b25-bb2b-202fe905bad4\goal-objective.md`, limiting this packet to Task 7.1 through 7.5. Architecture review reconciled the scaffold against PRD-0059 solo/homework sections 4, 6, 9-12, 15-17, 20, 21, and 25; no mismatch requiring stop was found. Child PRD-0059 remains authority for later 7.6+ implementation.
  - [x] 7.2 Start only after Task 5 authoring stability acceptance. Do not include live-session state or teacher authority.
    - Closure: Task 5.0 and Task 6.0 are checked/accepted locally before this packet. Batch A touched only solo/homework baseline tests and documentation; no live-session state, teacher authority, `audioCommand`, `masterAudioState`, Reading V2 runtime, remote mutation, deploy, staging, commit, push, cleanup, deletion, or `AudioPlayer.tsx` source/internal change occurred.
  - [x] 7.3 Capture baseline tests for `ListeningPracticeView`, `AudioPlayer`, solo hooks, timer, autosave, resume, submission, review, and mobile state before visual changes. Baseline coverage does not authorize modifying `AudioPlayer` internals.
    - Closure: Added characterization coverage for `ListeningPracticeView` host answer/playback autosave ownership, hydrated playback seek props, `useSoloTimer`, homework-scoped Listening autosave playback payloads, and mobile playback serialization. Existing baseline coverage was rerun for `AudioPlayer` public contract, `useSoloResume`, `useSoloSubmission`, and saved-result review via `SharedSavedResultCore`. Focused proof passed 8 files / 98 tests.
  - [x] 7.4 Map every solo/homework state owner: answer state, viewed section, current audio index, position, speed, volume, completed audio, timer, autosave, resume, and submission.
    - Closure: State owner map is recorded in findings and implementation log. `ListeningPracticeView` remains owner for answer/current-question/viewed-part/audio-index/position/volume/speed/completed-audio state; `useSoloTimer`, `useSoloAutoSave`, `useSoloResume`, `useSoloSubmission`, `mobileListeningState`, and saved-result review keep their existing bounded responsibilities.
  - [x] 7.5 Select only neutral presentation wrappers already proven in authoring. Do not move playback, persistence, submit, or resume logic into shared components.
    - Closure: Selected authoring-proven neutral primitives are `AssessmentAuthoringHeader`, `AssessmentAuthoringSection`, and `AssessmentStatusState`. `AssessmentValidationSummary` remains deferred for solo/homework runtime because Listening adoption is not proven. `MobileListeningExamScaffold` is recorded as current Listening host-owned presentation shell evidence, not promoted to shared runtime authority in this packet. No playback, persistence, submit, timer, autosave, or resume logic moved into shared components.
  - [x] 7.6 Apply visual alignment incrementally to shell/status/question-card/review display with one focused adopter patch at a time. Task 7 may wrap or configure `AudioPlayer`, but must not modify its internal source, refresh, synchronization, or playback logic; all `AudioPlayer` internal changes belong to Task 8.
    - Closure: Batch B applied one focused solo/homework presentation adopter patch. `ListeningPracticeView` now uses the neutral `AssessmentStatusState` for loading/error and empty direct-question status, adds inert mobile/desktop/review shell markers, and wraps the mobile direct-question body in a bounded question-card surface with safe-area scroll reserve. `MobileListeningAnswerSheet` adds CSS-only safe-area/keyboard reserve for the image-mode answer sheet body/footer. No `AudioPlayer.tsx` source/internal logic changed.
  - [x] 7.7 Preserve `mobileListeningState.ts` semantics and mobile hydration; do not write `audioCommand` or `masterAudioState`.
    - Closure: `mobileListeningState.ts` source was not edited. Existing mobile serialization/hydration semantics were preserved and rerun through `mobileListeningState.test.ts`, `useSoloAutoSave.test.ts`, and `ListeningPracticeView.test.tsx` playback hydration/autosave coverage. Static scoped scans found no `audioCommand` or `masterAudioState` writes in touched source/test files.
  - [x] 7.8 Verify viewport switching does not reset answer/audio state and mobile keyboard does not cover answer controls.
    - Closure: Added a `ListeningPracticeView.test.tsx` viewport-switch rerender test that flips `useMobileExamMode` and proves answers, elapsed timer payload, viewed part, and current audio index remain host-owned across mobile/desktop/mobile branches. Added RTL coverage for direct-question keyboard-safe reserve and `MobileListeningAnswerSheet.test.tsx` coverage for image-mode answer-sheet safe-area reserve. Added Playwright proof at `e2e/prd0055-task7-mobile-keyboard.spec.ts` with a Vite-served real `MobileListeningAnswerSheet` harness at `http://localhost:5174`; the `chromium-mobile` project uses Pixel 5 emulation plus a simulated 240 px keyboard overlay. Report `output/playwright/prd0055-task7-mobile-keyboard/report.json` shows 2 expected, 0 unexpected, 0 skipped, 0 flaky, with screenshots for 375 px and 320 px.
  - [x] 7.9 Add solo/homework submit race protection:
    - double-click/retry uses one idempotent submit operation;
    - pending submit disables duplicate action;
    - when time-up occurs during an accepted answer save, finish that in-flight save where possible, then execute one submit;
    - stale/failed save produces recoverable state without duplicate result records;
    - tests cover timer/save/submit ordering and reload recovery.
    - Closure: Batch C adds stable Listening solo attempt/result identity, passes it through autosave and submission, extends autosave with `waitForAcceptedSave()` and `flushNow()`, gates host time-up through one submit sequence, adds a synchronous submission lock/dedupe ref, and writes stable result IDs with existing-operation recovery in `saveTestResult`. Focused tests cover rapid submit dedupe, stable operation options, autosave flush/failure/in-flight join, idempotent result writes, and time-up wait/flush/submit ordering. Final focused proof passed 7 files / 142 tests; current mandatory GPT-5.5 medium verifier `019f1500-7ae7-7ec1-a5e6-79388678cba0` and GPT-5.4-mini high stale/drift explorer `019f1500-8f05-7fa2-9b21-b8f80af8b92a` returned PASS after Batch C doc/artifact recheck.
  - [x] 7.10 Add student runtime accessibility/mobile verification:
    - loading/error/validation states expose screen-reader semantics;
    - color is not the only state signal;
    - icon-only controls have accessible names;
    - keyboard and screen-reader navigation remain usable;
    - answer inputs remain visible above mobile keyboard;
    - visible touch controls meet the 44px by 44px floor;
    - shared mobile primitives are used only where the traceability matrix proves neutral contracts.
    - Closure: Batch C adds submit-sheet `role="alert"`/`aria-describedby`, submit `aria-busy`, low-time timer `role="status"`/`aria-live`/accessible warning label, and focused RTL proof for submit-sheet validation semantics, busy copy, 48 px submit/cancel controls, keyboard focusability, 44 px header controls, and low-time timer accessible naming. Existing Batch B keyboard-safe answer-region proof remains the mobile keyboard evidence; Batch C adds browser a11y/touch-target/no-horizontal-overflow proof for submit-sheet/header at desktop 1440 px, phone 375 px, and phone 320 px. No new native-keyboard proof is claimed in this packet. Final focused proof passed 7 files / 142 tests; Batch C Playwright proof passed 6 expected, 0 unexpected/skipped/flaky; current mandatory reviewers returned PASS after doc/artifact recheck.
  - [x] 7.11 Integrate solo/homework private delivery at the solo host/adapter boundary using Task 5's shared legacy resolver and Task 6's issuance service. Keep live traffic public. If safe solo refresh/cutover requires modifying `AudioPlayer` internals, block only the cutover, complete Task 8's approved shared-player delivery subphase, then return to Task 7 proof; never cross the protected live boundary early.
    - Closure: Batch D adds a bounded solo delivery adapter/client under `src/features/assessment/listening/runtime/solo/`, extends Task 6 authorized delivery with `soloScope` test/version/student authority, and wires `ListeningPracticeView` to resolve asset-ID solo/homework section audio before passing a URL to `AudioPlayer`. Legacy public sections continue through the shared legacy resolver without calling the issuer. Authorized URLs are not persisted into autosave/mobile state. Live session traffic remains public; no `AudioPlayer.tsx` internals, `audioCommand`, `masterAudioState`, `useAudioSync`, Cloudflare deployment, Google Drive behavior, remote mutation, staging, commit, push, or Task 8 work occurred.
  - [x] 7.12 Human-assisted pre-cutover gate: run focused runtime/mobile/a11y tests plus public/private solo proof at `localhost:5174` for desktop, 375 px, 320 px, iOS Safari, resume, time-up/submit, long playback, URL expiry/refresh, and legacy/new test records. Every Playwright run uses `--reporter=json > report.json`.
    - Closure: Batch D focused proof passed `rtk npx vitest run src/features/assessment/listening/storage/listeningAssetDelivery.service.test.ts src/features/assessment/listening/runtime/solo/listeningSoloDeliveryAdapter.test.ts src/features/assessment/listening/runtime/solo/listeningSoloDeliveryClient.test.ts src/components/practice/ListeningPracticeView.test.tsx` with 4 files / 55 tests. Localhost proof passed `rtk proxy cmd /c "npx playwright test e2e/prd0055-task7-solo-delivery.spec.ts --config=playwright.prd0055-task7-batch-d.config.js --reporter=json > output\prd0055-task7-batch-d\report.json"` at `http://localhost:5174` with 8 expected, 0 unexpected, 0 skipped, 0 flaky across desktop Chromium, 375 px Chromium, 320 px Chromium, and iOS Safari/WebKit projects. It covers authorized asset-ID solo delivery, legacy public playback, byte-range reads, no browser-supplied student authority, resume checkpoint state, one time-up submit, 3590-second long-playback checkpoint, URL expiry/refresh delegation, and legacy/new test records. Safe automatic refresh/source handoff through `AudioPlayer` internals remains blocked until Task 8 if rollout requires it; Batch D proves the adapter refresh contract only.
  - [x] 7.13 Perform Task-7-local solo rollout:
    - internal fixtures;
    - selected teacher/student solo or homework traffic;
    - percentage rollout only after playback/resume metrics remain healthy;
    - keep live session traffic public;
    - stop on reload, seek, refresh, resume, mobile, or legacy playback regression.
    - Closure: Batch E closes the Task-7-local rollout for internal fixtures only. Added `src/features/assessment/listening/runtime/solo/listeningTask7LocalRollout.ts` and `.test.ts` to evaluate local rollout evidence and block missing fixture proof, unauthorized selected traffic claims, percentage rollout without healthy playback/resume metrics, reload/seek/refresh/resume/mobile/legacy playback regressions, live private switching, `AudioPlayer.tsx` source/internal changes, `audioCommand`/`masterAudioState` writes, Reading V2 runtime touch, Task 8 start, deploy, remote mutation, cleanup, and object deletion. Focused proof passed 3 tests; JSON proof is `output/prd0055-task7-batch-e/local-rollout-report.json`; summary is `output/prd0055-task7-batch-e/local-rollout-summary.json`. No selected teacher/student solo or homework traffic is claimed because this packet has no explicit authorization. No percentage rollout is claimed because healthy playback/resume metrics and authorization are separate future gates. Live session traffic remains public.
  - [x] 7.14 Run the mandatory fresh-context independent verification pass from the Verification Standard. Reviewer must inspect `ListeningPracticeView.tsx` and other triggered large-file maps/touch regions, prove `AudioPlayer` internals are untouched, rerun submit/time-up/private-delivery mutation proofs, and verify mobile/accessibility/legacy behavior; resolve all findings and obtain clean re-review.
    - Closure: Fresh read-only GPT-5.5 medium verifier `019f154f-607c-79b0-a623-7f1ec8465465` returned PASS with no findings. It inspected `ListeningPracticeView.tsx`, the large-file map, `AudioPlayer.tsx`, public `AudioPlayer` contract tests, solo delivery adapter/client/service tests, solo autosave/submission/resume/timer tests, mobile Listening tests, taskbox/findings/traceability/log, and existing Playwright JSON artifacts. It reran focused local proof with 16 files / 183 tests passed, confirmed `git diff -- src/skills/listening/components/AudioPlayer.tsx` and `git ls-files -m -- AudioPlayer.tsx` were empty, parsed Batch D/C/B browser reports as expected-only, and found no live private switch, `audioCommand`, `masterAudioState`, Reading V2 runtime, Task 8, deploy, cleanup, delete, or remote-mutation drift.
  - [x] 7.15 Parent acceptance: solo/homework looks aligned, selected traffic uses proven private delivery when host-level integration is sufficient, resume/autosave/timer/playback/idempotent-submit/review remain correct, accessibility/mobile requirements pass, any touched monolith has before/after line counts, unchanged responsibility boundaries, and future decomposition seams recorded, independent verification passes, `AudioPlayer` internals remain untouched, and no live authority path is read or written.
    - Closure: Task 7 parent acceptance is local/internal-fixture scoped. Task 7.1-7.15 source, tests, browser artifacts, large-file maps, taskbox, findings, traceability, and implementation log now agree with live truth. Solo/homework presentation, resume/autosave/timer/playback/idempotent submit/review, mobile/accessibility, legacy public playback, and host/adapter-level private solo delivery have focused proof. `ListeningPracticeView.tsx` large-file map records before/after line counts, unchanged responsibility boundaries, and future seams. Independent verification and stale/drift exploration passed after Batch E sync. `AudioPlayer.tsx` internals remain untouched; no live authority path, `audioCommand`, or `masterAudioState` was read or written by Task 7 work. No Task 8+ work is checked or started.

- [ ] 8.0 Define, test, and implement protected live Listening authority and runtime alignment
  - Current governing scope correction, 2026-07-01: remaining Task 8.14-8.18 proof is localhost-only. Live-domain/deployed proof is not a current unlock step and must not be pursued until a future deployment/rollout PRD exists. Historical deployed artifacts in this tasklist remain retained evidence only.
  - Superseding closure-execution status, 2026-06-30: product owner reauthorized PRD-0055 final closure execution after the earlier local-only scope change. Current evidence includes browser-reachable local WAV fixtures, internal dev RTDB fixture session `T8P9J2`, Browser plugin media/progress state, user-confirmed audible teacher monitor proof with no wrong audio/no interruption/no visible drift, local Playwright matrix rerun, focused progress-duration regression proof, deployed/current Cloudflare/Firebase readback, live/private Worker activation with active version `993acdc9-dd93-4ee8-8764-15847146ac3a` at 100%, Hosting bundle readback for `/listening-delivery/live`, internal deployed live fixture `T8D116` issue/content/wrong-section proof, selected-class deployed browser proof `T843A5`, active-version pin deployment `7d32be9d-1470-4c82-bb6a-8782a80de1c9`, active-equivalent candidate `f217034a-4a21-48be-85d1-5b629ebd70b8`, 1% percentage rehearsal deployment `b8b6435d-bba6-4951-a2a0-6a5d8e140da3`, restore deployment `fd709c5b-c470-4c52-a3c2-1a7c1d4c18c1`, post-pin/split/restore smoke sessions `T8TDAS`, `T8HVWE`, `T8QYZU`, and focused Worker/RTDB auth-negative proof. Task 8.14, 8.15, 8.16, 8.17, and 8.18 remain unchecked and BLOCKED, not PASS, because selected-user acceptance, full rollout evidence, complete final deployed/private browser-role matrix, and final verification are still missing. No cleanup/delete, commit, push, merge, or checkbox closure occurred.
  - [x] 8.1 Record explicit product-owner plus architecture/security reviewer sign-off in findings. Reconcile/regenerate this provisional scaffold against the approved live-session child PRD/test plan; child PRD wins. Do not continue on mismatch.
    - Closure: Batch A records the current user/product-owner goal objective as explicit authorization for Task 8.1-8.4 only and records architecture/security review scope in findings. Reconciliation against PRD-0060 found no schema/contract/test mismatch for an inert foundation packet. PRD-0060 remains the authority for later behavior; its rollout/runtime/cutover gates still block Task 8.5+ and `DAG-81`.
  - [x] 8.2 Define canonical `masterAudioState` schema with monotonic revision, trusted server timestamp, section, position, speed, playing state, action metadata, and validation rules.
    - Closure: Added bounded live-session authority contract files under `src/features/assessment/listening/live-session/authority/`: `masterAudioState.types.ts`, `masterAudioState.validation.ts`, and focused tests. The validator covers schema version, monotonic revision, trusted timestamp markers, section, position, speed, playing state, teacher writer, command/heartbeat action consistency, browser/client non-authority, and metadata leakage bans for signed URLs/raw keys/secrets/audio/student/result data.
  - [x] 8.3 Define compatibility `audioCommand` contract and retirement criteria. Commands mirror canonical state transactions and cannot override a newer revision.
    - Closure: Added `audioCommandCompatibility.ts` and focused tests for command projection from accepted canonical command updates, command-to-canonical identity, stale command rejection, future-command canonical reread, legacy command rejection after v2 acceptance, and explicit retirement criteria. This is compatibility contract only; no existing live writer or runtime consumer was changed.
  - [x] 8.4 Write failing tests for authority conflicts, stale command rejection, late join, student reload, teacher reload, buffering during pause, long-pause resume, skip, seek, speed, network partition, teacher disconnect, session end during submit, authorized URL refresh, source handoff without interruption, and expiry retry.
    - Closure: Added `liveAuthorityFutureGaps.test.ts` with a guarded `it.fails` RED matrix for every required future runtime gap. The first unguarded RED run failed with exit 1 before behavior implementation, proving these live runtime cases remain unimplemented by Batch A. Default focused suite now stays green while retaining RED evidence. No `AudioPlayer.tsx` internals, `useMonitorControls`, `useMasterAudioState`, `useAudioSync`, `ListeningTestPage`, teacher monitor UI, live traffic, delivery cutover, or Task 8.5+ behavior changed.
    - Verification: Required final verifier `019f1576-3d5a-7f02-a32f-f9f0f30e8f1a` and stale/drift explorer `019f1576-9657-7d42-8fd7-a4c9f7b1cbee` passed with no findings. Focused authority suite, `git diff --check`, exact UTF-8, taskbox checkbox scan, and protected live-runtime diff scan passed.
  - [x] 8.5 Unify `TeacherTestControlBar` and `AudioProgressPanel` behind one authority transaction so default section `1`/position `0`/speed `1.0` cannot overwrite richer state.
    - Closure: Batch B adds `liveAudioAuthorityTransaction.ts` and routes `useMonitorControls`, `TeacherTestMonitorPage`, and `AudioProgressPanel` through the same canonical transaction builder. Teacher actions require a hydrated authority snapshot, increment `masterAudioState.revision`, emit `audioCommand` only as compatibility projection, and avoid default section/position/speed overwrite. `TeacherTestControlBar` continues to call monitor callbacks; those callbacks now enter the same authority writer as `AudioProgressPanel`.
  - [x] 8.6 Consolidate all required `AudioPlayer` internal changes in this task. Update `useMonitorControls`, `useMasterAudioState`, `useAudioSync`, `ListeningTestPage`, and `AudioPlayer` only as required by the approved authority and delivery contracts; re-run both solo and live playback tests because `AudioPlayer` is shared.
    - Closure: Batch B keeps direct shared-player source changes out of `AudioPlayer.tsx`; the required shared-player behavior is consolidated in `useAudioSync` and existing `AudioPlayer` integration continues through its hook contract. Updated live authority wiring in `useMonitorControls`, `useMasterAudioState`, `useAudioSync`, `ListeningTestPage`, monitor components, `useTestSession`, `audio.types.ts`, and feature tracking only. Focused live proof, existing live Listening characterization tests, shared `AudioPlayer` tests, solo/homework playback/autosave/submission tests, and Reading V2 boundary tests passed locally.
  - [x] 8.7 Implement late join/reload from canonical authority: hydrate state first, account for elapsed trusted time, then drift-correct; never restore solo/local playback authority.
    - Closure: Batch B adds `liveAudioRuntimeHydration.ts` and wires `ListeningTestPage`/`useTestSession` so canonical `masterAudioState` revision wins before compatibility commands. Student reload/late join derive section, position, speed, and play state from canonical authority plus elapsed trusted time; legacy/local playback restore cannot override accepted v2 authority.
  - [x] 8.8 Use 500 ms soft-correction and 2-second hard-seek values only as initial test baselines. Record measured browser results and obtain approval before setting final product thresholds.
    - Closure: Batch B adds `liveAudioSyncPolicy.ts` with 500 ms soft-correction and 2-second hard-seek values explicitly scoped as unit/integration test baselines. No browser-measured threshold approval or final product threshold is claimed; those remain future 8.14+ gates.
  - [x] 8.9 Preserve headphone pending/approved/denied behavior and teacher visibility; do not bypass readiness gates.
    - Closure: Batch B preserves pending/approved/denied request state and fixes teacher visibility for denied requests in `HeadphoneRequestPanel`. Focused tests prove pending controls, approved display, and denied display remain visible; no readiness gate bypass was added.
  - [x] 8.10 Prove teacher disconnect freezes playback and network loss pauses after approved bounded grace; recovery resumes only from canonical authority.
    - Closure: Batch B adds disconnect policy proof in `liveAudioSyncPolicy.ts`/`useAudioSync.test.tsx`: after the bounded 10-second local grace, stale canonical authority pauses playback and marks sync loss; a newer canonical revision clears the loss state and resumes/alignment only from canonical authority. This is local unit/integration proof, not browser cutover proof.
  - [x] 8.11 Define and implement load-test harness in child PRD for 100 students/session and 20 concurrent sessions, including client fidelity, network conditions, Firebase/worker limits, sync drift, audio failures, and pass/fail thresholds.
    - Closure: Batch C adds a local/dry-run-only deterministic load-test harness under `src/features/assessment/listening/live-session/tests/load/listening-live/` with bounded `config`, `scenarios`, `virtualTeacher`, `virtualStudent`, `metrics`, and `report` modules plus focused tests. The harness models 20 sessions, 100 students per session, 20 teacher writers, five deliberate two-monitor contention sessions, two-second heartbeats, 10-minute ramp / 30-minute steady / 10-minute drain, eight network profiles, synthetic teacher authority writes, synthetic student listeners, local sync drift, audio/source failure metrics, quota utilization, sanitized report output, and pass/fail stop criteria. Production execution is forbidden; isolated non-production remote execution is blocked unless an explicit approval reference, isolated project ID, cleanup plan, and remote-mutation gate are present. Focused RED failed before the harness modules existed; focused GREEN passed 7 Task-8 authority/load files / 42 tests, and adjacent live protection proof passed 4 files / 16 tests. No live traffic behavior, `AudioPlayer.tsx`, private live delivery/source handoff, teacher-monitor accessibility expansion, browser pre-cutover gate, rollout, parent Task 8.0 acceptance, Task 9, deploy, staging, commit, push, cleanup, deletion, or remote mutation occurred.
  - [x] 8.12 Implement live authorized-delivery refresh/source handoff in `AudioPlayer` while keeping production live traffic public:
    - old URL remains active until replacement is ready;
    - refresh failure does not independently pause playback;
    - retry uses bounded backoff;
    - canonical teacher authority survives source refresh;
    - teacher monitor receives an actionable warning before URL-expiry/interruption risk;
    - warning clears after recovery and does not leak signed URLs or raw keys.
    - Closure: Batch D adds an inert optional `authorizedDelivery` contract inside `AudioPlayer` only. Existing live traffic remains public unless a caller provides a refresh source. Focused RED failed before source-refresh behavior existed; GREEN proves old-source retention until replacement readiness, bounded retry without independent pause, warning issue/clear, no signed URL/raw-key leak, and canonical `masterAudioState` preservation through source handoff. Browser private-delivery cutover remains Task 8.14+.
  - [x] 8.13 Add teacher-monitor/live accessibility verification:
    - monitor controls remain keyboard reachable;
    - icon-only controls have accessible names;
    - pause/resume/skip/seek/headphone states are not conveyed by color alone;
    - loading/error/sync-loss/refresh-warning semantics use appropriate status/alert roles;
    - visible controls meet the 44px by 44px touch-target floor where applicable;
    - accessibility changes do not move authority into shared presentation code.
    - Closure: Batch D adds focused live/teacher-monitor accessibility tests and small Listening-owned UI fixes for keyboard reachability, accessible names, status/alert semantics, non-color-only state labels, and 44px touch targets in `AudioPlayer`, `StudentProgressCard`, `AudioProgressPanel`, and `TeacherTestControlBar`. Authority stays in existing live authority code; no shared presentation authority move occurred.
  - [x] 8.14 Human-assisted pre-cutover gate: run automated integration/a11y tests plus teacher/student private-delivery browser proof in separate contexts before switching live traffic. Every Playwright run uses `npx playwright test <optional-spec> --reporter=json > report.json`:
    - teacher desktop `localhost:5173`;
    - student desktop `localhost:5174`;
    - student mobile `localhost:5174`;
    - normal/late join;
    - teacher/student reload;
    - pause/resume/skip/seek/speed;
    - buffering, stale command, authority conflict;
    - headphone states;
    - accepted/rejected submit during session end.
    - Current final-closure execution evidence, not checkbox closure: 2026-06-30 localhost fixture proof was extended so the in-app browser can fetch actual local WAV files from `public/__prd0055-task8-local/section-1.wav` and `section-2.wav` instead of relying only on Playwright route fulfillment. Internal dev RTDB fixture session `T8P9J2` uses those URLs and loads in the teacher monitor at `http://localhost:5173/teacher-test/T8P9J2`; Browser plugin state shows audio `readyState` 4, source under `http://localhost:5174/__prd0055-task8-local/`, duration `20`, and seek max `20`. Human proof is recorded in `output/prd0055-task8-local-unblock/browser-plugin-current-teacher-t8p9j2-human-audible-confirmed.json`: the user manually clicked play in the teacher Audio Control Panel, heard the Browser tone, saw progress advance, and observed no wrong audio, interruption, or visible drift. `AudioProgressPanel` uses measured media duration when metadata differs, and this packet also adds fractional `step="any"` to the teacher monitor seek slider after deployed diagnostics showed whole-second quantization on a two-second clip; focused local regression evidence passed in `src/components/test/AudioProgressPanel.test.tsx`. Expanded local matrix proof after the fixture fix passed in `output/prd0055-task8-local-unblock/playwright-task8-after-browser-audio-fix-180s-report.json` and continues to cover teacher desktop reload hydration, student desktop reload, student mobile late join at 375px and 320px, pause/resume, skip/seek/speed, deterministic buffered/stalled audio during teacher pause staying paused/time-pinned, stale `audioCommand` rejection, equal-revision authority conflict rejection, headphone pending/approved/denied visibility, teacher End/result indexing, and post-End duplicate-submit rejection. Live/private delivery implementation is now present in source and deployed Worker/Hosting proof: active Worker version `993acdc9-dd93-4ee8-8764-15847146ac3a` exposes `/listening-delivery/live`, `/solo`, and `/content`; Hosting asset readback contains `/listening-delivery/live`; internal deployed fixture `T8D116` issued live delivery 200, served byte-range content 206, and denied wrong-section access 403. Additional selected-class internal deployed proof now exists for fixture session `T843A5`: `output/prd0055-task9-live-readback/prd0055-selected-class-live-1782839559853.json` passed Firebase readback, teacher/student live issue 200, content 206, refresh gate, cross-owner 403, and wrong-section 403; `output/prd0055-task9-live-readback/selected-class-deployed-browser-report-1782839559853-progress.json` passed deployed teacher desktop plus student desktop/mobile browser proof, with screenshots and network/audio/progress diagnostics under `output/prd0055-task9-live-readback/prd0055-selected-class-live-1782839559853-browser/`. Full 8.14 remains open because the complete pre-cutover matrix across deployed reload/late-join/pause/skip/seek/speed/buffering/authority/headphone/submit-end cases, rollout/rollback/evidence-capture gates, independent verification, and parent Task 8 acceptance remain open.
    - Current localhost-only reconciliation, checkbox closure: 2026-07-01 current local artifacts prove the localhost browser/human portion of Task 8.14: `playwright-task8-expanded-report.json` passed 1 expected / 0 unexpected, `local-matrix-supplement.json` covers teacher desktop, student desktop, student mobile 375px and 320px, reload, late join, pause/resume, skip/seek/speed, buffering during pause, stale command rejection, equal-revision authority conflict rejection, headphone states, End/result indexing, and duplicate-submit rejection, and `browser-plugin-current-teacher-t8p9j2-human-audible-confirmed.json` records human audible/no-wrong-audio/no-interruption/no-visible-drift proof. Under the current localhost-only boundary, the original pre-cutover/live-traffic acceptance, selected/full rollout, and deployed proof are future-deferred non-gates.
    - Superseding false-gate removal and Task 8.17 PASS, checkbox closure: 2026-07-01 live-traffic, selected/full rollout, and deployed proof are future-deferred non-gates. The current Task 8.14 local browser/human slice is proven and Task 8.14 is closed for the localhost-only packet. Parent Task 8 remains unchecked until Task 8.15, Task 8.16, Task 8.18, and parent reconciliation are resolved.
  - [x] 8.15 Record Task-8 live rollout deferral for the current localhost-only packet; do not perform live-server rollout until a future deploy/rollout PRD explicitly reopens it:
    - internal live sessions are historical retained evidence only for this packet;
    - selected teachers/classes are future-deferred rollout scope;
    - percentage rollout with rollback control is future-deferred rollout scope;
    - stop conditions remain recorded for future rollout: sync drift, source interruption, headphone regression, wrong section/position, or submit/session-end failure.
    - Current blocker evidence, not checkbox closure: 2026-06-30 Task 8.15 remains `BLOCKED_NOT_CLOSURE` because no selected-user rollout acceptance or full rollout proof has passed. Final closure execution authorization exists. Internal fixture class/session proof passed for selected-class live private delivery (`T843A5`, `prd0055-selected-class-live-1782839559853-class`) with deployed API and browser evidence. A constrained active-version pin rehearsal kept Worker version `993acdc9-dd93-4ee8-8764-15847146ac3a` at 100% on deployment `7d32be9d-1470-4c82-bb6a-8782a80de1c9`; equivalent candidate `f217034a-4a21-48be-85d1-5b629ebd70b8` matched active script/bindings; percentage rehearsal deployed active at 99% and equivalent candidate at 1% on `b8b6435d-bba6-4951-a2a0-6a5d8e140da3`, then restored active to 100% on `fd709c5b-c470-4c52-a3c2-1a7c1d4c18c1`. Post-pin/split/restore internal fixtures `T8TDAS`, `T8HVWE`, and `T8QYZU` passed teacher/student live issue 200, content 206, cross-owner 403, and wrong-section 403. This is recovery/percentage-path evidence, not selected-user rollout acceptance or full rollout.
    - Current localhost-only scope note: 2026-07-01 Task 8.14 is now closed for the localhost-only packet, and Task 8.15 closes as a recorded rollout deferral/non-action, not rollout execution. User confirmed: "pass, no rollout to live server anytime soon."
    - Superseding false-gate removal, checkbox closure: 2026-07-01 selected-user rollout, percentage rollout, full rollout, and production recovery proof are future-deferred non-gates. Historical selected-class/API/browser, active-version pin, equivalent-candidate, split/restore, and post-restore smoke artifacts remain retained evidence only; they are not current blockers and no live rollout occurred.
  - [x] 8.16 Record network writes, canonical durable DB state, screenshots/traces, `report.json`, measured drift, delivery refresh/warning, accessibility evidence, rollout cohort, and recovery evidence. Unit tests alone cannot close this task.
    - Current partial evidence, not checkbox closure: 2026-06-30 Task 8.16 remains `PARTIAL_LOCAL_AND_DEPLOYED_API_BROWSER_EVIDENCE_NOT_CLOSURE`. Local screenshots, JSON reports, canonical fixture state, Browser plugin human-audible/no-wrong-audio/no-interruption artifacts, Cloudflare/Firebase current readback, active live/private Worker route proof, internal deployed live fixture `T8D116`, selected-class deployed browser proof for `T843A5`, active-version pin readback at deployment `7d32be9d-1470-4c82-bb6a-8782a80de1c9`, equivalent-candidate and 1% split/restore proof, post-pin/split/restore smokes `T8TDAS`, `T8HVWE`, `T8QYZU`, progress-bar diagnostics, and fresh Worker/RTDB auth-negative reports exist. Missing proof: selected-user rollout acceptance, full rollout evidence, final deployed/private browser evidence-capture review, and final acceptance review.
    - Superseding false-gate removal, checkbox closure: 2026-07-01 deployed/private browser evidence-capture review, selected-user rollout, and full rollout are future-deferred non-gates. Current evidence-capture requirements are limited to localhost artifacts, local JSON reports/screenshots, canonical fixture state, local authorization proof, local Section 27 evidence, and docs reconciliation.
    - Current localhost-only closure: 2026-07-01 local evidence capture is complete for this packet through browser/human proof `T8P9J2`, local Playwright JSON reports, matrix supplement, local authorization/security reports, Section 27 localhost audit, build/test/static proof summaries, scoped UTF-8 acceptance, and current status/canonical audit artifacts. No live rollout cohort or production recovery evidence is claimed.
  - [x] 8.17 Run the mandatory fresh-context independent verification pass from the Verification Standard. Reviewer must inspect all triggered live large-file maps/touch regions, verify protected authority paths and undeclared hunks, rerun authority/delivery/accessibility mutation proofs, challenge load-test evidence, and recheck solo regressions from shared `AudioPlayer` edits; resolve all findings and obtain clean re-review.
    - Fresh independent blocker audit, not checkbox closure: 2026-07-01 `output/prd0055-task9-live-readback/prd0055-helmholtz-independent-verifier-summary.json` records Helmholtz `BLOCKED`; usable as blocker audit only, not Task 8.17 PASS.
    - Fresh local-only independent blocker audit, not checkbox closure: 2026-07-01 `output/prd0055-task9-local-readiness/prd0055-mencius-local-independent-verifier-summary.json` records Mencius `BLOCKED`; at audit time it confirmed target taskboxes remained unchecked, Section 27 local audit was clean locally at 85 rows / 77 current-local / 8 approved future deferrals / 0 local blockers with `REG-79` accepted, and no scoped stale `REG-79` blocker wording remained. It is usable as historical blocker/stale-doc audit evidence only, not Task 8.17 PASS or current taskbox truth after Task 8.14/8.17 closure.
    - Current blocker evidence, not checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/prd0055-final-closure-blocker-audit-report.json` records Task 8.17 as `BLOCKED_NOT_CLOSURE` because a fresh-context independent verification for full Task 8.14-8.18 closure cannot honestly pass until final 8.14-8.16 evidence exists.
    - Superseding false-gate removal, not checkbox closure: 2026-07-01 Mencius remains useful as stale-doc/blocker evidence, but its final blocker rationale referenced future live/deploy/rollout gates that are now non-gates. Pauli later supplies the current Task 8.17 PASS.
    - Closure: 2026-07-01 Pauli (`019f1d94-ccdc-7fc3-bb0b-cc16c8583eaf`) returned fresh independent localhost-only PASS for Task 8.17 only. Durable summary `output/prd0055-task9-local-readiness/prd0055-pauli-task817-local-independent-pass-summary.json` records inspected task/docs/status artifacts, accepts the localhost Task 8.14 browser/human proof, accepts REG-79 local private-delivery proof, confirms live/deploy/rollout/human-production/rollback false gates are future non-gates, and recommends checking only 8.17. This does not close 8.18, 9.0, or 9.1-9.15.
  - [x] 8.18 Parent acceptance: teacher authority remains canonical, monitor controls agree, solo state remains separate, `AudioPlayer` changes pass both solo/live suites, monitor warning/accessibility requirements pass, load target passes approved methodology, every touched monolith has before/after line counts, unchanged responsibility boundaries, and future decomposition seams recorded, independent verification passes, and selected live traffic survives reload/conflict/private-delivery refresh.
    - Current blocker evidence, not checkbox closure: 2026-06-30 final closure blocker audit records Task 8.18 as `BLOCKED_NOT_CLOSURE` because selected live traffic reload/conflict/private-delivery refresh proof plus parent Task 8 acceptance proof remain absent.
    - Current localhost-only scope note: 2026-07-01 Task 8.14, Task 8.15, Task 8.16, and Task 8.17 are now closed for the localhost-only packet. Task 8.18 closes because the current local-only acceptance surface is reconciled: canonical teacher authority, monitor controls, solo separation, `AudioPlayer` live/solo regressions, monitor warning/accessibility proof, load-harness methodology, large-file maps, and independent verification are recorded. Selected-live-traffic survival is future-deferred and not a current gate.

    - Superseding false-gate removal and Task 8.17 PASS, checkbox closure: 2026-07-01 selected-live-traffic survival is future-deferred and not a current gate. Task 8.18 is checked for the local-only packet after task/docs reconciliation.
    - Current closure after Task 8.14 through Task 8.18 local-only closure: 2026-07-01 Task 8 child target boxes are closed for the current packet. Parent Task 8.0 remains outside the exact target packet unless separately authorized.

- [x] 9.0 Complete compatibility verification, staged rollout, observability, documentation, and final acceptance
  - Current governing scope correction, 2026-07-01: remaining Task 9 proof is localhost-only unless a future deploy/rollout PRD explicitly reopens live-domain work. Selected-user rollout, percentage rollout, full rollout, production browser acceptance, and deployed/current documentation truth are future gates, not current unblock steps.
  - Current blocker evidence, not checkbox closure: 2026-06-30 final closure blocker audit records Task 9.0 as `BLOCKED_NOT_CLOSURE` with 22 remaining unchecked taskboxes across Task 8.0, Task 8.14-8.18, and Task 9.0-9.15. Deployed/live truth, final rollout, and parent acceptance remain absent.
  - Current requirements matrix evidence, not checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/prd0055-requirements-evidence-matrix-report.json` maps explicit objective requirements to current evidence and remains closure-blocked. Full 9.0 remains open because the human-assisted final packet, selected-user/full rollout acceptance, final independent verification, Section 27 row execution, and production-truth gates remain incomplete even though live/private Worker API proof, active-version pin proof, equivalent-candidate proof, 1% percentage rehearsal, and restore proof now exist.
  - Superseding closure-execution status, 2026-06-30: product owner reauthorized final closure execution after the earlier local-only deferral. Deployed/current readback now includes Cloudflare deployment `fd709c5b-c470-4c52-a3c2-1a7c1d4c18c1`, active Worker version `993acdc9-dd93-4ee8-8764-15847146ac3a` at 100%, delivery secret coverage including `LISTENING_DELIVERY_SECRET`, Hosting bundle readback for `/listening-delivery/live`, internal deployed fixture `T8D116`, selected-class deployed browser proof `T843A5`, post-pin/split/restore live smoke sessions `T8TDAS`, `T8HVWE`, `T8QYZU`, worker live issue 200, content byte-range 206, wrong-section 403, and RTDB/Worker auth-negative proof reports. Task 9.0 and Task 9.1 through Task 9.15 remain unchecked and BLOCKED, not PASS, pending final rollout/cohort acceptance, full rollout proof, final deployed/private browser role proof, final independent verification, full Section 27 execution, and parent acceptance.
  - Fresh independent blocker audit, not checkbox closure: 2026-07-01 Helmholtz (`019f19b7-e4fc-7913-97c6-37730f5cf210`) returned `BLOCKED` in `output/prd0055-task9-live-readback/prd0055-helmholtz-independent-verifier-summary.json`. The audit found no invalid checkbox closures and confirms current selected-class/pin/1 percent/restore proof is useful partial evidence, but Task 9.0 remains blocked by missing selected-user acceptance, full rollout, final human-assisted production browser packet, final independent verification, full Section 27 execution, and parent acceptance.
  - Canonical closure audit, not checkbox closure: 2026-07-01 `output/prd0055-task9-live-readback/prd0055-canonical-closure-audit-report.json` records `CANONICAL_CLOSURE_AUDIT_BLOCKED_NOT_PASS`; it supersedes older local-readiness report details only for active deployment/version, live/private route availability, and percentage rehearsal/restore truth while preserving 9 missing gates.
  - Superseding localhost-only status, 2026-07-01: live/deployed/rollout evidence is retained as historical/future-PRD context only and is no longer a current gate. After Task 8.14 and Pauli's Task 8.17 PASS, `output/prd0055-task9-live-readback/prd0055-rollout-current-status.json` records the current blocker as Task 9.11 / parent acceptance final-check wording versus repo-wide tracked UTF-8 failures in pre-existing out-of-scope files.
  - Superseding Task 8.17 status, 2026-07-01: Pauli fresh independent localhost-only verifier PASS closes 8.17. After owner scoped-pass clarification and local-only reconciliation, Task 8.14 through Task 8.18 and Task 9.1 through Task 9.15 are checked for the current localhost-only packet.
  - Current localhost-only parent closure: 2026-07-01 Task 9.0 is checked only for the local packet. Live-domain/deployed proof, selected-user rollout, percentage rollout, full rollout, human production acceptance, and production rollback/recovery are excluded from current pass criteria and deferred to a future deploy/rollout PRD. No live rollout or production deploy occurred.
  - [x] 9.1 Verify every prior parent task has focused tests, findings evidence, rollback notes, documentation updates, all approved independently reviewable/revertible packets merged and traceable, and its required internal/selected-user rollout evidence before final rollout.
    - Current local readiness evidence, not checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/task9-compatibility-readiness-report.json` records `LOCAL_COMPATIBILITY_READINESS_PASS_NOT_CLOSURE` and confirms parent Task 8.0 remains unchecked, so final prior-parent readiness cannot close. Full 9.1 remains open until every prior parent packet and required rollout/deployed truth are reviewed together.
    - Current localhost-only closure: 2026-07-01 prior-parent local readiness is accepted for this packet through checked Task 8.14-8.18 target children, existing earlier parent evidence, compatibility readiness, findings/traceability/log updates, and future deferral of selected-user/final rollout. Parent Task 8.0 remains outside the exact target packet unless separately authorized.
  - [x] 9.2 Verify existing Reading V2 create/import/draft/revision/publish/projection/runtime/result flows remain unchanged by shared presentation work.
    - Current local readiness evidence, checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/task9-compatibility-readiness-report.json` maps Reading V2 compatibility to `output/prd0055-task8-local-unblock/cross-system-compat-report.json`, which passed 26 suites / 144 tests with Reading V2 Studio, launch/listing/runtime integration, trusted submit, router, and saved-result coverage. Deployed/live compatibility is future-deferred and not a current gate.
  - [x] 9.3 Verify existing R2-backed Listening tests remain readable through legacy adapters, new writes use approved contracts, and a past result with raw public R2 URL still plays after private-delivery switch.
    - Current local readiness evidence, checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/task9-compatibility-readiness-report.json` maps legacy/public and new authorized Listening compatibility to `SharedSavedResultCore.test.tsx`, `listeningTestStorage.test.ts`, `r2Storage.test.ts`, and `listeningSoloDeliveryAdapter.test.ts` titles inside the 26-suite / 144-test cross-system pass. Deployed/private/live compatibility and final rollout evidence are future-deferred non-gates for this packet.
  - [x] 9.4 Verify this tasklist made no Google Drive behavior change. Separate cleanup/deletion task remains independently scoped.
    - Current local readiness evidence, checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/task9-compatibility-readiness-report.json` reuses `output/prd0055-task8-local-unblock/local-gate-scan-report.json` and current changed-path inventory to confirm zero Google-Drive-named behavior paths changed; Google Drive cleanup/deletion remains a separate approved deferral.
  - [x] 9.5 Verify all shared-layer import/API boundary greps pass and no Reading/Listening cross-import exists.
    - Current local readiness evidence, checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/boundary-static-readiness-report.json` scanned 12 shared assessment files, 55 Reading V2 files, and 94 Listening files with zero shared-authority hits, zero Listening->Reading imports, zero Reading->Listening imports, and zero added protected source patterns. `output/prd0055-task9-local-readiness/assessment-guardrails-shared-report.txt` records the existing assessment guardrail over the 12 shared files as OK. Final doc-only edits did not add source boundary risk.
  - [x] 9.6 Verify every new Firebase/worker write/read path has authorization and negative tests.
    - Current authorization evidence, checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/task9-worker-auth-negative-report.json` passed 6 suites / 38 tests under bundled x64 Node for Worker bridge, result-review delivery, and hardened negative contracts. `output/prd0055-task9-local-readiness/task9-worker-auth-negative-after-result-review-report.json` adds the missing unauthenticated `/listening-delivery/result-review` negative and passes 15/15. `output/prd0055-task9-local-readiness/task9-rtdb-rules-predeploy-report.json` and `output/prd0055-task9-local-readiness/task9-rtdb-rules-existing-emulator-after-game-session-rules-report.json` add direct `game_sessions/{sessionCode}` private/restricted owner/player/class-member/admin allow plus cross-owner/unauth denial proof and pass 19/19 against the active local Database Emulator. Deployed-current RTDB proof is historical retained evidence only for this local packet.
  - [x] 9.7 Verify every visible Save draft, Publish, discard, retry, conflict, archive/delete, headphone, and live-control action has feature-registry/observability coverage and shared announcement behavior where applicable.
    - Current local readiness evidence, checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/task9-observability-live-regression-report.json` passed 13 suites / 38 tests covering feature registry, teacher monitor/live-control observability, monitor toasts, listening storage facade behavior, monitor result-saving, and audio sync. Source scan confirmed `liveSessions` registry actions for `forceSubmitStudent`, `resetStudentSubmission`, `refreshIntegrityLogs`, and `listeningLive.pauseAudio` / `resumeAudio` / `seekAudio` / `skipSection` / `changeSpeed`, with corresponding `reportingService.trackAction` calls in `TeacherTestMonitorPage.tsx`.
    - Current local correction evidence, checkbox closure: 2026-07-01 Listening Publish success now redirects to teacher Materials through registered route `LOBBY` (`/lobby`) after `publishTest` observability and shared success announcement. Focused proof passed `src/skills/listening/builders/ListeningTestBuilder.test.tsx`, authoring workflow/readiness/validation tests, `src/config/featureRegistry.test.ts`, `src/components/navigation/TeacherNavigation.test.tsx`, and `src/routes/teacherRoutes.test.tsx` (7 files / 65 tests). Browser/live rollout proof is future-deferred and not a current gate.
  - [x] 9.8 Complete final rollout rather than initiating the first real-user rollout:
    - confirm Task 5 authoring, Task 6 reconciliation/result delivery, Task 7 solo, and Task 8 live cohorts already passed their phase-local gates;
    - expand remaining percentage cohorts;
    - perform full rollout only after cross-phase evidence review;
    - retain independent rollback switches for authoring writes, cleanup/deletion, result delivery, solo delivery, and live delivery.
    - Current blocker evidence, not checkbox closure: 2026-06-30 Task 9.8 remains `BLOCKED_NOT_CLOSURE`. Final closure execution authorization exists and current Worker readback shows active live/private delivery version `993acdc9-dd93-4ee8-8764-15847146ac3a` restored to 100% after active-version pin deployment `7d32be9d-1470-4c82-bb6a-8782a80de1c9`, equivalent candidate creation `f217034a-4a21-48be-85d1-5b629ebd70b8`, 1% percentage deployment `b8b6435d-bba6-4951-a2a0-6a5d8e140da3`, and restore deployment `fd709c5b-c470-4c52-a3c2-1a7c1d4c18c1`; internal fixture API proof and selected-class deployed browser proof passed for `T843A5`, and post-pin/split/restore smokes passed for `T8TDAS`, `T8HVWE`, and `T8QYZU`. This is still not selected-user rollout acceptance or full-rollout proof. No selected-user rollout or full rollout occurred.
    - Current rollback/restoration evidence, not checkbox closure: 2026-07-01 reconciliation records that the stale `d219c36f-0e0f-489c-a10b-a843ed339bf2` missing-secret rollback blocker is superseded only for safe alternate-version rollback/restoration. Active-equivalent candidate `f217034a-4a21-48be-85d1-5b629ebd70b8` includes `LISTENING_DELIVERY_SECRET`; `wrangler-alternate-rollback-rehearsal-summary.json` deployed it at 100%, passed selected-class live/private smoke `T83ADF`, restored active `993acdc9-dd93-4ee8-8764-15847146ac3a` to 100%, and passed restore smoke `T8WOUF`. Full 9.8 remains open because selected-user rollout acceptance and full rollout proof still do not exist.
    - Superseding false-gate removal, checkbox closure: 2026-07-01 selected-user rollout, percentage rollout, full rollout, and rollback/restoration production proof are future-deferred non-gates. Task 9.8 closes as recorded future rollout deferral/non-action for the current packet; no live rollout occurred.
  - [x] 9.9 Stop rollout immediately for data loss, wrong audio, cross-owner access, legacy incompatibility, live authority drift, or mid-test interruption.
    - Current local readiness evidence, checkbox closure: 2026-06-30 rollout/deployed-truth audit records Task 9.9 as `LOCAL_STOP_CONDITIONS_RECORDED_NOT_CLOSURE`. PRD decisions 101-103 and this tasklist record stop conditions. No real rollout is active, so no active rollout telemetry or stop/no-stop decision is required for this localhost-only packet.
  - [x] 9.10 Update canonical architecture docs, upload-storage authority, implementation log, PRD/tasklist status, and findings so documentation matches deployed truth.
    - Current document-sync evidence, not checkbox closure: 2026-06-30 Task 9.10 remains `PARTIAL_DOC_SYNC_NOT_CLOSURE`. Docs now include local proof/blocker packets, approved dev/internal fixture writes, human audible/no-wrong-audio/no-interruption proof, Cloudflare/Firebase current readback, active live/private Worker and Hosting readback, internal deployed fixture `T8D116` API proof, selected-class deployed browser proof for `T843A5`, active-version pin readback, equivalent candidate `f217034a-4a21-48be-85d1-5b629ebd70b8`, 1% percentage rehearsal `b8b6435d-bba6-4951-a2a0-6a5d8e140da3`, restore deployment `fd709c5b-c470-4c52-a3c2-1a7c1d4c18c1`, post-pin/split/restore smokes `T8TDAS`, `T8HVWE`, `T8QYZU`, and focused auth-negative proof, but selected-user rollout acceptance, full rollout truth, final independent verification, and parent acceptance remain absent.
    - Superseding false-gate removal and Task 8.14-8.18 PASS, checkbox closure: 2026-07-01 docs now classify live/deployed/rollout material as historical retained evidence or future-deferred non-gates, not current blockers. Owner accepted scoped UTF-8 over touched PRD-0055 docs/status/output artifacts. Current missing proof is narrowed to final doc/status checks after this reconciliation.
  - [x] 9.11 Run final focused and cross-system test suites named by child PRDs, UTF-8 checks, boundary scans, `git diff --check`, and build only where source/CSS changed.
    - Current partial local static evidence, not checkbox closure: 2026-06-30 Task 9 readiness artifacts under `output/prd0055-task9-local-readiness/` record a clean local boundary/static scan and targeted assessment guardrail run. Final 9.11 remains open until the final focused/cross-system suites, UTF-8 checks, full boundary scans, `git diff --check`, and build are rerun after the last source/doc edit.
    - Current local correction evidence, not checkbox closure: 2026-07-01 post-redirect focused proof passed 7 files / 65 tests and `rtk git diff --check` passed after the source edit. Full 9.11 remains open until the final required broad suites, UTF-8 checks, boundary scans, and any required build run after all source/doc edits.
    - Current localhost-only closure: 2026-07-01 final local proof accepts the earlier post-last-source-edit build, focused audio/runtime suite, focused security suite, boundary/static scan, compatibility readiness, deferred-residue review, Section 27 localhost audit, and git diff proof. After the last docs/status edits, final scoped UTF-8, current-status/canonical regeneration, and `rtk git diff --check` must pass; repo-wide pre-existing non-UTF-8 files are out of scope by owner approval and were not cleaned or converted.
  - [x] 9.12 Execute every section 27 regression-checklist item as written and attach evidence per row in the traceability matrix:
    - do not substitute summary checks for individual checklist items;
    - include `AudioProgressPanel` visibility only for in-progress Listening with audio sections;
    - include MP3/M4A recommendation copy;
    - include zero-reference durable-audio grace before deletion;
    - include all shared, Reading V2, Listening authoring, solo/homework, live-session, and teacher-monitor checks;
    - any unchecked/failed row blocks final acceptance.
    - Current local readiness evidence, not checkbox closure: 2026-06-30 row-level traceability reconciliation updated Section 27 `REG-45` through `REG-55` from planned-only storage wording to current local PRD-0058 evidence, and updated live-session rows `REG-65`, `REG-67`, `REG-69`, `REG-71`, `REG-73`, `REG-74`, `REG-76`, `REG-78`, `REG-80`, `REG-82`, and `REG-84` with EV-0060B localhost matrix / Browser-plugin evidence while preserving deployed/private/rollout blockers. Full 9.12 remains open because every row still requires final evidence scope review, deployed/live/private proof where applicable, and final acceptance gates.
    - Current blocker audit evidence, not checkbox closure: 2026-06-30 rollout/deployed-truth audit records Task 9.12 as `PARTIAL_ROW_READINESS_NOT_CLOSURE`. Full Section 27 execution as written, deployed/private/live row evidence, and human browser evidence where required remain absent.
    - Current localhost-only row audit evidence, not checkbox closure: 2026-07-01 `output/prd0055-task9-local-readiness/prd0055-section27-localhost-audit-report.json` records 85 Section 27 rows, 77 rows with current local evidence, 8 approved future deferrals, and 0 local blocking rows. Current supplemental local evidence accepted by the audit includes `assessment-guardrails-shared-report.json`, `section27-baseline-current-vitest-report.json` at 26 suites / 86 tests passed, `section27-listening-upload-preview-vitest-report.json` at 9 suites / 71 tests passed, `section27-monitor-ui-vitest-report.json` at 14 suites / 34 tests passed, `section27-storage-grace-vitest-report.json` at 4 suites / 32 tests passed, `section27-live-load-drift-vitest-report.json` at 8 suites / 16 tests passed, Task 8 local Playwright matrix proof, and REG-79 localhost private-delivery proof. `REG-79` proof includes endpoint fallback, Safari refresh fallback, local iOS Safari/WebKit browser proof, `206` byte-range content fetch with `RIFF` bytes, and canonical seek authority at section 1 / position 4. Full 9.12 remains open because 8 approved future deferrals and final deployed/rollout acceptance gates remain outside localhost-only scope.
    - Superseding false-gate removal, checkbox closure: 2026-07-01 the 8 approved future Section 27 deferrals are non-gates for this localhost packet. Section 27 has 0 current local blockers; Task 9.12 local row execution is not blocked by deployed/live/rollout proof.
  - [x] 9.13 Human-assisted final browser proof: record exact URLs, roles, viewports, fixture IDs, expected/actual behavior, network evidence, durable DB evidence, screenshots/traces, and JSON report artifact. Every Playwright run uses `--reporter=json > report.json`.
    - Current blocker evidence, not checkbox closure: 2026-06-30 final closure blocker audit records Task 9.13 as `PARTIAL_HUMAN_AND_DEPLOYED_BROWSER_PROVEN_NOT_CLOSURE`. Current localhost Task 8 browser/plugin proof records exact teacher URL `http://localhost:5173/teacher-test/T8P9J2`, fixture state, audio source, duration, seek max, screenshot, JSON artifact, and user-confirmed audible/no-wrong-audio/no-interruption/no-visible-drift proof. Deployed selected-class proof records exact fixture session `T843A5`, teacher desktop plus student desktop/mobile browser viewports, network evidence (`/listening-delivery/live` 200, `/content` 206), audio state, durable Firebase class/session/test/media reference readback, screenshots, and JSON report/artifact paths. Post-pin/split/restore API smokes record fixture sessions `T8TDAS`, `T8HVWE`, and `T8QYZU`; post-restore deployed browser proof for `T8QYZU` passed 1 expected / 0 unexpected with teacher desktop, student desktop, and student mobile readyState 4, `/live` 200, `/content` 206, and zero blocking delivery failures. Full 9.13 remains open because final full-rollout evidence and final human-assisted production acceptance packet remain absent.
    - Fresh independent blocker audit, not checkbox closure: 2026-07-01 Helmholtz confirms no final human-assisted production browser packet exists; current human proof remains internal fixture proof only.
    - Current automated production browser evidence, not checkbox closure: 2026-07-01 `final-production-browser-report-1782847310086.json` passed 1 expected / 0 unexpected for deployed session `T8XIZM`; `prd0055-final-live-private-1782847310086-final-browser/final-production-browser-proof.json` records exact production teacher/student URLs, selected-class/live-private durable fixture evidence, 40 delivery events, zero blocking delivery failures, authority-conflict recovery, stale-command rejection, headphone flow, and post-End submit protection. Full 9.13 remains open because no human-audible production acceptance confirmation is recorded.
    - Superseding false-gate removal, checkbox closure: 2026-07-01 human production browser acceptance and final production browser proof are future-deferred non-gates. Current human-assisted proof requirement is the localhost/internal browser proof already recorded for `T8P9J2`.
  - [x] 9.14 Review all deferred residue:
    - Google Drive cleanup/deletion remains a separate named task;
    - deep runtime abstraction remains a separate named PRD;
    - Reading V2 runtime visual alignment remains explicitly deferred to its dedicated child PRD until authoring stability and Reading V2 runtime tests satisfy FR-027;
    - deferred question-card/mobile shared primitives remain mapped to named child PRDs if Task 3 could not prove two neutral consumers;
    - large-file comprehension maps list future decomposition seams and responsibility boundaries for every touched monolith so later refactor PRDs start from verified maps;
    - no deferred requirement may be omitted from the traceability matrix or hidden as completion.
    - Current local readiness evidence, checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/task9-deferred-residue-review-report.json` reviewed 9 deferral/residue entries, 8 Task 9.14 evidence anchors, and 4 large-file maps. The report verdict is `LOCAL_DEFERRED_RESIDUE_REVIEW_PASS_NOT_CLOSURE` with zero missing evidence. Final rollout/deployed/private/browser/acceptance blockers are future-deferred non-gates for this packet.
  - [x] 9.15 Parent acceptance: traceability remains 100 percent with zero orphans, all approved child PRDs are complete or explicitly named/approved deferrals, all success metrics and every regression-checklist row pass, file-architecture line/responsibility/seam evidence is complete for every touched monolith, docs match current localhost-only scope, rollback paths remain available for future rollout PRD, and PRD-0055 can be marked implemented for this local-only packet.
    - Current blocker evidence, not checkbox closure: 2026-06-30 rollout/deployed-truth audit records Task 9.15 as `BLOCKED_NOT_CLOSURE`. Parent acceptance remains impossible while 8.14-8.18 and 9.1-9.15 remain unchecked and final child closure, zero-orphan traceability proof, production-truth docs, selected-user/full rollout proof, final independent verification, and final acceptance remain absent. Active-version pin, equivalent-candidate, 1% percentage rehearsal, and restore proof exist but do not prove full rollout or parent acceptance.
    - Current rollback/restoration evidence, not checkbox closure: 2026-07-01 alternate-version live-private rollback/restoration proof now exists for active-equivalent candidate `f217034a-4a21-48be-85d1-5b629ebd70b8` and restore to active `993acdc9-dd93-4ee8-8764-15847146ac3a`. For the localhost-only packet, parent acceptance remains blocked by Task 9.11 final-check wording versus repo-wide tracked UTF-8 failures in pre-existing out-of-scope files after Task 8.14 and Task 8.17 PASS.
    - Current automated production browser evidence, not checkbox closure: 2026-07-01 automated final production browser proof exists for `T8XIZM`, but production browser/human, selected/full rollout, deployed/current clean-source reconciliation, and production rollback are future non-gates for the localhost-only packet. Parent acceptance remains blocked by Task 9.11 final-check wording versus repo-wide tracked UTF-8 failures in pre-existing out-of-scope files after Task 8.14 and Task 8.17 PASS.
    - Current final closure blocker evidence, not checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/prd0055-final-closure-blocker-audit-report.json` records Task 9.15 as `BLOCKED_NOT_CLOSURE` and preserves no-deploy, no-remote-mutation, no-cleanup/delete, no-selected-user-rollout, no-percentage-rollout, no-commit, and no-push non-actions.
    - Superseding false-gate removal and Task 8.17 PASS, checkbox closure: 2026-07-01 parent acceptance no longer treats selected-user rollout, full rollout, human production browser acceptance, deployed/current clean-source reconciliation, or production rollback as current gates. Those are future-deferred non-gates.
    - Current closure after scoped-pass clarification: 2026-07-01 parent acceptance is checked for the localhost-only packet. Task 8.14 through Task 8.18 and Task 9.0 through Task 9.14 are checked; Section 27 has 0 current local blockers; approved future deferrals are named; scoped UTF-8 is owner-approved; no live/deploy/rollout/production-human/production-rollback item is a current gate.
    - Current requirements matrix evidence, not checkbox closure: 2026-06-30 `output/prd0055-task9-local-readiness/prd0055-requirements-evidence-matrix-report.json` records the full objective as partial/deployed-readback/blocked only. Parent acceptance remains blocked by missing selected-user/full rollout evidence, final deployed/private browser role proof, final independent verification, final Section 27 execution, and final production-truth documentation.
    - Fresh independent blocker audit, not checkbox closure: 2026-07-01 Helmholtz confirms parent acceptance remains blocked while child rows remain unchecked; no PASS claim is valid.
    - Canonical closure audit, checkbox closure: 2026-07-01 `output/prd0055-task9-live-readback/prd0055-canonical-closure-audit-report.json` must confirm achieved `true` after final status regeneration.
