# Firebase Configuration
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your_project.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Reading V2 trusted submission endpoint
# Production uses the existing backup Cloudflare Worker instead of Firebase Cloud Functions.
# Do not leave blank for production. In Vite dev, blank derives the local Functions emulator endpoint.
VITE_READING_V2_SUBMISSION_ENDPOINT=https://r2-backup-worker.iamhuwng.workers.dev/api/reading-v2/submit
VITE_READING_V2_SUBMISSION_EMULATOR_ORIGIN=http://127.0.0.1:5001

VITE_GOOGLE_API_KEY=your_google_api_key

# Gemini API Keys for Quiz Parsing (Multiple keys for rotation)
# Get free API keys from: https://aistudio.google.com/app/apikey
# Supports up to 5 keys - system will rotate when one hits quota (1,500 req/day)
# Format: VITE_GEMINI_API_KEY_1, VITE_GEMINI_API_KEY_2, etc.
VITE_GEMINI_API_KEY_1=your_first_gemini_api_key
VITE_GEMINI_API_KEY_2=your_second_gemini_api_key
VITE_GEMINI_API_KEY_3=your_third_gemini_api_key
# VITE_GEMINI_API_KEY_4=your_fourth_gemini_api_key (optional)
# VITE_GEMINI_API_KEY_5=your_fifth_gemini_api_key (optional)

# Super Admin Configuration
# Set this to the email that should have super admin privileges
# This user will automatically get 'super_admin' role and access to all admin features
VITE_SUPER_ADMIN_EMAIL=admin@example.com

# Reporting & Observability (PRD-0037)
# Also used by Worker-backed Teacher Lobby homework assignment.
# Production must set this to the Cloudflare Worker base URL.
VITE_DIAGNOSTIC_TOKEN=your_shared_secret_here
VITE_BACKUP_WORKER_URL=<Cloudflare Worker base URL>
VITE_BUILD_VERSION=local-dev

# Listening Worker endpoints (Cloudflare Worker)
# Use the deployed Worker URL for local dev and production builds.
VITE_R2_UPLOAD_WORKER_URL=https://r2-upload-signer.iamhuwng.workers.dev
VITE_LISTENING_AUTHORING_WORKER_URL=https://r2-upload-signer.iamhuwng.workers.dev
VITE_LISTENING_UPLOAD_SESSION_WORKER_URL=https://r2-upload-signer.iamhuwng.workers.dev
VITE_LISTENING_LIVE_DELIVERY_WORKER_URL=https://r2-upload-signer.iamhuwng.workers.dev
VITE_LISTENING_SOLO_DELIVERY_WORKER_URL=https://r2-upload-signer.iamhuwng.workers.dev
VITE_LISTENING_RESULT_REVIEW_DELIVERY_WORKER_URL=https://r2-upload-signer.iamhuwng.workers.dev

# Local development setup:
# 1. Keep real machine-local values in the shared user-level env directory:
#      Windows: %USERPROFILE%\.luyentap\env\.env
#      Other OSes: ~/.luyentap/env/.env
#    Override that directory with LUYENTAP_ENV_DIR when needed.
# 2. Run `npm run env:bootstrap` to migrate an existing repo/Desktop .env without printing values,
#    or copy this template to the shared path and fill in the real values.
# 3. Run `npm run env:check` to verify required key names are present.
# 4. `npm run dev` fails fast when required config is missing instead of starting a blank app.
# 5. A repo-local .env remains supported as a fallback, but shared config is preferred so Git worktrees
#    and sibling checkouts do not each need secret copies.
# 6. Never commit real .env files to version control.
