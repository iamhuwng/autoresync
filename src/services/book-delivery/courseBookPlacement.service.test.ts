import { describe, expect, it } from 'vitest';
import { CourseBookPlacementError, createCourseBookPlacementService } from './courseBookPlacement.service';
const pins={bookId:'book-001',publicationId:'pub-001',unitVersionId:'unit-v1',sourceVersionId:'source-v1',activityId:'activity-001',activityVersionId:'activity-v1',bindingRevision:'binding-v1'};
const input={actorId:'teacher-001',courseId:'course-001',moduleId:'module-001',courseMaterialId:'course-material-001',courseOwnerId:'teacher-001',publicationOwnerId:'teacher-001',pins};
describe('Course Book placement',()=>{it('creates/replays exact owner-bound placement and isolated keys',()=>{const api=createCourseBookPlacementService(new Map());expect(api.place(input).kind).toBe('created');expect(api.place(input).kind).toBe('replayed');const one=api.resolve({actorId:'student-001',studentId:'student-001',courseId:input.courseId,courseMaterialId:input.courseMaterialId,enrolled:true,moduleReleased:true,pins});const two=api.resolve({actorId:'student-002',studentId:'student-002',courseId:input.courseId,courseMaterialId:input.courseMaterialId,enrolled:true,moduleReleased:true,pins});expect(one.progressKey).not.toBe(two.progressKey);});it('denies owners, bare material, pins and revoked bindings',()=>{const api=createCourseBookPlacementService(new Map());expect(()=>api.place({...input,publicationOwnerId:'teacher-002'})).toThrow(CourseBookPlacementError);api.place(input);expect(()=>api.resolve({actorId:'student-001',studentId:'student-001',courseId:input.courseId,enrolled:true,moduleReleased:true,pins})).toThrow(CourseBookPlacementError);expect(()=>api.resolve({actorId:'student-001',studentId:'student-001',courseId:input.courseId,courseMaterialId:input.courseMaterialId,enrolled:true,moduleReleased:true,pins:{...pins,sourceVersionId:'source-v2'}})).toThrow(CourseBookPlacementError);api.revoke('teacher-001',input.courseMaterialId);expect(()=>api.resolve({actorId:'student-001',studentId:'student-001',courseId:input.courseId,courseMaterialId:input.courseMaterialId,enrolled:true,moduleReleased:true,pins})).toThrow(CourseBookPlacementError);});});
